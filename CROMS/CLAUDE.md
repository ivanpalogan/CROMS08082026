CROMS — Civil Registry Operations Management System

Full name: Civil Registry Operations Management System Client: LGU Peñablanca, Cagayan — Local Civil Registry Office (LCRO) Type: Desktop application (Windows Forms / C# .NET Framework 4.8) Purpose: A day-to-day operations system for the LCRO — not just a digitization tool, but the everyday workbench that staff use to register events, serve clients, issue certifications, track petitions, and submit reports to PSA.



What it does (in one paragraph)

CROMS replaces the paper-and-spreadsheet workflow of the LCRO with a single integrated desktop app. Clients get a queue number at the front desk, the receiving officer logs the transaction, the registrar processes the record (birth, marriage, death, or certification request), the cashier assesses fees against the Peñablanca Revenue Code, and the releasing officer captures the claimant's signature — all tied to one transaction ID with a full audit trail. In parallel, the office digitizes decades of old registry books through the OCR module, and generates PSA monthly submission reports automatically from the same database.



The 15 modules

🟦 Client Services (front-desk workflow)

Module	What it does

Dashboard	Live KPIs — queue length, today's registrations, collections, pending releases.

Queue Management	Issues numbered tickets (BIRTH-###, CERT-###, etc.), calls next client, tracks wait time.

Transactions	Master ledger of every client interaction — one TXN ID follows the client from queue to release.

Certificate Request	Intake for CTC (Certified True Copy) of Birth/Marriage/Death and Negative Certifications.

Release \& Claim	Captures claimant signature (or authorized representative + ID) on release; closes the transaction.

🟩 Record Management (the registry itself)

Module	What it does

Birth Registration	Municipal Form 102 — full data entry for live-birth registration.

Marriage Registration	Municipal Form 97 — marriage certificate registration.

Death Registration	Municipal Form 103 — death certificate + burial permit.

Registry Books	Digital equivalent of the physical registry books, organized by year/volume/page.

Petitions (RA 9048 / 10172)	Tracks correction-of-entry and change-of-first-name petitions through their legal stages (Filed → Posted → Decision → PSA endorsement).

Record Search	Cross-record search with fuzzy / SOUNDEX matching (so "Dela Cruz" finds "dela cruz" and "de la Cruz").

🟨 Document Workflow

Module	What it does

Incoming / Outgoing	Document routing log — what came in, where it went, who signed for it.

OCR Digitization	Scans old registry pages, runs OCR, shows confidence levels, lets staff verify and commit to the database. This is how the backlog gets digitized without replacing the daily workflow.

🟧 Operations

Module	What it does

Fees \& Payments	Auto-assesses fees per the Peñablanca Revenue Code, adds any additional charge, records payment method / reference / status / tendered+change, and reconciles Official Receipt numbers with the Treasurer.

Reports \& PSA	Generates the PSA monthly submission report (BReN, MReN, DReN counts) and internal management reports.

🟥 Administration

Module	What it does

Users \& Audit Trail	User accounts, role-based access (Registrar / Staff / Cashier / Releasing), and a tamper-evident audit log of every create/update/delete.

Why it's designed this way

Realistic, not ambitious. It solves what LCRO staff actually do every day — queue, register, assess, collect, release, report — instead of chasing AI features they don't need.

Deployable. WinForms + .NET 4.8 + MySQL runs on any office PC the LGU already owns. No cloud dependency, no subscription.

Survives past digitization. Even after every old book is scanned, the app is still the daily operations system — that's the whole point.

PSA-compliant. Uses the official Municipal Form numbers (102/97/103) and produces the monthly report PSA expects.

Auditable. Every transaction has one ID, one owner, one signature trail — important for a government office.



Progress Log

### 2026-07-07 — Progress Log started
- Added this Progress Log section. From now on, meaningful changes/decisions get a dated entry here (what changed + why), updated automatically without prompting.

### 2026-07-07 — Navigation shell (sidebar + content panel, no MDI)
- Built `MainForm` as the app shell: dark left sidebar (grouped nav buttons) + top header + `contentPanel` that swaps module UserControls. Chose single-instance UserControl swapping over MDI — simpler, modern look, keeps one window.
- Content swap caches each UserControl and uses `BringToFront()`, so a module keeps its state when navigated away and back (built lazily on first open).
- Added `Modules/ModuleInfo.cs` + `Modules/ModuleRegistry.cs`: all 15 modules registered in one list (grouped as in this doc). Adding a module = one entry; sidebar builds itself from the registry.
- Added `Controls/DashboardControl.cs` (KPI cards, placeholder values) and `Controls/ModulePlaceholder.cs` (reusable "coming soon" view used by the other 14 modules until built).
- Repointed entry point `Program.cs` from `Form1` to `MainForm`. `Form1` left in place for now. Build verified (Debug).

### 2026-07-07 — Per-module designable UserControls (visible in Solution Explorer)
- Replaced the shared `ModulePlaceholder` instances with 15 real UserControls, one per module (`QueueManagementControl`, `TransactionsControl`, `CertificateRequestControl`, `ReleaseClaimControl`, `BirthRegistrationControl`, `MarriageRegistrationControl`, `DeathRegistrationControl`, `RegistryBooksControl`, `PetitionsControl`, `RecordSearchControl`, `IncomingOutgoingControl`, `OcrDigitizationControl`, `FeesPaymentsControl`, `ReportsPsaControl`, `UsersAuditControl`).
- Reason: the old placeholder was a single class reused 15×, so nothing per-module appeared in Solution Explorer and no module could be opened in the WinForms Designer. Each new control has its own `.cs` + `.Designer.cs` + `.resx`, so it shows as a nested, drag-drop-editable form.
- Each control is blank except a title label; `ModuleRegistry` factories now build the real controls. `ModulePlaceholder.cs` kept (unused) for reference. csproj updated with all 45 files. Build verified (Debug).

### 2026-07-07 — Converted module views from UserControls to real Forms
- Per user request: replaced the 15 module UserControls + `DashboardControl` with real **Forms** in a new `Forms\` folder (`namespace CROMS.Forms`): `DashboardForm`, `QueueManagementForm`, `TransactionsForm`, `CertificateRequestForm`, `ReleaseClaimForm`, `BirthRegistrationForm`, `MarriageRegistrationForm`, `DeathRegistrationForm`, `RegistryBooksForm`, `PetitionsForm`, `RecordSearchForm`, `IncomingOutgoingForm`, `OcrDigitizationForm`, `FeesPaymentsForm`, `ReportsPsaForm`, `UsersAuditForm`. Deleted the old `Controls\*Control.*` files and `ModulePlaceholder.cs`; `Controls\` folder now empty.
- Reason: user wanted each module to be a first-class Form (shows the standard Form icon, opens on the drag-drop Designer surface with Toolbox + Properties grid). UserControls were functionally identical to design but the user's mental model is Forms.
- Kept the one-window navigation: `MainForm.ShowModule` now embeds each Form in `contentPanel` with `form.TopLevel = false; form.FormBorderStyle = None; form.Dock = Fill; form.Show()`, cached per module. `ModuleInfo.Factory` is now `Func<Form>`; `MainForm._cache` is `Dictionary<string, Form>`.
- `DashboardForm` keeps the KPI-card code (built in ctor after `InitializeComponent`). All forms have `.cs` + `.Designer.cs` + `.resx`. csproj updated. Build verified (Debug).

### 2026-07-07 — Database: chose Option B (fresh clean `croms` schema), scripts drafted
- Scanned the imported legacy DB `ocr_record_sytem` (from `NEWOCR.sql`, an older narrower OCR prototype). Found: table `" baranggay"` with a leading space, `hostpital`/`MerriageID` typos, plaintext passwords in `tblaccount`, text-typed dates (`tblbirth.DateOfBirth varchar(50)`), redundant place columns + `parent`/`residents` stubs, conflicting `tblaccount`+`tblusers` with a `Student` role enum, and ~9 CROMS tables missing (transactions, audit_log, fees/payments, petitions, certificate_requests, releases, registry_books, routing).
- Decision (user picked Option B): build a NEW `croms` database rather than mutate the old one. Old DB is left untouched as a frozen backup; migration only reads from it. Reversible via `DROP DATABASE croms;`.
- Added `Database/` folder with `01_schema.sql` (clean schema: your Birth/Death/Marriage + lookups cleaned, snake_case, real DATE/DECIMAL types, `users` with roles Admin/Registrar/Staff/Cashier/Releasing, plus the 9 new CROMS tables + `audit_log`), `02_seed.sql` (admin placeholder + Peñablanca fee schedule with placeholder amounts), `03_migrate_from_ocr.sql` (tolerant `INSERT...SELECT` copy preserving lookup ids; text→DATE/INT/DECIMAL conversions; skips plaintext accounts), and `README.md` (run order + caveats). SQL files added to csproj as None so they show in Solution Explorer.
- Wired `App.config` with a `Croms` connection string (password placeholder `YOUR_PASSWORD`). NOT yet run against MySQL — user runs the 3 scripts in Workbench (needs their password). Build still verified (Debug).
- Follow-ups flagged: fill real fee amounts, set real BCrypt admin password when auth is built (Phase 2), review `births.date_of_birth` NULLs from unparseable legacy text dates.
- Correction after first run: legacy schema is actually named `NEWOCR` (not `ocr_record_sytem`) — migration script updated. Per user, place-of-birth/death/marriage is now split into 3 dropdown FKs (births/deaths: `hospital_id`+`municipality_id`+`province_id`; marriages: `church_id`+`place_municipality_id`+`place_province_id`), so the single-name `places_of_*` lookup tables were dropped. Legacy single place names can't auto-split → migrated records get NULL place fields (old names remain in frozen NEWOCR).

### 2026-07-07 — Database built & app connected (data layer live)
- Found the team's prior working WinForms app at `Downloads\itobago\...\OCR-Based_Record Management_System` — used as the basis. Its `MySQL.cs` (`Push`/`Pull`/`GetCount`/`IsConnected`) is the data-access pattern CROMS adopts; its old App.config held the MySQL root password (`@ivan123`).
- Ran the 3 DB scripts against local MySQL 9.3: imported `NEWOCR.sql` into schema `NEWOCR`, dropped the stale first-cut `croms`, rebuilt `croms` (place-split schema), migrated. Verified counts match: births 8/8, deaths 3/3, marriages 1/1, queue 6/6. Seed loaded: 1 admin, 10 fees. NOTE: `municipalities` + `hospitals` migrated empty (old tables had no rows).
- Added `Data/Db.cs` (adopts the old MySQL.cs pattern but reads the `Croms` connection string from App.config). Wired `App.config` with the real password. `DashboardForm` now runs a live connection test — shows "Database: Connected" + real counts (queue/births/deaths/marriages). Build verified (Debug); `MySql.Data.dll` + config confirmed in bin output.
- App is now connected to the database end-to-end. Next: build real record forms (Phase 3/4) reusing the old system's forms as reference; add auth (Phase 2, BCrypt) to replace the placeholder admin password.

### 2026-07-07 — Birth Registration form built as flagship (designer-visible + DB-wired)
- User confusion clarified: WinForms Designer only renders controls declared in the `.Designer.cs`. MainForm nav buttons + Dashboard cards are built in code at runtime, so they show only when running, never on the design canvas; the other module forms were genuinely empty. Going forward, module forms are laid out declaratively in the Designer (like the old system) so every control is visible/editable.
- Built `BirthRegistrationForm` fully in the Designer (GroupBoxes Child/Place/Parents, labels, textboxes, comboboxes, DateTimePicker, Save/Clear buttons, DataGridView list) using standard WinForms controls (old system's Bunifu/RJ custom controls not available). Wired code-behind to `births`: `LoadLookups()` binds combos from lookup tables, `LoadBirths()` fills the grid, Save inserts via parameterized `Db.Push`. Added a parameterized `Db.Push(sql, params MySqlParameter[])` overload (SQL-injection safe).
- This is the template to replicate for the other 14 module forms. Build verified (Debug).

### 2026-07-07 — Sidebar nav converted from code-gen to designer-placed (fully editable)
- Per repeated user request: moved the 16 nav buttons + 5 group-header labels out of code (`BuildNavigation`/`MakeNavButton`/`MakeGroupHeader`, now removed) into `MainForm.Designer.cs` so every button is visible and editable on the WinForms design canvas (color, font, size, drag).
- Each button keeps its module key in `Tag` and wires `Click` to a single `NavButton_Click` handler. `RegisterNavButtons()` indexes them by Tag for the active-highlight. `ModuleRegistry` still drives which Form each key shows (content), so nav and content stay in sync. Tradeoff accepted: adding a future module now needs a button placed in the Designer (Tag=key, Click=NavButton_Click) plus its registry entry, instead of just a registry line.
- MainForm already had WindowState=Maximized + MinimumSize 1200x720 (user set these in Properties). Build verified (Debug).

### 2026-07-07 — Dashboard cards converted to designer-placed controls
- Converted `DashboardForm` from code-built cards to Designer-placed controls: 4 KPI card panels (In Queue / Birth / Death / Marriage records) each with a colored stripe, value label, and caption label, plus a connection-status label — all visible/editable on the canvas. Code-behind `LoadKpis()` now only sets the label texts + connection color from live `Db` counts (removed `BuildCards`/`MakeCard`). Build verified (Debug).
- Remaining 13 module forms are intentionally left as blank title-only (per user: leave until we build them). Only Dashboard + Birth have real content so far, both now fully designer-editable.

### 2026-07-07 — Birth Registration rebuilt as full PSA Form 102 (matches web prototype)
- User showed a lovable.dev web prototype ("Civil Registry Companion") and asked to match it exactly. Rebuilt Birth Registration as the full Municipal Form 102: header (title + "Municipal Form 102" subtitle) + Save Draft / Submit for Approval buttons (top-right, anchored), Delayed Registration checkbox, a 7-tab TabControl (Child / Mother / Father / Marriage of Parents / Attendant / Informant / Certification), and a Recent Registrations grid (Registry No / Child / Sex / DOB / Book / Status). Child tab matches the prototype; the other 6 tabs use standard PSA Certificate of Live Birth fields. All controls designer-placed/editable.
- Schema: `Database/04_birth_form102.sql` expanded `births` with ~40 columns (dropped the 3 place-dropdown FKs + if_multiple_birth + weight_at_birth; place is now a single text field, weight in grams; added time_of_birth, registry_no, book_volume, status, full mother/father/attendant/informant/certification fields). Ran against `croms`; existing 8 rows set status='Registered'. NOTE: this reversed the earlier 3-dropdown place design per the prototype.
- Code-behind wires Save Draft (status 'Draft') / Submit (validates child name+sex, status 'Pending Approval') via one parameterized `Db.Push` over all 46 columns; grid lists recent births. Compile verified clean (lock-free temp build).

### 2026-07-07 — Marriage Registration overview screen (full-width, matches prototype)
- Built `MarriageRegistrationForm` as a full-screen overview (ClientSize 1690x966) matching the web prototype: title "Marriage Registration & License" + subtitle (Form 90 license / Form 97), two action buttons (+ License Application, + Register Marriage), 4 KPI cards (Licenses in Posting / Issued this month / Expiring ≤30d / COMs Registered this month), a "Licenses — Posting Period Tracker" grid, and a "Marriage Records (Form 97)" grid. Grids anchored to fill/stretch on resize.
- Schema `Database/05_marriage_form97.sql`: added registry_no/book_volume/status/solemnizer to `marriages` (existing row set 'Registered'), created `marriage_licenses` table (license_no, husband/wife, filed_date, posting_ends, status). Ran on `croms`.
- Code-behind loads KPI counts + both grids from the DB. The two buttons currently show a "coming next" message — the actual Form 97 registration entry form and Form 90 license entry form are the next builds. Compile verified clean.

### 2026-07-07 — Death Registration (Form 103) built + registration functions verified
- Built `DeathRegistrationForm` matching the prototype: header + subtitle, "Register Death" (saves) + "Print COD + Burial Permit" (stub) buttons, two GroupBoxes — Deceased Information (full name, sex, civil status, age, citizenship, date/time of death, place, religion) and Cause of Death & Disposal (immediate/antecedent/underlying cause, medical certifier, license no, disposal method, place/date of disposal, permit type) — plus a Recent Death Registrations grid. Reactive (grid anchored).
- Schema `Database/06_death_form103.sql`: relaxed deaths first/middle/last/sex to NULL, added registry_no, status, full_name, citizenship, time_of_death, place_of_death, religion_name, immediate/antecedent/underlying_cause, medical_certifier, certifier_license_no, disposal_method, place_of_disposal, date_of_disposal, permit_type. Existing rows set Registered + full_name backfilled.
- Register Death validates full name, auto-numbers registry (YYYY-D-####), saves status 'Registered' via parameterized insert.
- VERIFIED the registration functions work: ran real INSERTs for Birth (46 cols) and Death against `croms` with the exact column lists the forms use — both succeeded, then cleaned up. Birth + Death save functions confirmed working end-to-end. (Marriage screen is an overview; its Form 97 entry form is still pending behind the button.)

### 2026-07-07 — Queue Management built + verified (kiosk + call-next + live queue)
- Built `QueueManagementForm` matching the prototype: header + Pause Queue / Call Next buttons, a KIOSK panel (6 transaction buttons: R Request CTC, N New Registration, P Petition, V Verification, F Follow-up, ★ Priority Lane), a NOW SERVING panel (3 window cards), and a LIVE QUEUE — TODAY grid (Queue No / Type / Issued / Wait / Priority / Status) with Export CSV. Serving panel + Export anchored/reactive.
- Schema `Database/07_queue.sql`: added type_label, priority, window_no, served_by to `queue_tickets`.
- Functions wired + verified: kiosk issues a numbered ticket per prefix per day (R-###), Call Next pulls the next Waiting ticket with priority-lane ordering (FIELD(priority,'Priority','PWD','Senior','Regular')) and assigns it to the first free window (completing whoever that window was serving), Now Serving + Live Queue refresh from DB, Pause toggles, Export CSV writes the grid to a file. Smoke-tested against `croms`: priority ticket was correctly called before a regular one; then cleaned up.

### 2026-07-07 — OCR Digitization built with real Tesseract (based on old system)
- Integrated **Tesseract 4.1.1** into CROMS using the old system as basis: copied the managed `Tesseract.dll` + native x64/x86 (leptonica-1.80.0 + tesseract41) into `CROMS/Tesseract/`, referenced the managed dll, and the csproj copies the native dlls to `bin\...\x64` + `x86` on build. Uses the installed `C:\Program Files\Tesseract-OCR\tessdata\eng.traineddata` (falls back to a local tessdata folder).
- `Data/OcrService.cs`: wraps Tesseract with the old system's preprocessing (bicubic 2× upscale + grayscale + contrast + binarize) and also returns mean confidence. `IsAvailable()` guards missing language data.
- `OcrDigitizationForm` matches the prototype: header + Load Image / Run OCR, 3 mode buttons (Backlog / Single Doc / Endorsement), left scanned-image panel with Zoom ±/Deskew (scroll-zoom; deskew is a stub message), right Extracted Fields panel (Doc Class + confidence badge, Registry No, Year, First/Middle/Last, Sex, DOB, Place, Mother, Father) with Re-OCR / Save as Draft / Commit to Birth Registry, and an OCR Batch — Today grid.
- Flow: Load image → Run OCR (Tesseract text + confidence; best-effort regex parse of date/registry/year/sex into fields for review) → logs a row in `ocr_batch` (For Review / Low Confidence <70%) → Commit inserts into `births` (status Registered) and marks the batch row Committed. `births.sex` made nullable for partial commits. Compile clean; ocr_batch + commit inserts smoke-tested against `croms`.

### 2026-07-09 — Module trim + full CRUD (Birth/Death) + Master Files (demo-ready)
- Per user's scope decision (avoid over-scoping the defense): removed **Registry Books** and **Incoming/Outgoing** from the sidebar (registry entries + nav buttons; form files left unused/harmless). Kept Petitions + Transactions per the agency requirement.
- Added **Master Files** module (`MasterFilesForm`) — one screen with a category dropdown to CRUD every lookup table (barangays, municipalities, provinces, hospitals, churches, causes_of_death, religions, nationalities, occupations, civil_statuses, birth_orders, type_of_births, relationships, residences). Category→table is a fixed whitelist (safe). Fixes the empty Hospital/Municipality dropdowns. Replaces the old system's 14 separate lookup forms with one.
- **Birth Registration → full CRUD**: added New/Update/Delete buttons + grid row-load. Save Draft/Submit create; click a row loads all 46 Form-102 fields into the tabs; Update writes changes; Delete removes (confirm). Refactored to shared `Columns`/`ValuePlaceholders`/`SetClause` + `FieldParams()` so INSERT and UPDATE stay in sync.
- **Death Registration → full CRUD**: same pattern (New/Update/Delete + row-load) on `deaths`; registry_no + status preserved on update.
- VERIFIED against `croms`: Master Files add/update/delete on a lookup; Birth full round-trip (create Draft → update to Registered/edited → delete); Death full round-trip (create → update sex/disposal → delete). All column lists match, all clean, all cleaned up. Compile clean.
- Still pending: Marriage full CRUD needs its Form 97 entry form built (currently overview-only).

### 2026-07-09 — Client Services completed: Transactions + Certificate Request + Release & Claim (transaction spine wired)
- Built the 3 remaining Client Services modules and connected them through the `transactions` table (the spine):
  - **Certificate Request** — intake for CTC / Negative Certification. Creating a request opens a transaction (type Certification, status ForRelease) via `Db.Insert` (returns new id) and links a `certificate_requests` row. Record picker loads births/deaths/marriages by type.
  - **Release & Claim** — lists transactions with status ForRelease; officer picks one, enters claimant (owner or representative + ID), draws a **signature** (on-screen PictureBox pad → PNG bytes into `releases.signature_image`), and releases — which sets the transaction Released + the cert request Released.
  - **Transactions** — searchable/filterable master ledger of all transactions.
- Added `Db.Insert` (returns LastInsertedId) and a parameterized `Db.Pull` overload.
- VERIFIED the end-to-end spine against `croms`: Cert Request → transaction ForRelease → Release inserts release + closes transaction Released + cert request Released → ledger reflects it; cleaned up. Compile clean.
- KNOWN SEAMS (by design / pending, not bugs): Queue issues tickets but does NOT auto-create a transaction (queue = waiting line; transaction is created when a named service starts at Cert Request/registration). Fees is not built, so Cert Request goes straight to ForRelease (payment step skipped). No auth yet → `created_by`/`released_by` are NULL. Marriage Form 97 entry, Petitions, Record Search, Fees, Reports, Users/Login still unbuilt.

### 2026-07-09 — Fixed stale-data bug: auto-refresh cached modules on navigate
- Symptom: Release & Claim's Pending list stayed empty after creating a Certificate Request, because module forms are cached in `MainForm._cache` and only loaded their data in the constructor (first open); revisiting just `BringToFront()` without re-querying. Transactions only looked fine because it has a manual Refresh button.
- Fix: added `Modules/IRefreshable` interface (`RefreshData()`); `MainForm.ShowModule` now calls `RefreshData()` on the cached form every time a module is navigated to. Implemented on DashboardForm, BirthRegistrationForm, DeathRegistrationForm, TransactionsForm, CertificateRequestForm, ReleaseClaimForm — so navigating to any of them always shows live data (no manual refresh needed). Compile clean.

### 2026-07-14 — Full technical audit (state of the project, verified against live `croms` DB)

**Module status (7 working, 2 partial, 4 empty stubs):**
- Working: Dashboard, Queue Management, Release & Claim, Transactions, Birth Registration (full CRUD), Death Registration (full CRUD), OCR Digitization, Master Files.
- Partial: Certificate Request (creates OK but hands off to broken Fees); Marriage Registration (overview only — both action buttons are stubs, NO create/edit).
- Empty stubs (title-only forms, not wired): Petitions, Record Search, Reports & PSA, Users & Audit Trail.

**KNOWN BUGS / TECHNICAL DEBT (ranked):**
- 🔴 **C1 — Fees & Payments is dead against the live DB.** `fees`/`payments` are defined TWICE with conflicting shapes: `01_schema.sql` (live: `fees.code/description/amount/is_active`, `payments.discount_type/cashier_id`) vs `10_fees.sql` (`fees.name/amount`, `payments.fee_name`). `10_fees.sql` uses CREATE IF NOT EXISTS so it NEVER applied — the live tables are the 01 shape. But `FeesPaymentsForm` was written for the 10 shape: `AssessFee()` does `SELECT amount FROM fees WHERE name=@n` and `RecordPayment()` inserts `payments(...fee_name...)` — both reference columns that do not exist → runtime crash (AssessFee has no try/catch; called from OnSelect on row click). VERIFIED: 0 payments ever recorded, 1 txn stuck at ForPayment. All `fees.amount = 0.00`. Because CertificateRequest now sets txn status 'ForPayment' and routes to Fees, the CTC spine is broken end-to-end. FIX: delete 10_fees.sql, rewrite the 2 queries to the 01 shape (look up fee by CODE e.g. `CTC-BIRTH`/`NEG-CERT`), fill real amounts, add try/catch. (Note: this reverses the 2026-07-09 "goes straight to ForRelease" seam — the flow now includes a payment step that doesn't work.)
- 🔴 **C2 — No authentication or audit.** `Program.cs` opens straight to MainForm, no login. Users & Audit Trail is an empty stub. `audit_log` table exists but nothing writes to it. `transactions.created_by`, `releases.released_by`, `payments.cashier_id` always NULL. The system's core "one owner, one signature trail" claim is currently unbacked. `users.password_hash` is still the `PLACEHOLDER_SET_ON_FIRST_RUN` seed. Build LoginForm (BCrypt), `Session.CurrentUser`, `Audit.Write(...)` helper.
- 🟠 **H1 — Registry-number race/gap.** `DeathRegistrationForm.NextRegistryNo()` uses `COUNT(id)+1` → duplicates after a delete or under concurrency. CertRequest's `NextTxnCode()` already does it right with `MAX(seq)+1` — copy that. Birth doesn't auto-number at all.
- 🟠 **H2 — Marriage has no entry form.** Overview dashboard only; marriages can be viewed (migrated rows) but never created/edited in-app. One of 3 core PSA record types has no CRUD.
- 🟠 **H3 — String-concatenated SQL** in hot paths (`WHERE window_no = " + window`, `LIKE '" + prefix`, `FROM " + table`). Not exploitable today (ints / whitelisted), but fragile next to the safe parameterized style.
- 🟠 **H4 — Lookup tables nearly empty** (live: hospitals 2, municipalities 1, barangays 2). Birth/Death forms use pick-only dropdowns from these, so staff can't record most places. Seed real Peñablanca data before any demo.
- 🟡 M1 — `deaths` stores names twice (legacy `first/middle/last` + new `full_name`); only `full_name` is written now, structured cols are dead weight.
- 🟡 M2 — Place-of-X denormalized into one comma-joined `place_of_birth`/`place_of_death` text (split back on ", "), discarding the original hospital/municipality/province FKs; corrupts if a value contains a comma.
- 🟡 M3 — `OcrService.Preprocess` uses per-pixel GetPixel/SetPixel on a 2× image, on the UI thread — slow, blocks. Use LockBits + background thread.
- 🟡 M4 — Dead/misleading artifacts: `Form1.cs` (superseded), `10_fees.sql` (never applied), orphan tables `document_routing`/`registry_books` (no forms), FeesPaymentsForm builds UI in code (empty .Designer.cs) unlike every other module.
- 🟡 M5 — Two parallel status vocabularies: `transactions.status` (enum) vs `queue_tickets.status` (free text: Waiting/Serving/Completed/Pending Approval/For Receiving), no DB constraint on the latter.

**Ratings (1-10):** Code Quality 6, Maintainability 6, Readability 8, Scalability 5, Performance 5, Security 2, Database Design 5, UI/UX 6, WinForms Architecture 8, OCR 6, Overall Readiness 4.5. Strong capstone skeleton; NOT deployable yet (broken money path, no auth, 4 empty modules).

**Recommended roadmap order:** (1) Fix C1 Fees + fill fee amounts → makes the CTC spine work. (2) Auth + audit (C2). (3) Fix H1 registry numbers. (4) Seed lookups (H4). (5) Build Marriage Form 97 CRUD (H2). (6) Then Petitions / Record Search (SOUNDEX) / Reports & PSA (BReN/MReN/DReN). (7) Polish: OCR speed (M3), remove dead code (M4).

**Design decisions worth preserving:** the transaction-spine model; the embedded-Form shell (TopLevel=false + cache) instead of MDI; the `IRefreshable` navigate-refresh pattern; parameterized `Db` helpers; Master Files whitelist; kiosk/display as separate apps sharing only the DB.

### 2026-07-14 — Phase 0 DONE: fixed Fees & Payments (C1), CTC spine now works end-to-end
- Root cause was the duplicate/conflicting table defs. Fix:
  - Deleted `Database/10_fees.sql` (never applied; it was the source of the shape conflict). Live schema is the `01_schema.sql` shape: `fees(code,description,amount,is_active)`, `payments(transaction_id,or_number,gross_amount,discount_type,discount_amount,net_amount,cashier_id,paid_at)`.
  - `FeesPaymentsForm.AssessFee()` rewritten to map the certificate request to a fee CODE (Birth CTC→`CTC-BIRTH`, Marriage→`CTC-MARRIAGE`, Death→`CTC-DEATH`, Negative→`NEG-CERT`) and `SELECT amount, description FROM fees WHERE code=@c AND is_active=1`. Now wrapped in try/catch with a 0-fee fallback so a row click can never crash the cashier window (was previously an unhandled throw from OnSelect).
  - `FeesPaymentsForm.RecordPayment()` INSERT rewritten to the real `payments` columns (`or_number, gross_amount, discount_type, discount_amount, net_amount`); `discount_type` = 'Senior' when the RA 11261 checkbox is ticked else 'None'; `cashier_id` left NULL until Phase 1 auth sets `Session.CurrentUser`.
  - Filled live fee amounts: CTC-BIRTH/MARRIAGE/DEATH = 155.00, NEG-CERT = 210.00. **TODO: confirm these against the actual Peñablanca Revenue Code (placeholders, realistic but unverified).**
- VERIFIED: `dotnet build` clean (0 warn/0 err — needs `-p:GenerateResourceMSBuildArchitecture=CurrentArchitecture` when building via dotnet SDK instead of VS, because of the WinForms resx x86 task-host quirk). Smoke-tested the exact form queries against `croms`: Birth CTC → fee 155 → payment insert (senior −20% → net 124) → txn advanced ForPayment→ForRelease; test rows cleaned up.
- Remaining C1 follow-ups (minor): Senior vs PWD not distinguished (both store 'Senior'); `payments.cashier_id` NULL until auth. Next up: Phase 1 (auth + audit).

### 2026-07-14 — Roadmap re-ordered per user: auth/audit deferred to LAST (pure defer, no stub)
- User prefers to finish all functional modules first, then add login + audit trail at the very end (no interim Session/Audit stub — accepted the retrofit cost). New order: Phase 0 Fees ✅ → Phase 2 correctness → Phase 3 Marriage Form 97 CRUD → Phase 4 stubs (Reports & PSA, Record Search, Petitions) → Phase 5 (LAST) Auth + Audit + polish.

### 2026-07-14 — Phase 2 (part 1) DONE: registry-number correctness (H1)
- **Death** (`DeathRegistrationForm.NextRegistryNo`): replaced `COUNT(id)+1` with year-scoped `MAX(CAST(SUBSTRING_INDEX(registry_no,'-',-1) AS UNSIGNED))+1` over `registry_no LIKE 'YYYY-D-%'`. Count+1 produced duplicates after a delete / under concurrency; max+1 does not. Format unchanged (`YYYY-D-####`).
- **Birth**: previously never auto-numbered (`registry_no` left blank). Added a matching `NextRegistryNo()` (`YYYY-B-####`) and now assign it when a record is saved as anything other than Draft (Submit → Pending Approval, or Update to a non-draft status) and the field is empty. Drafts stay unnumbered.
- VERIFIED: build clean (0/0). Smoke test against `croms` (isolated year 2099): inserted 0001/0002/0003, deleted 0002 → old count+1 would reissue 0003 (collision with existing), new max+1 returns 0004. Cleaned up.
- KNOWN residual: max+1 still has a tiny race window if two registrars submit the same instant (single-office desktop use → acceptable; true fix = counter table / SELECT ... FOR UPDATE, deferred).
### 2026-07-14 — Phase 2 (part 2): generic lookups seeded; geo lookups awaiting LGU data
- Added `Database/10_seed_lookups.sql` (idempotent — inserts each value only if the name isn't already present, so it never duplicates existing/legacy rows; values editable in Master Files). Seeded national-standard lists: nationalities (→12), religions (→15), occupations (→18), civil_statuses (→6), type_of_births (→4), birth_orders (→10), relationships (→15). Ran against `croms`, verified.
- Left the messy legacy religion rows in place ('HOLIC', 'CNI', 'INC', 'Muslim') — NOT deleted because `deaths.religion_id` may reference them; user can prune in Master Files.
- Per user (chose "seed generics now, you give geo"): the local geo tables — `barangays` (Peñablanca's official ~24), `hospitals`, `churches`, and neighbouring `municipalities` — are official LGU data and will be seeded from the list the user provides. NOT fabricated. This is the one remaining Phase 2 item before all the place-of-birth / residence / place-of-death dropdowns are fully usable.
- Phase 2 code work (registry numbers) is complete; only the geo seed remains, then Phase 3 (Marriage Form 97 CRUD).

### 2026-07-14 — Phase 3 DONE: Marriage Registration Form 97 CRUD (H2)
- Marriage was the only core record type with NO data entry (overview dashboard + two stub buttons). Built it.
- Added `MarriageEntryForm` (a code-built modal dialog) inside `Forms/MarriageRegistrationForm.cs` — self-contained, so NO new .Designer.cs/.resx and NO .csproj change (same in-file pattern as `ClientDisplayForm`). It is the PSA Form 97 entry screen: top block (registry no auto / book / status / date / time / solemnizer), Husband + Wife group boxes (first/middle/last, age, optional DOB, citizenship, religion, civil status, residence), and Place of Marriage (church / municipality / province). Create / Update / Delete + Cancel.
- Wiring in `MarriageRegistrationForm`: "+ Register Marriage" button (`btnRegister_Click`) opens a blank dialog; clicking a row in the Marriage Records grid opens it loaded (added a hidden `id` column + `CellClick`). On close, `RefreshAll()` re-pulls KPIs + both grids.
- Schema note: `marriages` stores lookups as FK **ids** (husband/wife citizenship_id, religion_id, residence_id; church_id, place_municipality_id, place_province_id) — UNLIKE Birth/Death which store lookup text. So the dialog uses `FkCombo` (bound to the lookup table, DisplayMember=name/ValueMember=id, with a leading blank=id 0 → saved as NULL). civil_status is VARCHAR (plain text combo).
- Registry numbering: `YYYY-M-####`, same year-scoped MAX+1 pattern as Birth/Death; assigned on save when status != Draft and the field is blank.
- Compile note: removed `TextBox.PlaceholderText` (that property is .NET Core 3.0+, NOT available on .NET Framework 4.8) and renamed my `Update()`/`Validate()` methods to `UpdateRecord()`/`ValidateNames()` (they shadowed Form's built-ins). Build clean 0 warn/0 err.
- VERIFIED against `croms`: full round-trip — insert (with real FK ids citizenship=Filipino, religion=Roman Catholic; auto reg 2026-M-0001) → update (solemnizer + wife civil status) → grid query returns the couple → delete. Cleaned up.
- STILL a stub (by scope, not a record type): the Form 90 Marriage License application (`btnLicense`) — the posting-period tracker reads `marriage_licenses` but there's no entry form yet. Lower priority than the 4 empty modules. Next: Phase 4 (Reports & PSA, Record Search, Petitions).

### 2026-07-14 — Domain research: PSA ↔ LCRO process (informs the Reports module)
- CORRECTION to the top-of-file pitch wording: **BReN / MReN / DReN are Birth/Marriage/Death REFERENCE NUMBERS printed on the PSA security paper (SECPA)** of an authenticated certificate (with QR + serial) — they are NOT the monthly report's count fields. The monthly thing the LCRO sends PSA is a **transmittal/endorsement of the registered vital events** for the period.
- Registration chain: informant → LCRO registers the event + keeps the registry book → monthly transmittal to PSA (via Provincial Statistical Office → OCRG) → PSA later issues the nationwide authenticated copy on SECPA. The "Certificate of Live Birth" (MF102) and a "PSA birth certificate" are the SAME record — original vs authenticated copy. A CROMS `births` row = the COLB; Certificate Request just issues a CTC of it.
- Three registrable events / forms, each accomplished in quadruplicate: Birth = MF102 (COLB), Marriage = MF97 (COM), Death = MF103 (COD).
- Reglementary (on-time) period: RA 3753 — birth must be registered within 30 days; past that = late/delayed registration (extra affidavits). Schema already has `births.is_delayed`, so the monthly report SHOULD split timely vs delayed.
- Implication for Reports & PSA module design: a monthly Report of Registered Vital Events per event type = counts (births/marriages/deaths registered in the chosen month) + timely-vs-delayed split (from `is_delayed`/registration date) + a detail roster (registry no, name, date of event, date registered, status) + collections summary (SUM `payments.net_amount`) + CSV/print export. All buildable from existing tables.
- UNVERIFIED (confirm with the office): exact 4-copy distribution of the quadruplicate, and whether Peñablanca submits via eCRS/eCivil (electronic) or physical packets.

### 2026-07-14 — Phase 4 (module 1) DONE: Reports & PSA + fixed a real GetCount bug
- **NEW BUG found + fixed (was undermining several "working" modules):** `Db.GetCount(sql)` ran the caller's SELECT through `ExecuteScalar`, which returns the FIRST ROW's value, not a row count. Every caller passes `"SELECT id FROM ... WHERE ..."` expecting a count, so they got the first id instead. Confirmed live: `GetCount("SELECT id FROM births")` returned 3, actual count 11; deaths returned 5, actual 3. This corrupted Dashboard KPIs, Marriage license KPIs, AND the kiosk/OCR ticket numbering (`GetCount(...LIKE prefix) + 1` → first id + 1, i.e. wrong/duplicate-prone ticket numbers). Fix: one line in `Db.GetCount` — wrap as `SELECT COUNT(*) FROM (<sql>) AS _croms_count`, so all 12 call sites get a true row count without touching them. Verified each caller wants a count (dashboard counts, `+1` numbering, window-busy boolean). Re-verified wrapped counts correct (births 11, deaths 3).
- **Reports & PSA module built** (was an empty stub): `ReportsPsaForm` (UI built in code; designer holds only the title; implements `IRefreshable`). Month + Year pickers → Generate produces the monthly Report of Registered Vital Events: KPI cards (births / marriages / deaths registered that month, by registration date = `created_at`; collections = SUM `payments.net_amount` for the month), the births Timely-vs-Delayed split (from `is_delayed`, RA 3753 30-day period), and a detail roster grid switchable by event type (Births/Marriages/Deaths) with Export CSV for the submission packet. Uses local `Scalar`/`ScalarDec` over `Db.Pull` with `COUNT(*)`/`SUM(...)` (not the id-returning GetCount).
- VERIFIED against `croms`: build clean 0/0; report for 2026-07 → births 11, marriages 1, deaths 3, timely 11 / delayed 0, collections 0.00 (no payments yet), roster populated. Registry_no shows NULL for the migrated rows (they were never numbered); newly registered records get numbers from Phase 2.
- NOTE: report counts "registered this month" by `created_at`; migrated historical rows all share the migration date (2026-07), so they all land in July for now — fine for the demo. Format is the sensible standard (user had no specific PSA layout to match); refine columns later if the office provides the exact form.
- Phase 4 remaining: Record Search (UNION + SOUNDEX), Petitions (CRUD + stages). Then Phase 5 (auth + audit, last).

### 2026-07-14 — Phase 4 (module 2) DONE: Record Search (UNION + SOUNDEX fuzzy)
- Built `RecordSearchForm` (was empty stub; UI in code, designer holds only title; implements `IRefreshable`). One search box + type filter (All/Birth/Marriage/Death) + Fuzzy(SOUNDEX) checkbox. Live search on text change.
- Builds a `UNION ALL` across births/marriages/deaths, each sub-query returning the same 6 columns (Type, id[hidden], Registry No, Name, Event Date, Status). Blank term shows all (LIMIT 300). Matching: name `LIKE %term%`; when Fuzzy on, also `SOUNDEX(last_name)=SOUNDEX(@q)` (marriages: husband/wife last names; deaths: full_name). Parameterized (@like, @q reused across sub-queries — MySql.Data substitutes all occurrences).
- Double-click a result → `Shell().GoToModule(birth|marriage|death)` to open that record's module, with a dialog naming the registry no + name to click. NOTE: this is a navigate-to-module jump, not a deep record-open (would need public prime methods on the 3 forms) — acceptable for now, flagged as possible polish.
- VERIFIED against `croms`: blank → all 15 (11 births + 1 marriage + 3 deaths); exact 'Talosig' → 7; MISSPELLING 'Talosick' WITH SOUNDEX still finds the TALOSIG family (both → code T420), WITHOUT fuzzy → 0 (proves SOUNDEX is the matcher). Build clean 0/0.
- Phase 4 remaining: Petitions (CRUD + RA 9048/10172 stage tracker). Then Phase 5 (auth + audit, last).

### 2026-07-14 — Phase 4 (module 3) DONE: Petitions (RA 9048 / 10172 stage tracker) — Phase 4 COMPLETE
- Built `PetitionsForm` (was empty stub; UI in code; implements `IRefreshable`). Left = petitions list; right = create/edit panel.
- Fields: petition type (RA 9048 / RA 10172 → stored as enum codes RA9048/RA10172), record type (Birth/Marriage/Death) + record picker (loads records of that type, same pattern as Certificate Request), filed date, stage, remarks. Buttons: Save (insert/update), Advance Stage, New, Delete. Click a grid row to edit.
- Stage flow Filed → Posted → Decision → PSA_Endorsement (DB enum): "Advance Stage" moves to the next and persists; stops at the final stage. Grid shows the stage with underscores replaced by spaces.
- Grid resolves the linked record's NAME with a CASE + correlated subquery over births/deaths/marriages by `record_type`+`record_id` (no single join possible since record_id points at one of three tables).
- SEAM (by design, like the other modules pre-auth): `petitions.transaction_id` left NULL — petitions don't open a transaction / fee flow yet. Can be wired later if the office charges petition fees (fee codes PET-9048 / PET-10172 already seeded).
- VERIFIED against `croms`: insert linked to a birth → grid CASE resolved the child's name → advanced Filed→Posted→Decision→PSA Endorsement → deleted. Build clean 0/0.
- **PHASE 4 COMPLETE.** All previously-empty modules now built except Users & Audit (that belongs to Phase 5). Module status is now: 11 working (Dashboard, Queue, Transactions, Certificate Request, Release, Birth, Death, Marriage, OCR, Master Files, Reports, Record Search, Petitions — plus Fees fixed), only Users & Audit is an intentional stub pending Phase 5.
- REMAINING before demo-ready: Phase 5 (auth + login + audit-trail writes + role gating + Users & Audit screen), plus the deferred Phase 2 geo-lookup seed (awaiting the office's official barangay/hospital/church lists), and optional polish (Form 90 marriage-license entry form; OCR speed via LockBits; deep record-open from Record Search).

### 2026-07-14 — Phase 5 DONE: Authentication + Audit Trail + Role gating (C2 closed)
- **Login required.** `Program.cs` now shows `LoginForm` first and only runs `MainForm` on DialogResult.OK.
- **Password hashing = PBKDF2, not BCrypt.** Could not add the BCrypt NuGet package (packages.config project, no restore in this env), so used `Data/PasswordHasher.cs` — `Rfc2898DeriveBytes` HMAC-SHA256, 16-byte salt, 100k iterations, stored as `iterations.saltB64.hashB64` in `users.password_hash`. Built into .NET Framework, no new dependency. (Schema comment still says "BCrypt" — same intent, different algorithm; both are salted+slow.)
- **Seeded admin login: username `admin`, password `admin123`** (hash generated with the app's own PasswordHasher via reflection against the built exe, so it matches Verify exactly; self-verified correct→true / wrong→false). ⚠️ TODO: change this password before real use (do it in Users & Audit → edit admin → set password).
- New data layer: `Data/Session.cs` (`Session.User` = id/username/fullname/role; `Session.UserIdParam`), `Data/Audit.cs` (`Audit.Write(action, table, recordId, details)` → audit_log, stamped with Session user; swallows its own errors so it never blocks the real op; actions Create/Update/Delete/Login/Logout).
- **Accountability columns now filled** (were always NULL): Certificate Request stamps `transactions.created_by`; Fees stamps `payments.cashier_id`; Release stamps `releases.released_by`. All read `Session.UserIdParam`.
- **Audit wired** into: Login/Logout, Certificate Request (create txn), Fees (payment), Release, and record CRUD in Birth / Death / Marriage / Petitions / Master Files. So audit_log now fills on every meaningful action.
- **Users & Audit Trail screen built** (`UsersAuditForm`, was empty stub): tab 1 = user CRUD (add / edit / reset password via PasswordHasher / activate-deactivate, unique-username 1062 handled); tab 2 = audit log joined to users, newest 500. 
- **Role gating** in `MainForm`: `ApplyRoleAccess()` hides sidebar buttons not allowed for the signed-in role (Admin=all, Registrar=records+workflow+reports+masterfiles, Staff=queue/cert/txn/search/release, Cashier=fees/txn, Releasing=release/txn). Cross-module hand-offs still work programmatically (gating only hides the menu). Header now shows the signed-in user + role and a **Logout** button (audits Logout, `Application.Restart()` back to login). Role map is a sensible default — adjust in `MainForm.AllowedKeys` if the office wants different access.
- VERIFIED: build clean 0/0; PasswordHasher round-trip (correct/wrong) via the built assembly; audit INSERT + Audit-Trail join view show 'admin' as actor; Users view lists accounts; a full stamped spine insert (transaction created_by=1, payment cashier_id=1, release released_by=1) succeeded; all test data cleaned up. Could NOT click the live GUI (no interactive desktop) — verified via compile + running the exact form queries/logic against `croms`.
- **STATE: all 15 modules now functional; auth + audit live; the transaction spine is accountable end to end.** Remaining = deferred geo-lookup seed (awaiting LGU barangay/hospital/church lists) + optional polish (change admin password; Form 90 license form; OCR LockBits speed-up; deep record-open from Search; the schema "BCrypt" comment wording).


### 2026-07-16 — Kiosk multi-service requests (one queue number, many services) + Queue refactor
- **New feature:** the client kiosk (`CROMS.Kiosk`) now lets a client bundle SEVERAL services in one visit and get ONE queue number. `KioskForm` rebuilt as a two-panel touch UI: left = tappable service cards (New Registration, CTC, Marriage Certificate, Death Certificate, Petition, Verification — catalogue-driven, add a `Service` entry to add a card); selected cards highlight (accent border/bg). Right = summary panel with optional Name/Contact, a Priority-lane checkbox, a live list of chosen services each with a ✕ remove button, and **Print Queue Ticket** which stays DISABLED until ≥1 service is selected.
- **Schema:** `Database/11_kiosk_multiservice.sql` adds `queue_ticket_services` (id, ticket_id FK→queue_tickets ON DELETE CASCADE, service_code, service_label, status default 'Pending', created_at). One `queue_tickets` row (one queue number, code `Q-###` daily sequence) + one `queue_ticket_services` row per selected service. `queue_tickets.type_label` stores the comma-joined labels for the staff live-queue display; `document_type` = first service. Backward-compatible: single-service tickets with no service rows behave exactly as before. Ran against `croms`, verified.
- **Kiosk `Db.cs`:** added `Insert(...)` (returns `LastInsertedId`) so services can be linked to the new ticket id. Submit inserts the ticket then loops the services; then prints.
- **Printing:** real `System.Drawing.Printing.PrintDocument` — prints a physical ticket (office header, big queue number, priority flag, numbered list of requested services, timestamp). Wrapped in try/catch so a machine with no printer still shows the on-screen confirmation (`ShowTicket` now lists every service under the number).
- **Staff refactor (as requested):** removed `WireWindowClicks()`, `WireWindowClick(Panel,int)`, `WindowClicked(int)` and the ctor call from `Forms/QueueManagementForm.cs` (also removed the now-orphaned `Shell()`/`OpenCertificateRequest`/`OpenNewRegistration`/`PromptRecordType` helpers that only `WindowClicked` used). The old click-a-Now-Serving-card-to-open-the-form shortcut is gone. Replaced with **`WireQueueDoubleClick()`**: double-clicking a row in the LIVE QUEUE grid opens `ShowTicketServices()`, which lists all services on that ticket from `queue_ticket_services` (falls back to `type_label` for legacy/single tickets). `LoadQueue()` now selects a hidden `id` column as the lookup key. This is how the staff app retrieves all requested services from one ticket. Call Next / priority ordering / Now Serving / Export / Client Display unchanged.
- VERIFIED: both projects `dotnet build` clean 0 warn/0 err (with `-p:GenerateResourceMSBuildArchitecture=CurrentArchitecture`). Smoke-tested against `croms`: inserted one Q-901 ticket + 2 services (CTC + Death) → staff live-queue query returns the joined type_label, retrieval query returns both services (Pending) → deleting the ticket cascaded the services to 0 orphans. Test rows cleaned up. Could not click the live GUI (no interactive desktop) — verified via compile + the exact queries the forms run.
- NOTE / trade-off: to keep the kiosk touch-first, the old mandatory webcam ID-photo capture was dropped from `KioskForm` (AForge refs left in the .csproj, harmless; re-addable if the office wants the photo back). Name/Contact are now optional. `Program.cs` unchanged.

### 2026-07-16 — Kiosk UI redesign: 3-part name + restored webcam + priority checkboxes
- Reworked `CROMS.Kiosk/KioskForm.cs` right-hand panel per request (multi-service card selection on the left is unchanged):
  - **Name split:** the single Full Name textbox is replaced by First / Middle / Last name fields. `FullName()` joins them into the existing `queue_tickets.full_name` column (First + Last required; Middle optional) — no schema change.
  - **Webcam restored** (was dropped in the previous redesign): live AForge preview in a PictureBox, "Capture / Retake" button, retake overwrites, JPEG bytes saved to `_idBytes` and written to the existing `queue_tickets.id_image` LONGBLOB via `ImageParam()`. `StartCamera`/`OnFrame`/`CapturePhoto`/`StopCamera` brought back (same logic as the original kiosk); camera stopped on form close.
  - **Priority lane** is now three checkboxes (Senior Citizen / PWD / Pregnant) instead of one. `PriorityValue()` maps them onto the SAME single `priority` column the staff app already orders by (FIELD(priority,'Priority','PWD','Senior','Regular')): Pregnant→'Priority', PWD→'PWD', Senior→'Senior', none→'Regular'. DB logic unchanged.
  - Contact Number kept (optional). The details panel is a scrollable Fill panel with the Print button docked bottom (stays disabled until ≥1 service selected).
- Everything else preserved: one `Q-###` queue number per ticket, `queue_ticket_services` rows per service, single ticket print (now lists services + priority flag), staff-side live queue / Call Next / window assignment / double-click-to-see-services all untouched.
- NO SQL change needed — `full_name`, `contact_no`, `id_image`, `priority` already exist on `queue_tickets` (from 07_queue.sql + 09_kiosk.sql). VERIFIED: `dotnet build` clean 0 warn/0 err (built to a temp OutDir because the user had the old kiosk .exe running, which locked bin\Debug — that lock was the only build error, not a code error). User must close the running kiosk and rebuild in VS to see the new UI.

### 2026-07-16 — Removed the staff-side KIOSK panel from Queue Management
- Per user: the "KIOSK — SELECT TRANSACTION" panel (6 buttons R Request CTC / N New Registration / P Petition / V Verification / F Follow-up / ★ Priority Lane + the "Ticket auto-prints on 58mm thermal printer" note) is gone from `Forms/QueueManagementForm`. Ticket issuing now lives only in the client kiosk (CROMS.Kiosk); staff no longer issue tickets from this screen.
- Removed from the Designer: `grpKiosk` groupbox, `btnCtc`/`btnNew`/`btnPetition`/`btnVerify`/`btnFollow`/`btnPriority`, `lblKioskNote` (field inits, the control-setup block, SuspendLayout/ResumeLayout/PerformLayout, `Controls.Add(grpKiosk)`, and the field declarations). Removed from code-behind: `KioskButton_Click` and the empty `grpKiosk_Enter` handler. Updated the class doc comment.
- Untouched / still working: Call Next + priority ordering, Now Serving windows, Live Queue grid (+ double-click to see a ticket's services), Pause, Export CSV, Client Display. VERIFIED build clean 0 warn/0 err.

### 2026-07-16 — Fix: widen queue_tickets.type_label (multi-service overflow)
- Kiosk submit threw "Data too long for column 'type_label' at row 1" when a client picked several services — the comma-joined labels (e.g. "New Registration, Certified True Copy (CTC), Marriage Certificate") exceed the old VARCHAR(50). `Database/12_widen_type_label.sql` MODIFYs `queue_tickets.type_label` to VARCHAR(255); applied to live `croms` and verified. `document_type` left VARCHAR(50) (holds only the single primary service). No C# change.

### 2026-07-16 — Now Serving: sequential same-ticket processing (window click)
- Re-added clickable Now-Serving window cards (`WireWindowClicks`/`WireWindowClick`) and a NEW sequential processor `WindowClicked(int)` in `Forms/QueueManagementForm.cs`. Uses the existing `queue_ticket_services` table (the spec's "separate table" — one row per service, `status` Pending/Serving/Completed, order = insertion `id`). ONE ticket number throughout; only the current form changes.
- Flow per click on the assigned window: load the Serving ticket for that window → if a service is already 'Serving', ask "mark completed?" (Yes → Completed; No/Cancel → reopen same form) → then open the first Pending service (mark it 'Serving') via the shell (`Shell()` + `GoToModule`). When no Pending remains, `FinishTicket` marks the whole ticket Completed and frees the window (staff press Call Next). The ticket stays on the Now Serving board (window not freed) until EVERY service is done; statuses are persisted so work resumes from the last unfinished service after a refresh.
- Service→module map: CTC/VERIFY→certrequest, MARRIAGE→marriage, DEATH→death, PETITION→petitions, NEWREG→prompt Birth/Marriage/Death (re-added `PromptRecordType`). Birth still links back via `PrepareForQueueTicket`. Legacy tickets with no service rows fall back to a one-shot finish.
- ADAPTATION vs the spec's "auto-open next on Save": the module forms don't call back to the queue on Save, so advancing is click-driven — after saving a form the staff click the same window again to complete it and auto-open the next. Same end result, resumable, no deep cross-module coupling. (A true auto-advance would need each form to raise a "saved for ticket N" event.)
- NO schema change (status column already on queue_ticket_services from 11_kiosk_multiservice.sql). VERIFIED: build clean 0/0; simulated a 3-service ticket at Window 2 through 4 clicks against `croms` — NEWREG→CTC→Change Status completed in order, ticket flipped Completed + window freed only after the last; cleaned up.

### 2026-07-16 — Dynamic service windows (DB-driven, unlimited) + admin module
- Removed the hardcoded 3 windows everywhere. New `Database/13_windows.sql`: `windows` table (id, window_name, status Active/Inactive, display_order, created_at), seeded ids 1-3 so existing `queue_tickets.window_no` (1..3) stays valid. `window_no` now references `windows.id`. Applied to `croms`, verified.
- **New admin module `WindowManagementForm`** (Administration group, key "windows", Admin-only via default role gating): DataGridView of windows + Add / Rename / Activate-Deactivate / Move Up / Move Down (swaps display_order) / Delete. Delete + Deactivate are blocked when a ticket is currently assigned (`IsAssigned`). Every change writes `Audit.Write`. Registered in `ModuleRegistry`, added a `btnWindows` nav button to `MainForm` (navFlow + Designer field/init/config), and a Compile entry in the csproj (code-built form, no .Designer.cs).
- **Now Serving (staff, QueueManagementForm)** is now built in a loop from the active windows: `SetupServingArea()` clears the designer's 3 static cards and drops in a wrapping `FlowLayoutPanel`; `BuildServingCards()` queries `windows WHERE status='Active' ORDER BY display_order` and makes one clickable card each (title=window_name, big ticket code, services, click → `WindowClicked(id)`). Cards fill the row for ≤4 windows and wrap for more; rebuilds on refresh and on resize. Removed `WireWindowClicks`/`SetWindow`; `RefreshServing()` → `BuildServingCards()`.
- **PickWindow** rewritten: first ACTIVE window (by order) with no one serving, else first active, or 0 if none (Call Next then warns "no active windows"). Call Next / click-flow messages now show `WindowName(id)`. `LoadQueue` LEFT JOINs `windows` to show "Serving · <window_name>".
- **Client Display board (`ClientDisplayForm`)** rewritten to a responsive `TableLayoutPanel` (up to 4 equal columns, wraps to more rows). Rebuilds the grid only when the active-window set/name signature changes (no 2s flicker); refreshes ticket labels every tick. 1 window → one big centered card; N windows → equal cells.
- No fixed limit anywhere; adding/removing/reordering a window in the admin panel changes both the staff Now Serving and the public board with no code edit. VERIFIED: build clean 0/0; DB test added Window 4 → 4 active, deactivate → 3 active, LoadQueue join resolves the name, cleanup back to 3.

### 2026-07-19 — Kiosk UX redesign: single dense screen → guided 4-step wizard
- Reason: UX review judged the old single-screen `KioskForm` wrong-shaped for the target user (rural LGU walk-ins, many elderly / first-time computer users). Everything was on one screen and the right-hand details panel was an AutoScroll `Fill` — so the webcam + selected-services list sat BELOW the fold and could be missed entirely. Overload + hidden scroll were the two worst problems.
- Rebuilt `CROMS.Kiosk/KioskForm.cs` as a **4-step wizard, one task per screen**: (1) Choose service(s) — the same catalogue-driven tappable cards; (2) Your details — First/Middle/Last + optional contact + Senior/PWD/Pregnant priority; (3) Your photo — bigger 480×360 live webcam + Capture/Retake; (4) Review & print — read-back of name/contact/priority/services + photo thumbnail, then the Print button. Persistent header shows "Step X of 4" + the step title; footer has Back (hidden on step 1) + Next, Next becomes green "🖨 Print Queue Ticket" on the last step.
- Per-step gating shows a friendly Warn ("Please tap at least one service", "Please enter your first and last name" + focuses the empty field) instead of a silently-disabled button, so the client always gets told why they can't advance.
- **Idle auto-reset added** (privacy): `IMessageFilter` + a 1s `Timer` — after `IdleSeconds` (90) of no mouse/keyboard the form clears every field, drops the photo, and returns to Step 1, so an abandoned session never leaves the previous client's name/photo for the next person. `HasProgress()` guards it (no reset on a truly-empty kiosk). Message filter removed + timer stopped in `OnFormClosing` alongside `StopCamera`.
- **User decisions taken before building** (asked): keep the webcam photo IN the kiosk (not moved to counter); English only for now (bilingual deferred); NOT a touchscreen — mouse+keyboard, so NO on-screen keyboard was added and touch-target sizing was not the constraint (buttons still enlarged for elderly readability).
- Everything DB-facing is UNCHANGED from the previous kiosk: one `Q-###` `queue_tickets` row (photo in `id_image`, priority mapped by `PriorityValue()`), one `queue_ticket_services` row per service, same print ticket + `ShowTicket` confirmation. NO schema change. Old file kept as `KioskForm.old.cs.bak` (not in the .csproj `<Compile>` list, so it does not build).
- VERIFIED: `MSBuild` (VS2019) build of `CROMS.Kiosk` clean — `CROMS.Kiosk -> bin\Debug\CROMS.Kiosk.exe`, no errors. Could not click the live GUI (no interactive desktop); logic/flow verified by compile + reading the exact queries (unchanged Submit path). User must close any running kiosk .exe and rebuild in VS to see the new wizard.
- KNOWN residual (pre-existing, not introduced): `NextNum()` still uses `MAX(number_queue)+1` — tiny duplicate-number race under true concurrency (single-kiosk office → acceptable; same residual noted for registry numbers in Phase 2).
- FOLLOW-UP same day — filled the blank space: on a maximized screen the first cut clustered each step's content top-left, leaving large empty areas. Reworked every step to CENTER + enlarge its content and reflow on resize: Step 1 cards bumped 300×150 → 348×200 in an AutoSize `FlowLayoutPanel` (MaximumSize 1160 → 3×2 grid) centred H+V; Step 2 details in a 720-wide block centred; Step 3 webcam preview 480×360 → 680×510 with a centred block; Step 4 review panel capped at 1100px wide, centred, fills screen height. Each step panel re-centres its inner content in a `Resize` handler. No logic/DB change; build clean 0/0.

### 2026-07-19 — Kiosk queue-ticket print reformatted for 58mm thermal + queue position
- Problem: the old `DrawTicket` set NO `PaperSize`, so the queue ticket printed as a tiny top-left blob on the printer's default full A4/Letter sheet, left-aligned, number too small. It's a QUEUE ticket (not a payment receipt — fees are their own module).
- Rewrote `PrintTicket` + `DrawTicket` in `CROMS.Kiosk/KioskForm.cs` as a proper **58mm thermal ticket, centred**: `PaperSize("Q58", 228, h)` (228 hundredths-inch = 58mm wide; height `h = 330 + services*24 + (priority?36:0)` estimated from line count so the roll doesn't over-feed), small margins. Layout top-to-bottom: optional `★ PRIORITY LANE ★ (lane)` badge → office header (CROMS — LCRO Peñablanca / Local Civil Registry Office / Municipality of Peñablanca) → rule → "QUEUE NUMBER" + big `Q-###` (Consolas 30pt) → rule → Name / Date / Time → rule → numbered services → rule → **"People ahead of you: N"** → rule → "Please wait… Keep this ticket." Centred lines via `MeasureString` + `StringFormat.Center` into a `RectangleF` (wraps within the roll width); `row()`/`mid()`/`rule()` local helpers.
- **Queue position:** new `AheadCount(selfId, priority, num)` counts still-`Waiting` tickets that outrank this one — anyone in a stronger lane (`FIELD(priority,'Priority','PWD','Senior','Regular')` smaller) OR same lane with a smaller `number_queue`. Matches the staff Call-Next ordering. Returns -1 on error → ticket just omits the line (never blocks printing). Computed in `Submit` after the insert (excludes self by `id`), passed to `PrintTicket`.
- Added a parameterized `Db.Pull(sql, params MySqlParameter[])` overload to the kiosk `Db.cs` (it only had the raw-string `Pull`; `AheadCount` needs params). Same safe pattern as the main app's Db.
- User decisions before building: 58mm thermal roll; show People-ahead. VERIFIED: `MSBuild` build clean (fixed one CS1501 — kiosk Db had no param Pull — by adding the overload). GUI/printer not exercised (no interactive desktop); layout/SQL verified by compile + reading the queries. Old full-sheet print behaviour fully replaced.

### 2026-07-19 — Login UI polish (eye toggle, Caps Lock hint, Exit, focus, anti-double-submit)
- Reviewed `LoginForm` + the account model at user request. Confirmed the account model is CORRECT and unchanged: accounts are created ONLY inside the app by an Admin (`UsersAuditForm`), there is NO public self-registration on the login screen — right for a government internal system (keeps the audit trail meaningful; a self-serve signup would let anyone self-assign a Registrar/Cashier role). User agreed: keep admin-only.
- Added to `LoginForm.Designer.cs` + `LoginForm.cs` (the only thing user asked to build now — login UI polish): (1) **show-password eye button** `btnEye` beside the password box (toggles `UseSystemPasswordChar`, 👁 ↔ 🙈); password box narrowed 356→316 to fit it. (2) **Caps Lock warning** `lblCaps` ("⚠ CAPS LOCK is ON", amber, hidden) driven by `Control.IsKeyLocked(Keys.CapsLock)` on load + KeyUp in both boxes — #1 cause of false "wrong password". (3) **Exit button** `btnExit` (closes with `DialogResult.Cancel` → `Program.Main` exits the app). (4) **Focus username on load**. (5) **Anti-double-submit**: `SetBusy(true/false)` disables Sign In + shows "Signing in…" during the DB check; re-enabled on every failure path. Form height 360→410 for the Exit button. Both new buttons `TabStop=false` so Tab order (user→pass→Sign In) is unchanged; Enter still submits (`AcceptButton`).
- No logic/DB/schema change to the auth itself (still PBKDF2 verify, same-message-for-unknown-user-or-wrong-password, deactivated-account block). VERIFIED: `MSBuild` build clean (`CROMS -> bin\Debug\CROMS.exe`). GUI not clicked (no interactive desktop).
- NOTED to user but NOT built (their scope choice — login polish only): default `admin`/`admin123` still needs rotating before deploy; no failed-login lockout/rate-limit or failed-attempt audit yet; no password policy (min length / confirm-password) or force-change-on-first-login on account creation. Flagged as the real security follow-ups when they want them.

### 2026-07-19 — Auth hardening: forced first-login password change + lockout + password policy
- User said "do what you think is needed" → implemented the three security follow-ups flagged above (account model stays admin-only, unchanged).
- **Force-change-on-first-login (this is what kills the default admin123):** new `users.must_change_password TINYINT(1) NOT NULL DEFAULT 1`. `Database/14_password_policy.sql` ALTERs it in (ADD COLUMN sets every existing row to 1, so the seeded `admin` is now forced to rotate on next sign-in); also added to `01_schema.sql` for fresh installs; registered in csproj. Applied to live `croms` (verified: admin.must_change_password=1). At login, after a successful verify + active check, if the flag is 1 a modal `ForceChangePasswordForm` (new, in-file in `LoginForm.cs`, `ControlBox=false` so it can't be dodged) requires a new password (min 8 + confirm), writes the new PBKDF2 hash, sets the flag to 0, and audits `Update` "Password changed on first login". Cancel aborts the sign-in (`Session.User` cleared, not admitted). Login `SELECT` now also reads `must_change_password`.
- **Failed-login lockout + audit:** `LoginForm` keeps an in-memory (per running app instance) `_attempts` map keyed by lowercased username — after `MaxFails=5` wrong tries it locks that username for `LockSeconds=60` (message "Too many attempts. Try again in Ns."), cleared on the first success. Every failed attempt now writes an audit row (`action=Login`, user_id NULL, details "Failed sign-in for '<user>'") — previously only *successful* logins were audited, so brute-force was invisible. In-memory (not a DB column) is deliberate: single-workstation desktop app, no schema needed, resets on app restart (acceptable for this threat model).
- **Password policy on account creation (`UsersAuditForm`):** added a **Confirm Password** field; `Require()` now enforces min 8 chars + match whenever a password is being set (new user OR an edit that types a new one). An admin **password reset** now also sets `must_change_password=1`, so a reset user is forced to pick their own password on next login (admin never knows the final password). New accounts inherit the DB default (1) → forced change on first login automatically.
- Auth core otherwise unchanged (PBKDF2 verify, same-message-for-unknown-or-wrong, deactivated block). VERIFIED: `MSBuild` build clean (`CROMS -> bin\Debug\CROMS.exe`); live-DB smoke test on a temp user confirmed the column defaults to 1 and the change-password UPDATE clears it to 0, then cleaned up. GUI not clicked (no interactive desktop).
- STILL open (cheap, optional): actually rotate/retire the literal `admin123` seed doc wording; the "BCrypt" comment in `01_schema.sql` line 133 is stale (it's PBKDF2). Lockout is per-username-per-instance, not per-IP/global — fine for one office PC.
- BUGFIX (same day, caught on first run): `ForceChangePasswordForm` created the two password TextBoxes (`_p1`/`_p2`) but never called `Controls.Add` on them, so the dialog showed the labels + buttons with NO input fields (invisible). Added both to `Controls`. Also renamed the primary button "Save & Continue" → "Save and Continue" (the `&` was being eaten as a WinForms mnemonic, showing "Save Continue"). Build clean; forced-change flow now usable.

### 2026-07-19 — UI polish pass 1: shell logout bar + grid-fill anchors
- User reported: overlapping Logout button, some tables not filling (blank space), wants an overall tidier UI + more button emphasis.
- **Logout overlap FIXED (root cause):** `MainForm.BuildUserBar` positioned the user label + Logout button by absolute math off `headerPanel.Width` (captured at ctor = design width) with `Anchor=Top|Right`; a long full-name label ran under the button. Rebuilt as a **right-docked, RightToLeft `FlowLayoutPanel`** — Logout is always pinned to the right edge, the variable-length name flows to its left, they can never overlap at any width. Logout restyled red (`220,53,69`) with a hover colour = more emphasis + clearer "danger" affordance. Added a 1px divider under the header.
- **Grid-fill audit across all 20 form files:** most `DataGridView`s already anchor all four sides + `AutoSizeColumnsMode=Fill` (Transactions, CertRequest, Death, Marriage, Queue, DocumentAi, Settings, WindowMgmt, RecordSearch, Birth — all OK). Found + FIXED two left-anchored ones that left a blank gap when the window widened: **Petitions** (grid `Top|Left|Bottom`→added `Right`; editor panel `Top|Left|Bottom`→`Top|Right|Bottom` so it pins right while the grid fills) and **Users & Audit** (`gridUsers` added `Right`; its editor panel already pinned right).
- STILL to do (same pattern, left for pass 2 — need multi-control re-anchoring, higher risk so deferred for user to confirm pass 1 first): **MasterFiles** (`dgvItems` is `Top|Bottom|Left`, and its right-side edit controls lblName/txtName/btnAdd/Update/Delete/New/lblHint are loose `Top|Left` — grid needs `Right`, the 7 edit controls need `Top|Right`) and **ReleaseClaim** (`grpPending` is `Top|Bottom|Left`). Also pending: a consistent primary/secondary **button-emphasis** style across the module forms.
- VERIFIED: build clean 0/0 after the shell + Petitions + Users fixes. GUI not clicked (no interactive desktop).

### 2026-07-19 — Designer-visible conversion (reversing code-built UIs) — form 1 of ~6: Users & Audit
- User reopened a long-standing request: every control must be visible/editable on the WinForms **design canvas + Properties grid**, NOT built in a `BuildUi()` code method (screenshot showed `UsersAuditForm.cs [Design]` blank except the title). Convert ALL code-built forms.
- Code-built forms found (have `private void BuildUi()`): **UsersAuditForm, PetitionsForm, RecordSearchForm, ReportsPsaForm, FeesPaymentsForm**, and **MarriageRegistrationForm** (partial — designer has the grids, BuildUi adds the KPI cards). Also `QueueManagementForm` builds the serving cards in code by necessity (dynamic, DB-driven — those can't be static designer controls). Dashboard was already converted earlier.
- **Converted UsersAuditForm (1st):** moved every control out of `BuildUi()` into `UsersAuditForm.Designer.cs` `InitializeComponent` — `tabControl` + `tabUsers`/`tabAudit`, `gridUsers`, `pnlEdit` with named labels (lblUsername/lblFullName/lblRole/lblPassword/lblConfirm), `txtUsername`/`txtFullName`/`cboRole` (Items added in designer)/`txtPassword`/`txtConfirm`/`chkActive`, `btnAdd`/`btnUpdate`/`btnNew`, and `gridAudit`. All declared as designer fields. Events wired in the designer to named handlers (`btnAdd_Click`/`btnUpdate_Click`/`btnNew_Click`/`gridUsers_CellClick`). Code-behind stripped of `BuildUi`/`Cap`/`Field`/`Btn` + the moved field declarations; kept the data methods (LoadUsers/LoadAudit/AddUser/UpdateUser/Require/ClearForm) + the 3 tiny handlers. Grid anchors kept (fill). `must_change_password=1` on admin reset preserved.
- One behaviour note: the password label text is now the literal "Password (blank = keep on edit, min 8)" in the designer (was string-built from `MinPasswordLength`); if that const changes, update the label text too.
- VERIFIED: `MSBuild` build clean 0/0. REMAINING to convert (same mechanical pattern, next): PetitionsForm, RecordSearchForm, ReportsPsaForm, FeesPaymentsForm, MarriageRegistration KPI cards.
- **Form 2 — PetitionsForm:** converted. All controls into the designer: `lblSubtitle`, `grid` (fills, TLRB), `pnlEdit` (pins right, TRB) with `lblSel`, `lblType`/`cboType` (RA labels as items), `lblRecordType`/`cboRecordType` (Birth/Marriage/Death), `lblRecord`/`cboRecord`, `lblFiled`/`dtpFiled`, `lblStage`/`cboStage` (stage labels), `lblRemarks`/`txtRemarks`, and `btnSave`/`btnAdvance`/`btnNew`/`btnDelete`. Events wired to named handlers (`cboRecordType_SelectedIndexChanged`, `btnSave_Click`, etc.). Enlarged form/panel (900×700, panel 560) so the bottom buttons aren't clipped (they were at y=504 in a 496-tall panel before). Kept `TypeCodes`/`StageCodes`/`StageLabels` in code-behind (index-mapped to the designer's combo item order — must stay in sync). Build clean.
- **Form 3 — RecordSearchForm:** converted. `lblSubtitle`, `txtQuery`, `cboType` (All/Birth/Marriage/Death), `chkFuzzy`, `lblCount`, `grid` (fills). Added a `_ready` bool guard: the designer-wired `TextChanged`/`SelectedIndexChanged`/`CheckedChanged` fire during `InitializeComponent` (when chkFuzzy.Checked / cboType default are set), so `Search()` early-returns until the ctor finishes and sets `_ready=true` (replaces the old `if (txtQuery==null)` guard). Events to named handlers. Build clean.
- STATE: 3 of 6 done (Users, Petitions, RecordSearch). REMAINING: ReportsPsaForm, FeesPaymentsForm (both have runtime-populated KPI cards — declare the card panels + value labels statically, keep the value-setting in code), MarriageRegistration KPI cards.
- **Form 4 — ReportsPsaForm:** converted. `lblSubtitle`, `lblMonth`/`cboMonth` (12 month names as items), `lblYear`/`cboYear` (years filled in ctor — dynamic), `btnExport`, the 4 KPI cards each declared statically as a `pnl*Card` panel + `stripe*` colour bar + `lbl*Val` value label + `cap*` caption (values still set at runtime in Generate()), `lblSplit`, `lblRoster`, `cboType`, `grid`. `_ready` guard (combo defaults set in ctor fire handlers before data ready). Named handlers. Build clean.
- **Form 5 — FeesPaymentsForm:** converted. Dock layout preserved: `lblHeader` (Top), `_dgv` (Left, the pending list), `pnlRight` (Fill) with `_lblSel`/`_lblFee`/`_chkDiscount`/`_lblNet`/`lblOr`/`_txtOr`/`btnPay`. Kept the underscore field names so the data code (AssessFee/Recalc/RecordPayment/PreselectTransaction) is unchanged. Named handlers (`dgv_CellClick`/`chkDiscount_CheckedChanged`/`btnPay_Click`). Build clean.
- **Form 6 — MarriageRegistration: NO conversion needed** — the *module* class was already fully Designer-based (its KPI labels `lblPostingVal`/`lblIssuedVal`/`lblExpiringVal`/`lblComsVal`, `dgvLicenses`, `dgvMarriages`, `btnRegister`, `btnLicense` are all designer controls). The `BuildUi` the grep found belongs to `MarriageEntryForm`, a runtime modal dialog (self-contained, opened on "Register Marriage") — not an embedded module surface, so it stays code-built by design.
- **DONE — every embedded MODULE form now shows all its controls on the WinForms design canvas + Properties grid.** Still intentionally code-built (correct — these are runtime dialogs / dynamic content, never the blank-canvas problem): `MarriageEntryForm` + `ForceChangePasswordForm` + `WindowAssignmentForm` (modal dialogs), and the QueueManagement "Now Serving" cards (generated from a variable number of DB windows). VERIFIED: full `Rebuild` clean 0/0.

### 2026-07-19 — UI polish pass 2: global button emphasis (hover + hand cursor) + MasterFiles grid fill
- User asked for overall UI polish, re-emphasising button emphasis + mouse-cursor change; said to check current trends. Confirmed conventions (hand cursor on clickable buttons, subtle hover tint, primary-right/secondary-left hierarchy, colour + a second cue) via web search (mockplus/logrocket/uxpin button-state guides).
- **New `Modules/UiTheme.cs` — one global button styler.** `UiTheme.PolishButtons(Control root)` recurses a control tree and for each `Button`: sets `Cursor = Hand`, and for FLAT buttons that don't already define a hover colour, adds a ~10% darker `MouseOverBackColor` + ~18% darker `MouseDownBackColor` derived from the button's own colour (so green stays green, red stays red, just darker on hover/press). Buttons that already set their own hover (the dark sidebar nav, the red Logout) are left as-is. Standard (non-flat) buttons only get the hand cursor (they have native hover). Added to csproj.
- **Applied from 3 call sites (covers the whole app):** `MainForm` ctor → nav + header buttons; `MainForm.ShowModule` right after a module form is first created → every module's buttons, centrally; `LoginForm` ctor → Sign In / eye / Exit. So every button in the app now has consistent hover feedback + a hand cursor with no per-button edits.
- **MasterFiles grid fill (the deferred pass-1 item):** `dgvItems` was `Top|Bottom|Left` and the right-side edit controls (lblName/txtName/btnAdd/btnUpdate/btnDelete/btnNew/lblHint) floated at fixed `Top|Left`, so a wide window left a big dead gap. Fixed in the ctor: grid → `Top|Bottom|Left|Right` (fills), and the 7 edit controls → `Top|Right` (pin to the right edge). ~30px gap between them stays constant; no overlap.
- STILL deferred (only remaining grid-gap): **ReleaseClaim** — `grpPending` is `Top|Bottom|Left`; its layout (grpPending + grpClaim + dgvReleased) is more intricate, left for a careful look so it isn't broken.

### 2026-07-19 — ReleaseClaim grid-gap fixed (last of the deferred layout items)
- Looked closely: the gap is NOT horizontal (left `grpPending` list + right `grpClaim`/`dgvReleased` already fill the width via their L/R anchors; `grpPending` being `Top|Bottom|Left` is correct for a fixed-width left column). The real dead space is VERTICAL in the right column — `grpClaim` is fixed-height/top-anchored (ends ~y516) while `lblReleased` + `dgvReleased` were `Bottom`-anchored, so on a tall screen the Recent Releases grid dropped to the bottom and left a growing empty band between the claim box and the grid.
- Fix (2 anchor changes in `ReleaseClaimForm.Designer.cs`): `lblReleased` `Bottom|Left` → `Top|Left` (stays pinned just under the claim box), and `dgvReleased` `Bottom|Left|Right` → `Top|Bottom|Left|Right` (its top fixed under the label, bottom grows, so it stretches to fill the whole remaining right-column height). No code-behind change.
- VERIFIED: build clean 0/0. **All grid-gap / fill items are now done** (Users, Petitions, MasterFiles, ReleaseClaim); the shell logout, global button polish, and the designer-visible conversions are complete too.

### 2026-07-19 — Professional DataGridView styling (global, via UiTheme)
- User asked for professional-looking fonts + tables/grids. Extended `Modules/UiTheme.cs`: the recursive walker now styles `DataGridView`s too (method renamed to `Polish`; `PolishButtons` kept as an alias so the 3 existing call sites — MainForm ctor, `ShowModule`, LoginForm — need no change, and every module's grids get styled automatically when the form is first shown).
- Grid style (cosmetic only, no data/behaviour change): `EnableHeadersVisualStyles=false` + light header (`#F8F9FA` bg, `#495057` bold Segoe UI 9.5, left-aligned, height 40); `CellBorderStyle=SingleHorizontal` (thin row rules, no heavy gridlines) with a light `GridColor`; zebra striping (`AlternatingRowsDefaultCellStyle #FAFBFC`); soft-blue selection (`#E8F0FE` bg / `#0D47A1` text) instead of the default hard highlight; consistent Segoe UI 9.5 cells with 8px padding; no outer border, no row headers; `RowTemplate.Height=34` only where a grid isn't already `AutoSizeRowsMode=AllCells`.
- VERIFIED: compiles clean (had to build to a temp `OutDir` because the user had the app running, which locked `bin\Debug\CROMS.exe` — that lock was the only error, not a code problem). User must close the running app + rebuild in VS to see it.

### 2026-07-19 — App-wide font consistency (Segoe UI everywhere)
- User wanted every text element on one consistent font. Added `NormalizeFont` to `UiTheme.Polish` (runs in the same recursive pass): for every control it forces the family to **Segoe UI** while KEEPING that control's size + weight (so the size hierarchy — big titles, small captions — is preserved; only the family is unified). This fixes any control that was still on the WinForms default (Microsoft Sans Serif, from forms that never set an ambient Font) or any stray family.
- Deliberately KEEPS intentional special fonts: anything containing "Emoji" (Segoe UI Emoji glyphs) and "Consolas" (the monospace queue-number / receipt displays) — so those still render correctly.
- Does NOT touch the form's own `Font` (avoids an AutoScaleMode=Font re-scale); normalises per child control instead, which is layout-safe. Applied globally via the existing `ShowModule` / MainForm / Login hooks — covers all forms + future ones. VERIFIED: compiles clean (temp OutDir again, app still running/locked).

### 2026-07-19 — Fix OCR batch table (columns not filling) — globally
- User screenshot: the "OCR BATCH — TODAY" grid (`dgvBatch` on `OcrDigitizationForm`) looked like a tiny table clustered on the left with blank space to the right. Cause: `dgvBatch` never set `AutoSizeColumnsMode` (stayed `None`), so its 5 columns kept the default ~100px width instead of stretching to the (full-width, TBLR-anchored) grid — the empty right side rendered white and blended into the form.
- Fixed in `UiTheme.StyleGrid` (global, not a one-off): if a grid's `AutoSizeColumnsMode == None`, set it to `Fill`. Columns now stretch to fill the grid width; also guards any other grid that forgot to set it. VERIFIED: compiles clean (temp OutDir, app still running).

### 2026-07-19 — Queue Management: NOW SERVING now spans full width (removed the empty-left gap)
- User screenshot showed a big empty band on the LEFT of the Queue Management top area. Cause: when the staff-side KIOSK panel was removed earlier (2026-07-16), `grpServing` ("NOW SERVING") stayed at its right-half position `Location.X=583, Width=845`, leaving x=21..583 empty.
- Fix: moved `grpServing` to `Location.X=21` and widened it to `1407` (full content width, still `Top|Left|Right` anchored). The serving-window cards are built in code to fill the row for ≤4 windows, so the 3 cards now spread across the whole width instead of clustering on the right. VERIFIED: compiles clean.
- NOTE on the other forms the user flagged: the remaining "blank space" is mostly inherent form whitespace (record-entry forms like Birth/Death/Certificate Request naturally left-align their fields, leaving the right side open) or grids showing only a few rows (fills as data grows) — not layout bugs. Genuine gap bugs (Users/Petitions/MasterFiles/ReleaseClaim grids, OCR batch columns, this Queue panel) are all fixed. Filling the entry-form right side would mean redesigning each form's field arrangement (2-column / move the records grid beside the form) — a per-form redesign, offered as targeted follow-up rather than a risky mass edit.

### 2026-07-19 — Entry-form whitespace: Certificate Request → two-column (form left, records right)
- User chose the "redesign to fill (form + grid side-by-side)" approach for the entry forms. Assessed each:
  - **Certificate Request** — WAS form-on-top (`grpDetails` 729×312 top-left) + full-width grid below, leaving ~700px blank to the right of the form. Redesigned to two columns: moved `lblRecent` + `dgvReq` into the RIGHT column (grid now `Location 770,112`, `Size 658×700`, still `Top|Bottom|Left|Right` anchored so it fills the whole right side top-to-bottom). Form stays left. Fills the right blank. Build clean.
  - **Death Registration** — NO change: `grpDeceased` (686w) + `grpCause` (699w) already sit side-by-side spanning the full width; the grid below fills width. Already good; only blank is below the grid (few rows).
  - **Birth Registration** — NO change: its `tabControl` is FULL width (1636px, `Top|Left|Right`) and the Child-tab fields already use two columns. Can't move the grid beside it without clipping the 2nd field column; the leftover whitespace is inherent to a sparse tab in a 7-tab PSA form. A true fill would require reflowing every tab's fields to a single narrow column — deferred (too invasive for the benefit).
- VERIFIED: build clean (temp OutDir, app running/locked). Net: Certificate Request materially improved; Death already filled; Birth left as-is by design.

### 2026-07-19 — Birth Registration: centered content (balanced whitespace)
- User asked to make the entry forms pleasing while keeping them working. Birth's tab + grid already fill the width; the eyesore is the sparse Child tab dumping whitespace on the right. Rather than reflow all 7 tabs (invasive, risky), added a centered-content layout in `BirthRegistrationForm.cs`: `SetupCenteredLayout()` re-anchors `tabControl` (Top), `dgvBirths` (Top|Bottom), `lblRecent` + the New/Update/Delete buttons (Top), then `CenterContent()` centers them at a comfortable max width (`maxW=1280`) with equal side margins, re-running on `Resize`. On a wide screen the content sits centered (balanced margins ≈ modern max-width container); on a narrow screen it just fills. The Save Draft / Submit header buttons stay top-right.
- Fields inside the tabs are UNTOUCHED (layout-only: Left/Width/Anchor of 6 controls), so all CRUD still works — `dgvBirths.CellClick`, save/load, and the button handlers are unchanged. VERIFIED: compiles clean (temp OutDir). `maxW=1280` > the Child tab's field extent (~1240) so nothing clips.
- STATE of entry-form whitespace: Certificate Request = two-column (fills right); Death = already fills (two groups); Birth = centered (balanced). All build clean.

### 2026-07-19 — Rounded button corners (subtle, professional) — global via UiTheme
- User asked for lightly rounded, professional button edges. Added to `UiTheme.PolishButton`: for every FLAT button, clip it to a rounded rectangle (`CornerRadius=6` — small, not pill) via `Region = RoundedRect(...)`, re-applied on `Resize` (guards against double-wiring). `RoundedRect` builds a GraphicsPath from 4 corner arcs; falls back to a plain rectangle if the button is too small for the radius. Only flat/solid buttons are rounded (standard buttons keep native chrome). Runs in the same global pass, so all action buttons get it; dark full-width sidebar nav buttons technically get it too but their corners blend into the same-coloured sidebar (harmless).
- Trade-off noted: Region clipping isn't anti-aliased, so corners are very slightly hard-edged — acceptable/standard at r=6; a fully smooth version would need owner-draw Paint per button (heavier, not warranted). VERIFIED: compiles clean (temp OutDir, app still running/locked).

### 2026-07-19 — Unified button system (kill native chrome, consistent secondary + fonts)
- User screenshots: the "white" buttons (Re-OCR, Save as Draft, Edit Window, Enable, Disable, Move Up/Down, …) were native/standard buttons — grey chrome, inconsistent with the colored flat ones, and where a border existed the rounded region notched the corners. Also inconsistent button fonts.
- Rewrote `UiTheme.PolishButton` to give EVERY button one flat style: `UseVisualStyleBackColor=false`, `FlatStyle=Flat`, `BorderSize=0`, and **bold Segoe UI at the button's own size** (unifies weight/family, keeps size hierarchy). Then classifies by BackColor: **neutral** (Empty/Control/White/near-white) → a light-grey secondary chip `#E9ECEF` with dark text (or KEEPS an intentional non-dark foreground like danger-red); **colored** → keeps its colour with white text. Both get hover/press shade + the rounded region. Because secondary buttons now have NO border, the rounded corners are clean (no notch — fixes the OCR Re-OCR/Save-as-Draft look).
- Applies system-wide via the existing global pass (MainForm/ShowModule/Login). Sidebar nav buttons are dark (non-neutral) so they stay dark; `SetActiveButton` still owns their active/idle font+colour. Kiosk/Display are separate apps (own styling) — unchanged. VERIFIED: compiles clean (temp OutDir, app still running).

### 2026-07-19 — Smooth (anti-aliased) rounded buttons — replaced Region clipping with owner-draw
- The Region-clip rounding looked jagged (a `Region` can't anti-alias). User: make it smooth, else revert to sharp. Went smooth: `RoundButton` now OWNER-DRAWS each button in a `Paint` handler — `SmoothingMode.AntiAlias` + `PixelOffsetMode.HighQuality`, `g.Clear(parent.BackColor)` to blend the corners, then `FillPath` a rounded-rect in the button's colour, then `TextRenderer.DrawText`. No more `Region`.
- Details: per-button hover/press captured in closures (MouseEnter/Leave/Down/Up → Invalidate), fill uses the FlatAppearance hover/press shades; text respects the button's own `TextAlign` via `AlignFlags` (nav stays left-aligned/indented, actions centred), keeps `&&`→`&` and `EndEllipsis`; the button is double-buffered (via reflection on `Control.DoubleBuffered`) so the owner-draw doesn't flicker on hover.
- Corner blend uses `parent.BackColor` — correct on forms/panels; on a GroupBox the corner grey can be a hair off (minor). VERIFIED: compiles clean (temp OutDir, app still running/locked). If the user still dislikes it, the fallback is to drop `RoundButton` (sharp flat buttons) — the rest of the button system (secondary chip, colours, bold font, hover) stands on its own.

### 2026-07-19 — Stub #1 DONE: Death "Print COD + Burial Permit" (was a "Coming next" message)
- Working through the open functional stubs one at a time. First: `DeathRegistrationForm.btnPrint_Click` was a placeholder MessageBox.
- Implemented real printing via `System.Drawing.Printing.PrintDocument` (added `using System.Drawing;` + `System.Drawing.Printing;`). Requires a selected/registered record (`_editingId`); re-queries `SELECT * FROM deaths WHERE id=@id` for authoritative values (incl. `registry_no`, which isn't shown in a form field), then shows a `PrintDialog` and prints:
  - **Page 1 — Certificate of Death (Municipal Form 103):** centred LGU header, then label:value rows for registry_no/status/name/sex/civil_status/age/citizenship/date+time of death/place/religion, a rule, the three causes + medical certifier + license, a rule, disposal method/place/date + permit type, a Municipal Civil Registrar signature line, and a printed-timestamp footer.
  - **Page 2 — Burial/Transfer Permit:** printed ONLY when `permit_type` is set (`e.HasMorePages`); LGU header, permit no (= registry_no), granting sentence using the disposal method, big deceased name, died-on/at line, place+date of disposal, registrar signature.
- Helpers: `V(row,col)` safe cell read, `FmtDate` (parses DATE → "dd MMMM yyyy"), `Dash` (— for blanks). VERIFIED: compiles clean (temp OutDir). GUI/printer not exercised (no interactive desktop); drawing/flow verified by compile + the query.
- REMAINING stubs (next, one at a time): Marriage Form 90 License entry (`MarriageRegistrationForm.btnLicense_Click`), geo-lookup seed (awaiting LGU lists), fee amounts vs Revenue Code, stale "BCrypt" comment.

### 2026-07-19 — Stub #2 DONE: Marriage License (Form 90) entry — full CRUD
- `MarriageRegistrationForm.btnLicense_Click` was a "Coming next" placeholder; the posting-period tracker grid could show `marriage_licenses` rows but nothing could create/edit them.
- Built `MarriageLicenseForm` — a self-contained code-built modal (same in-file pattern as `MarriageEntryForm`; NO designer/.resx/.csproj change). Fields: License No (auto `YYYY-L-####`, delete-safe MAX+1, editable), Husband / Wife full name, Filed Date, Posting Ends, Status (Posting/Issued). **Posting Ends auto-fills = Filed + 10 days** (Family Code Art. 17 posting period) for a NEW application via `dtpFiled.ValueChanged` (editable; not auto-changed when editing an existing one). Save (insert)/Update/Delete/Cancel, `AcceptButton`=Save, validates both names, audits Create/Update/Delete.
- Wiring in the module: `btnLicense_Click` → `OpenLicense(null)` (blank); added `id` (hidden) to the `LoadLicenses` query + `dgvLicenses.CellClick` → `OpenLicense(id)` (edit); `OpenLicense` shows the dialog then `RefreshAll()` (KPIs + both grids).
- VERIFIED: build clean (temp OutDir); live-DB smoke test of the exact CRUD — insert (posting_ends = filed+10), update Posting→Issued, next-number logic returns 9002 after a 9001 row, delete leaves 0 — all clean, cleaned up. This also feeds the existing Marriage KPI cards ("Licenses in Posting", "Issued this month", "Expiring ≤30d").
- REMAINING stubs: geo-lookup seed (awaiting LGU barangay/hospital/church lists), fee amounts vs Peñablanca Revenue Code, stale "BCrypt" comment in 01_schema.sql.
- VERIFIED: build clean 0/0 after each step. GUI not clicked (no interactive desktop) — hover/cursor confirmed by the code path, not visually.

### 2026-07-19 — Dashboard modernised (actionable KPIs + 7-day trend + clickable cards)
- Researched modern dashboard practice (context.dev / uxpin / medium guides): metrics must be ACTIONABLE (today-focused + context/threshold), F-pattern hierarchy, at least one trend viz, thresholds/alerts, clickable metric→action, live refresh + timestamp. Judged the old dashboard ~5/10: visually fine (cards) but it showed LIFETIME totals (all births ever, all queue tickets ever), top-left was the DB-connection label, no today/collections/pending, no trend, not clickable, KPIs didn't auto-refresh, cards fixed-position. It also didn't match the top-of-file pitch ("queue length, today's registrations, collections, pending releases").
- Rebuilt DashboardForm — user wanted the FULL modern version but **designer-visible, not code**. Everything is declared in `DashboardForm.Designer.cs` (cards/labels/panels/timer on the canvas); code-behind only fills values + draws the trend.
- **New KPI cards (all TODAY, actionable):** Waiting Now (`queue_tickets` Waiting + today — red past `WaitingAlert=10`), Registered Today (births+marriages+deaths by `created_at`=today), Collections Today (`SUM(payments.net_amount)` today, shown `₱N.NN`), Pending Releases (`transactions` ForRelease — blue when >0). Each card has a bold caption + a muted sub-line for context.
- **Clickable cards → module** (metric-to-action): Waiting→queue, Collections→fees, Pending→release, via `Card_Click` walking up to the card's `Tag` (module key) then `Shell().GoToModule(key)`; hand cursor on those cards. Registered card is not wired (no single target).
- **7-day registrations bar-trend:** designer `pnlTrend` panel + `pnlTrend_Paint` draws GDI+ bars (no chart library) from `LoadTrend()`, which sums per-day counts across births/marriages/deaths (3 grouped queries, table names whitelisted). Graceful "No registrations in the last 7 days." empty state.
- **Header context:** `lblToday` (full date, top-left = most important per F-pattern), `lblUpdated` ("Updated h:mm tt") + `lblConnection` demoted to small top-right badges.
- **Live refresh:** existing `statusTimer` (3s) refreshes the windows board every tick and the KPIs+trend every 4th tick (~12s), stamping lblUpdated. Cards/trend/windows anchored so they fill (trend Top|Bottom|Left, windows Top|Bottom|Right).
- Uses local `Scalar`/`ScalarDec` over `Db.Pull` (correct COUNT/SUM — not the id-returning `Db.GetCount` the old dashboard used). VERIFIED: build clean 0/0; smoke-tested the 4 KPI queries + a trend query against live `croms` (waiting 1, registered-today 0, collections ₱0.00, pending 2 — all run clean). GUI not clicked (no interactive desktop).

### 2026-07-19 — Added README_RUN.md (multi-app run/deploy guide) — no code change
- User saw the kiosk fullscreen with the login page + Now Serving board stacked behind it on one laptop screen and asked what to do (esp. for an admin who doesn't need kiosk/display on their monitor). Diagnosed: NOT a bug — nothing in CROMS launches the other exes (grepped, no `Process.Start`). It's Visual Studio set to **Multiple startup projects**, so F5 opens `CROMS` + `CROMS.Kiosk` + `CROMS.Display` at once, all on the primary screen. The 3 apps are correctly separate, meant for 3 machines/roles (admin laptop = CROMS.exe only; kiosk PC = kiosk; waiting-area TV = display).
- Wrote `README_RUN.md` at the repo root documenting: the three apps + which machine each runs on; the VS fix (Solution → Set Startup Projects → **Single startup project → CROMS**); deployment (one exe per machine, kiosk in Startup, all sharing the same MySQL); second-monitor note (manual drag now, auto-placement offered but not built); first sign-in (admin/admin123 → forced change); CLI build commands. Recommended answer to the admin case: run only CROMS.exe on the admin machine; don't auto-launch kiosk/display there.

### 2026-07-20 — Window Assignment / Online-Offline presence / transaction-matched routing (Phase 1 of 9-section spec)
- User spec: operators pick a service window + the transaction types they handle at login; a status dashboard shows each window Online/Offline; Call Next only routes a ticket to a window authorized for it; multi-window forwarding of multi-service tickets. **User decisions (asked up front):** (1) re-enable login — window assignment needs an operator identity (this UNDOES the "remove login for the meantime" change made earlier the same session); (2) final routing model = multi-window forwarding; (3) PHASE it. This entry is Phase 1 (presence + assignment + status on cards + transaction-matched Call Next). Multi-window auto-forwarding + full per-window indicators are Phase 2.
- **Schema `Database/14_window_assignment.sql`** (applied to `croms`, verified): presence columns on `windows` (`current_operator` INT, `operator_name` VARCHAR(80), `last_heartbeat` DATETIME) — Online = current_operator NOT NULL AND heartbeat within `StaleMinutes` (2). This is SEPARATE from `windows.status` (Active/Inactive = admin availability), so `WindowManagementForm` is untouched. New `window_transactions` (window_id FK→windows ON DELETE CASCADE, service_code, UNIQUE(window_id,service_code)); **no rows for a window = handles All Transactions.** Added `queue_ticket_services.locked_by_window` INT NULL for Phase-2 per-service forwarding lock (not yet used).
- **Login restored** (`Program.cs`): LoginForm → then `WindowAssignmentForm` dialog → MainForm. Assignment is **skippable** (admins / non-operators press Skip → enter with no window).
- **New `WindowAssignmentForm`** (`Forms/WindowAssignmentForm.cs`, code-built, no Designer; csproj Compile entry added): radio list of Active windows (occupied-by-another shown disabled: "— occupied by <name>"), transaction checkboxes (6 kiosk codes NEWREG/CTC/MARRIAGE/DEATH/PETITION/VERIFY) + "All Transactions" (mutually exclusive). Confirm re-checks occupancy at the last moment → if taken, shows **"This window is already occupied by another operator."** Else UPDATE windows (operator + heartbeat), rewrite window_transactions, set `Session.WindowId`/`WindowName`, audit. Static helpers `Heartbeat(id)` / `Release(id)` / `Catalogue` / `StaleMinutes`.
- **`Session`** gained `WindowId`/`WindowName`/`HasWindow`. **`MainForm`** starts a 30s heartbeat timer for the claimed window and `Release()`s it on Logout AND on FormClosing (so a crash/exit doesn't leave a window falsely occupied). Logout also clears Session.WindowId.
- **Transaction-matched Call Next (`QueueManagementForm`):** `btnCallNext_Click` now walks the waiting queue in priority order and assigns the FIRST ticket some eligible window can take. Eligible = ONLINE + FREE + authorized for the ticket's first pending service (`FirstPendingServiceCode` → first `queue_ticket_services` row with status Pending; null for legacy no-service tickets). `PickWindowFor(code)` / `WindowHandles(id,code)` (no window_transactions = all) / `OnlineWindowCount()` added; old `PickWindow()` removed. Distinct messages for "no online windows" vs "nothing matches an available window".
- **Now Serving cards** show operator presence: green "● Online · <operator>" or grey "○ Offline", card dimmed when offline; idle subtitle shows the window's assigned transactions (`AssignedLabel`, "All transactions" when unrestricted).
- VERIFIED: MSBuild (VS2019) build clean — `CROMS -> bin\Debug\CROMS.exe`. DB smoke test against `croms` (windows 1=CTC,2=MARRIAGE online; 3=All offline; tickets NEWREG/CTC/MARRIAGE): routing query resolved MARRIAGE→win2, NEWREG→none (no online window assigned), CTC→win1 skipped only because win1 had a real leftover 'Serving' Q-001 (correct busy behavior); occupancy lock returned locked for a different user. Cleaned up test rows (left the pre-existing real Q-001 alone). GUI not clicked (no interactive desktop) — verified via compile + the exact queries the forms run.
- **Phase 2 (next, after review):** true multi-window forwarding — after a window completes a ticket's current service, mark it Completed and re-route the ticket (same queue number) to the next window handling the next pending service, using `queue_ticket_services.locked_by_window`; plus a fuller per-window indicator (current + next ticket, operator) and a dedicated Window Status dashboard if the cards aren't enough.

### 2026-07-20 — Dashboard Service-Windows status (real-time Online/Offline) + code/designer separation
- **Task 1 — Window Status on Dashboard:** added a "Service Windows" panel to `DashboardForm` listing every Active window as 🟢 <name> - Online (with operator) or 🔴 <name> - Offline. Static host controls (`lblWindowsTitle`, `pnlWindows`, `statusTimer`) placed in `DashboardForm.Designer.cs`; the data-driven rows are filled by `LoadWindowStatus()` in code-behind (query: online = current_operator NOT NULL AND heartbeat within `WindowAssignmentForm.StaleMinutes`). A 3s `Timer` re-polls so a login/logout at any window (even another PC) shows here in near real time without restarting — no manual refresh. Wrapped in try/catch so a DB hiccup never crashes the timer. VERIFIED query returns online=1 for a signed-in window, 0 for empty ones.
- **Task 2 — proper WinForms code/design separation.** Audit finding: only 3 forms were fully code-built (no `.Designer.cs`) — `LoginForm`, `WindowAssignmentForm`, `WindowManagementForm`. Every other form already has a Designer file; the title-only modules (Reports/Search/Petitions/Fees) build *data-driven* controls in code-behind, which is correct practice (dynamic content can't live in the Designer), so they were left as-is.
- Split all 3 violators into `*.Designer.cs` (control declarations + `InitializeComponent()` + layout/properties + event wiring via `+= new EventHandler(this.handler)`) and `*.cs` (event handlers, DB ops, validation, business logic only). Behaviour + appearance preserved exactly (same locations/fonts/colors):
  - `LoginForm` — pure static UI; clean exemplar. `TryLogin` → `btnSignIn_Click`.
  - `WindowAssignmentForm` — static chrome (labels, two container panels, All-Transactions checkbox, Start/Skip buttons) in Designer; the window radio list + 6 service checkboxes stay code-built (data/catalogue-driven) into the Designer's `winPanel`/`txnPanel`. `Confirm`→`btnStart_Click`, added `btnSkip_Click`/`chkAll_CheckedChanged`.
  - `WindowManagementForm` — title/subtitle, toolbar `FlowLayoutPanel` with 6 named buttons, and `dgvWindows` grid in Designer; logic (`LoadWindows`/Add/Rename/Toggle/Move/Delete/`Prompt`) in `.cs`. Renamed `_grid`→`dgvWindows`; the `Btn()` helper + inline lambdas replaced by Designer buttons wired to `btnAdd_Click` etc. Dropped the ctor `Dock=Fill`/`BackColor` since `MainForm.ShowModule` already sets TopLevel=false + BorderStyle None + Dock Fill on embed (verified).
- csproj: added `<Compile>` entries for the 3 new Designer files with `<DependentUpon>` so VS nests them under their form.
- VERIFIED: `MSBuild -t:Rebuild` clean — `CROMS -> bin\Debug\CROMS.exe`, 0 errors. All event handlers remain wired; no feature removed. GUI not clicked (no interactive desktop) — verified by clean rebuild + the exact status query against `croms`.

### 2026-07-20 — CROMS.Display board shows Online/Offline (not just "idle")
- `CROMS.Display/DisplayForm.cs` `Reload`: when a window has no Serving ticket, it now queries the window's presence (`current_operator` NOT NULL AND heartbeat < 2 min) and shows "● Online — idle" (green) or "○ Offline" (grey) instead of a bare "idle". Serving state unchanged (ticket + services). Same change was already applied to the main app's in-process `ClientDisplayForm`.
- KNOWN LIMITATION (unchanged): CROMS.Display is still hardcoded to 3 windows and reads `window_no` 1..3 directly, not the dynamic `windows` table — so it won't reflect windows added/renamed/removed in the admin module. Flagged to user; make dynamic (like the main-app board) if wanted. Build clean (MSBuild → CROMS.Display.exe).

### 2026-07-20 — Settings module (dynamic Window Management + searchable User Manual)
- New `SettingsForm` module (key "settings", Administration group) with a TabControl — replaces the standalone "Service Windows" nav entry. `MainForm.Designer` `btnWindows` repointed Tag "windows"→"settings", text "Settings". Gated to **Admin + Registrar** (`MainForm.AllowedKeys` — added "settings" to Registrar; Admin already full). Proper split: `SettingsForm.Designer.cs` = UI (tabs, grid, toolbar, manual search/list/content), `SettingsForm.cs` = logic only.
- **Schema `Database/15_window_description.sql`** (applied to `croms`): `windows.description` VARCHAR(255) NULL — optional free-text, informational only.
- **Tab 1 Window Management:** grid (Window ID / Window Name / Description / Status) + Add / Edit / Enable / Disable / Move Up / Move Down / Delete. Add & Edit open a new self-contained `WindowEditDialog` (code-built, no Designer — same pattern as MarriageEntryForm) with Window Name, optional Description, and Active/Inactive **radio** default status. Edit blocks setting Inactive while the window is serving; Disable blocked while serving.
  - **Delete validation (stricter than the old module):** `IsInUse` blocks deletion when an operator is logged in (Online: current_operator NOT NULL + fresh heartbeat) OR any non-Completed ticket is assigned to the window today. Blocked message: "This window cannot be removed because it is currently assigned to an operator or has active queue transactions." Otherwise confirms with "Are you sure you want to remove this service window? This action cannot be undone." then deletes + audits.
  - Non-Admin/Registrar reaching the tab get the toolbar disabled (`ApplyManageAccess`) — defence on top of the module gating.
- **Tab 2 User Manual:** search box + topics ListBox + RichTextBox content. ~24 topics across Login / Queue Management / Window Management / Settings / Reports / FAQ (all 6 FAQ questions from the spec). `FilterManual` filters by keyword over category+title+body (case-insensitive) live on TextChanged; selecting a topic shows it with bold headings. Content is data-driven (`BuildManual()` in code-behind).
- **Dynamic propagation already satisfied** by prior work: Login (WindowAssignmentForm), Dashboard status panel, Now Serving board, and Call Next routing all read the `windows` table live, so a window added/removed in Settings appears everywhere with no restart or code change, and the layouts already wrap for any number of windows.
- `WindowManagementForm.cs`/`.Designer.cs` are now unreferenced (dropped from the registry) but still compile — left in place; can be deleted later. csproj: added `SettingsForm.cs` + `.Designer.cs` (DependentUpon).
- VERIFIED: MSBuild clean 0/0. DB smoke test against `croms`: add window with description → edit rename/desc/status → IsInUse returned BLOCK with an operator online, OK-delete once cleared → delete + cleanup. GUI not clicked (no interactive desktop) — verified by build + exact form queries.

### 2026-07-20 — Admin re-verification gate for Window Management CRUD + audit stamping
- **New `AdminVerificationForm`** (`Forms/AdminVerificationForm.cs` + `.Designer.cs`, proper split): secure re-auth dialog (Username / masked Password / Verify / Cancel). Validates against `users` via the existing PBKDF2 `PasswordHasher` (never plaintext, never stored/logged). Wrong creds → "Invalid username or password."; valid but role not Admin/Registrar → "You do not have permission to manage service windows."; deactivated → blocked. On success exposes `VerifiedUser` and returns OK.
- **SettingsForm gate:** Window Management CRUD controls (`bar`) start **locked/disabled**; a yellow "🔒 Verify to Manage Windows" button (`btnUnlock`) shows while locked. Verification is prompted once on first entry to the Window Management tab (`OnLoad` + `tabs_SelectedIndexChanged`, guarded by `_promptedOnce`) and re-triggerable via the unlock button. On success `SetLocked(false)` enables Add/Edit/Enable/Disable/Move/Delete for the rest of the session. Cancel → stays locked, user remains on Settings. Every CRUD handler also has a defensive `if (!_verified) { RequestVerification(); ... }` guard.
- **Session-scoped:** `_verified` lives on the cached SettingsForm instance; logout does `Application.Restart()` (new process, fresh instance), so verification is required again next session — exactly as specified.
- **Audit stamping:** every window op (Add/Edit/Enable/Disable/Delete) and the unlock event now append `Stamp()` = " [by <verifier-username> · <role> · PC: <MachineName>]" to the audit `details`, on top of the existing `audit_log` row (user_id → username/fullname/role via the Users & Audit join, action enum, record_id, created_at). Enable/Disable audits now read "Enabled '<name>'" / "Disabled '<name>'". Covers the spec's audit fields (user, full name, role, action, window, timestamp, device).
- **Startup flow** already compliant (unchanged): `Program.cs` shows LoginForm first, exits if not authenticated, and only then constructs MainForm — MainForm is never created/shown before a successful login, no preload.
- csproj: added AdminVerificationForm `.cs`/`.Designer.cs`. VERIFIED: MSBuild clean 0/0. Only account in `croms` is `admin`/Admin (verify accepts it); Registrar accounts can be added in Users & Audit. GUI not clicked (no interactive desktop) — dialog reuses the same query/hasher as the working LoginForm.

### 2026-07-20 — Document AI module: OCR classify + field extract + auto-fill (Birth phase 1)
- **Decisions (asked up front):** offline engine = **Tesseract OCR + rule/label heuristics** (no cloud AI — civil-registry PII stays on the machine); **images first** (JPG/JPEG/PNG/BMP/TIF), **PDF deferred** (project has no PDF lib, NuGet unreliable here); **Birth first**, full pipeline end-to-end, Marriage/Death classified but extraction is a later phase.
- **`Data/DocumentAI.cs`** (engine, no UI): `Analyze(Bitmap)` → OCR via existing `OcrService`, then classify + extract. Classification is a modular list of `DocProfile` (strong/weak marker phrases per type) scored against the OCR text — identifies by LABELS/headings, not fixed coordinates, so it tolerates PSA form revisions; new doc types (CENOMAR, court orders…) plug in by adding a profile. Unknown → caller shows the required "Unable to identify…" message. `ExtractBirth` uses label-based `ValueAfter(labels…)` (reads the field label, takes the value on the same line or the next), regex for registry/certificate no, `SplitName` (handles "LAST, FIRST MIDDLE" and space-separated), `NormDate` → yyyy-MM-dd. Returns `DocField{Key,Label,Value,Uncertain}` (Uncertain when OCR<80% or empty) + `Map()`. `LoadImage` caps huge scans to 2200px and throws a friendly NotSupported for PDF.
- **`Forms/DocumentAiForm.cs` + `.Designer.cs`** (proper split): Upload Document button (OpenFileDialog) → image preview (PictureBox Zoom) + OCR runs on a background `Task` with a marquee progress bar. Shows "Document Detected: ✔ <type>", a confidence line (Classification% / Recognition%[OCR] / Extracted / Missing), and an **editable review grid** (Field / Value / Status ✔ green · ⚠ amber-highlighted uncertain · — missing). If OCR<80% pops a "please review" prompt. **Auto-Fill Birth Form** button walks to the shell, `GoToModule("birth")`, and calls the new `BirthRegistrationForm.PrimeFromExtraction(dict)` with the operator-edited grid values (nothing saved — operator reviews + Saves).
- **`BirthRegistrationForm.PrimeFromExtraction`**: ClearForm → maps RegistryNo/Child names/Sex/DOB/TimeOfBirth/PlaceOfBirth/Father+Mother names/Nationality onto the Form-102 controls (txtRegNo, txtFirstName…, cboSex, dtpDob, dtpTime, txtPlace, txtF*/txtM*, _cboMCit/_cboFCit), sets status Draft, jumps to the Child tab. Non-mapped fields left for the operator.
- Wired: `docai` module in Document Workflow group, `btnDocAI` nav button (MainForm.Designer), Registrar+Admin access, csproj entries for DocumentAI.cs + the form.
- **VERIFIED end-to-end against the REAL pipeline** (not just compile): built a throwaway harness that rendered a birth-cert image (labels+values) and called `DocumentAI.Analyze` using the built binaries + installed Tesseract. Result: Kind=Birth, ClassifyConf 99, OCR 94, 15/20 fields correct — names split first/middle/last, Sex=Male, DOB normalised to 2026-01-15, place/parents/nationality/informant all extracted; the 5 "missing" were fields not present in the test image (cert no, time, date registered, registrar, remarks). Harness deleted after. MSBuild clean 0/0.
- **Deferred (next phases):** PDF support (needs a bundled rasterizer/text-extractor); Marriage & Death field extraction + auto-fill into their forms; deskew/rotation correction beyond Tesseract's own; future doc types via new DocProfiles.

### 2026-07-20 — Document AI: tuned Birth extraction to the REAL PSA Form-102 scan
- User uploaded a real COLB scan; the first-cut generic label search (and the legacy OCR Digitization module) got almost nothing (only Sex; "Year" grabbed the form's "Revised 2007"). Found the actual file (`Downloads/Birth Certificate.jpeg`), ran the real OCR, and rewrote `DocumentAI.ExtractBirth` to be **PSA Municipal Form 102 structure-aware** instead of flat label search.
- Real OCR reality (72% conf): each numbered field's VALUE is on the line BELOW its label, prefixed by the vertical section band letters (C/H/…, M/O/T/…, F/A/T/…) plus stray marks; names come as 3 cells (First|Middle|Last); DOB is "12 June 2018"; registry no + time + informant are HANDWRITTEN (OCR can't read → correctly left blank).
- New logic: `FindIdx` anchors on the numbered labels (`1. NAME` child, `MAIDEN` mother, `14. NAME` father), `NameAfter` scans the next ≤3 lines for the value, `StripPrefix` drops leading section letters / stray marks, `SplitNameCells` maps tokens to cells (last=Last, 2nd-last=Middle, rest=First → handles 2-word first names like "SHELLIAN CLEAR"). Sex by keyword; DOB via `FindDmy` ("d MMMM yyyy" → yyyy-MM-dd); place via the `P.?ACE OF` anchor's next line; weight via `\d{3,5} gram`; **type of birth read off the weight VALUE row** (not the whole text — the label line "(Single, Twin, Triplet, etc.)" was making it misread "Triplet"; fixed to "Single"); nationality = Filipino; registry `20\d{2}-\d{3,5}` (blank when handwritten). Added Weight + TypeOfBirth to `PrimeFromExtraction` (txtWeight, cboTypeOfBirth).
- **VERIFIED on the real scan** (harness on built binaries + installed Tesseract): Kind=Birth 99%, OCR 72%, 15 fields extracted, ALL correct vs the document — child SHELLIAN CLEAR/BALOSO/TALOSIG, Female, 2018-06-12, Cagayan Valley Medical Center…, Single, 2722g, mother SHEILA/ARTICULO/BALOSO, father GILBERT/CATAGGATAN/TALOSIG, Filipino. The 3 blanks (registry/time/informant) are handwritten on the form. All fields flagged ⚠ for review because OCR<80% (correct — spec wants review below 80). Harness deleted after; MSBuild clean 0/0.
- NOTE: the older `OcrDigitizationForm` (separate legacy parser) still uses its weak extraction — Document AI is the tuned path. Could retrofit OcrDigitization to call DocumentAI later.

### 2026-07-20 — OCR Digitization now uses the shared DocumentAI engine
- Per user: pointed the legacy `OcrDigitizationForm` at `DocumentAI` so it reads the same fields the same way as the Document AI module (no more weak local `ParseFields`). `RunOcr` now calls `DocumentAI.Analyze(_image)` → sets Doc Class from the classification, `FillFromDocAi` maps the extracted fields onto the screen's textboxes (RegistryNo/Child first-middle-last/Sex/DOB/Place; Mother & Father textboxes get the joined First+Middle+Last via `JoinName`; Year derived from the DOB), clears the review fields first so a prior scan doesn't linger, keeps the confidence badge + `ocr_batch` logging (uses `r.RawText`/`r.OcrConfidence`). Removed the old regex `ParseFields`.
- So the OCR Digitization screen in the screenshot (which previously got only Sex + a wrong "Year 2007") now extracts the full PSA Form-102 field set like Document AI (child/mother/father names, sex, DOB, place, registry when printed).
- VERIFIED: compiles clean (built to a temp OutDir — the normal `bin\Debug` copy was blocked only because the user had the app running, i.e. a file lock, NOT a code error). Extraction accuracy already verified on the real `Birth Certificate.jpeg` in the prior entry (DocumentAI unchanged). User must close the running CROMS.exe and rebuild in VS to see it.

### 2026-07-21 — Certificate Request now auto-fills from kiosk input when served from queue
- BUG: `QueueManagementForm.OpenServiceForm` called the prefill (`PrepareForQueueTicket`) ONLY for the `birth` key. Serving a CTC/Verification ticket opened Certificate Request via `GoToModule("certrequest")` but never prefilled it, so staff re-keyed the client's name even though the kiosk already captured it. `CertificateRequestForm.PrepareForQueueTicket(ticketId, ticketCode)` existed and worked but was never wired to the queue.
- FIX: threaded the ticket code through `OpenServiceForm(serviceCode, ticketId, ticketCode)` (3 call sites in `WindowClicked` pass the existing `code`); after `GoToModule` it now also does `else if (key == "certrequest" && form is CertificateRequestForm cert) cert.PrepareForQueueTicket(ticketId, ticketCode)`.
- RESULT: serving a CTC/Verify ticket from a Now Serving window auto-fills Certificate Request from `queue_tickets` — full_name split into First/Middle/Last, contact + photo shown, "Linked to queue ticket: Q-###" badge. Purpose stays blank (kiosk doesn't collect it). No schema change (purpose/contact_no/id_image already on queue_tickets from 09_kiosk.sql).
- VERIFIED: compiles clean to temp OutDir (EXIT 0); `bin\Debug` copy blocked only by the running CROMS.exe (file lock, not a code error). User must close the running app + rebuild in VS to see it.

### 2026-07-21 — Certificate Request layout fix (photo/grid overlap)
- BUG: the lazily-created Client Photo box (`EnsurePhotoBox`) sat at (770,118) — the SAME right column as `lblRecent` (770,90) + `dgvReq` (770,112). The photo overlapped Record Type / the grid, crowding the form.
- FIX (2 files): moved Request History to a full-width bottom row — `dgvReq` → (21,435) size 1407×381 (kept all-sides anchor so it fills on resize), `lblRecent` → (21,412). Repositioned the photo to the top-right column — `picClient` (1168,112) 260×283 + `lblPhotoCap` (1168,90), both anchored Top|Right so they hug the right edge and never cover the fields or grid. grpDetails unchanged (top-left, all inputs visible).
- Result matches the requested layout: Request Details left / Client Photo right / Request History grid full-width bottom. No overlaps; controls resize proportionally on maximize. Build clean 0/0.

### 2026-07-21 — Document AI: Marriage + Death field extraction + auto-fill (were classify-only)
- Context: user asked what the ✔ / ⚠ / — icons in the review grid mean and to extend extraction to Marriage & Death. Icons: ✔ green = extracted + confident (OCR≥80% and value found); ⚠ amber = value found but uncertain (OCR<80%) → must review before save; — grey = missing/blank. (A 71% scan flags every field ⚠ by design.)
- Previously only Birth had `ExtractBirth`; Marriage/Death were classified but returned no fields ("later phase"). Implemented both:
  - **`DocumentAI.ExtractMarriage`** (PSA Municipal Form 97): anchors on HUSBAND / WIFE headings → 3-cell name split (First/Middle/Last) via the shared `SplitNameCells`/`NameAfter`; Date of Marriage (`FindDmy`), Place of Marriage + Solemnizing Officer (label→next line), Citizenship (Filipino keyword), Registry no (YYYY-NNN pattern; usually handwritten→blank). Keys: RegistryNo, Husband/Wife First/Middle/Last, DateOfMarriage, PlaceOfMarriage, Solemnizer, Nationality.
  - **`DocumentAI.ExtractDeath`** (PSA Municipal Form 103): deceased name (3 cells + joined FullName), Sex, Civil Status (keyword→normalized), Age (`\d+ years`), Date of Death (`FindDmy`), Place of Death (label→next line), Citizenship, Registry no. Keys: RegistryNo, Deceased First/Middle/Last, FullName, Sex, CivilStatus, Age, Citizenship, DateOfDeath, PlaceOfDeath.
  - Wired both into `Analyze` (else-if on Kind).
- **Auto-fill routing (`DocumentAiForm`):** the Auto-Fill button is now enabled for all three types (label switches Birth/Marriage/Death). `btnAutoFill_Click`: Birth→`GoToModule("birth")`+`PrimeFromExtraction`; Death→`GoToModule("death")`+ new `DeathRegistrationForm.PrimeFromExtraction`; Marriage→opens the `MarriageEntryForm` modal (Form 97 entry is a dialog, not an embedded module) primed via new `MarriageEntryForm.PrimeFromExtraction`, then refreshes the marriage overview.
  - **`DeathRegistrationForm.PrimeFromExtraction`**: ClearForm then sets txtFullName (explicit FullName or joined names), txtAge, cboSex/cboCivil (item match), `_cboDCit` citizenship via `SetLookup`, `_pod` place via `SplitToTriple` (best-effort), dtpDod. Added `using System.Linq`.
  - **`MarriageEntryForm.PrimeFromExtraction`**: sets txtReg, txtSolemnizer, husband/wife First/Middle/Last, dtpDate, citizenship for both spouses + church via new `SelectFkByName` (matches DataTable-bound Fk combo by display name), status→Draft.
- VERIFIED: MSBuild clean 0/0. Birth extraction accuracy already proven on a real scan (prior entry). Marriage/Death extraction built to the PSA Form 97/103 label structure but NOT yet verified against a real Marriage/Death scan image (none on hand) — accuracy should be confirmed when a sample COM/COD image is available; the label anchors may need the same tuning ExtractBirth got against the real COLB.

### 2026-07-21 — Smart Learning Library (shared reference store + auto-learn + autocomplete)
- Built the "intelligent Document AI + learning library" feature. Recognition/mapping/confidence already existed (DocumentAI + PrimeFromExtraction + Uncertain flag); the NEW piece is a shared, self-growing reference library.
- **Schema `Database/16_learning_library.sql`** (applied to `croms`, verified): `reference_library` (id, category, value, normalized, usage_count, created_at, updated_at) with `UNIQUE(category, normalized)`. `normalized` = case+diacritic-folded key so "Peñablanca"/"PENABLANCA"/"penablanca" collapse to one row (proper spelling kept in `value`, usage bumped). "Peñablanca, Cagayan" folds to a DIFFERENT key (superset) — exact-normalized dedup only, not fuzzy superset matching (documented limitation).
- **`Data/LearningLibrary.cs`** (new service): `Normalize` (lower + FormD diacritic strip + punctuation→space + collapse), `Learn(category,value)` (INSERT … ON DUPLICATE KEY bump; best-effort, never throws so it can't block a real save), `LearnMany`, `LearnCorrection`, `Suggest(category,prefix)` (usage_count DESC, prefix via normalized LIKE), `Source` (AutoCompleteStringCollection), `Attach(TextBox,category)` (suggest-append autocomplete + learn-on-Leave), `EnsureSeeded` (once per process, only when table empty — seeds from lookups municipalities/provinces/barangays/hospitals/churches/nationalities/occupations + distinct names/places from births/deaths). ~17 categories as string consts (PlaceOfBirth/Marriage/Death, Municipality, Province, Barangay, Hospital, HealthCenter, Church, Cemetery, FuneralHome, EventLocation, Nationality, Occupation, Surname, GivenName, Officer).
- **Auto-learn on Document AI fill:** `DocumentAiForm.btnAutoFill_Click` now calls `LearnFromExtraction(kind, GridValues())` — GridValues are the OPERATOR-EDITED values, so a correction (e.g. "Penablanca"→"Peñablanca", "Sta Maria"→"Sta. Maria") is what gets learned and becomes the preferred spelling next scan (satisfies "continuous learning from corrections"). Maps each extracted key to categories (PlaceOfBirth→PlaceOfBirth+Hospital, names→GivenName/Surname, Solemnizer→Officer, etc.).
- **Confidence threshold 80→90%:** all three extractors flag ⚠ below 90 now (spec wanted <90 review); the review prompt message updated to 90%.
- **Autocomplete wired across modules** via `LearningLibrary.Attach`: Birth (txtPlace→PlaceOfBirth, first/last name boxes incl. father/mother→GivenName/Surname), Death (txtFullName→Surname, txtDispPlace→Cemetery, txtCertifier→Officer), Marriage entry (spouse first/last, solemnizer), Certificate Request (txtFirst/txtLast). Each Attach also learns on Leave, so manual typing grows the library too — shared store, every module reads+writes the same table.
- csproj: added `Data\LearningLibrary.cs` (Compile) + `16_learning_library.sql` (None). VERIFIED: MSBuild clean 0/0; live-DB dedup test — "Peñablanca"+"PENABLANCA" collapsed to 1 row (count 2), "Peñablanca, Cagayan" separate; cleaned up.
- SCOPE NOTES / not built (documented for honesty): per-field confidence is still the single OCR mean (not a separate score per field); Municipality/Province are not extracted as separate fields (carried inside the place string); fuzzy "same-location" matching beyond exact-normalized dedup is not implemented; seeding runs lazily on first Attach/EnsureSeeded. GUI not clicked (no interactive desktop) — verified by build + SQL.

### 2026-07-21 — Fuller Birth extraction + Document AI writes new lookup values into Master tables
- User: Document AI should fill EVERY Birth field incl. the ComboBox ones (Place of Birth hospital/muni/prov, occupation, religion), and any NEW value must persist so it appears as a selectable option in the registration ComboBoxes afterwards. Key design point: combos accept AI values by inserting them into the bound Master-File lookup table (Birth combos store TEXT and `SetLookup` already adds a missing item — so the mechanism just needed the extra extraction + a persist step).
- **DocumentAI.ExtractBirth — more fields (15→21):** splits "Place of Birth" ("Hospital, Municipality, Province") into `PlaceHospital`/`PlaceMunicipality`/`PlaceProvince`; adds `Religion` (keyword→canonical spelling, applied to both parents) and `MotherOccupation`/`FatherOccupation` (block scan between the MAIDEN and "14. NAME" anchors against a common-occupations list). New helpers `ReligionOf` + `OccupationIn` + `Occupations[]`.
- **`LookupStore` (new, in LearningLibrary.cs):** `Ensure(table, name)` inserts a value into a whitelisted (id,name) Master-File table if no case/diacritic-normalized match exists (dedup via `LearningLibrary.Normalize`); returns true if newly added; best-effort (never throws). Whitelist = the 14 real lookup tables (hospitals, municipalities, provinces, barangays, churches, nationalities, religions, occupations, residences, relationships, birth_orders, causes_of_death, civil_statuses, type_of_births). Confirmed each needs only `name` (rest auto/nullable).
- **`BirthRegistrationForm.PrimeFromExtraction` rewrite:** now fills the lookup combos via new `PrimeLookup(combo, table, libCategory, value)` = `LookupStore.Ensure` (persist) + `LearningLibrary.Learn` + `SetLookup` (select now). Fills Place-of-Birth triple (`_pob` hospitals/municipalities/provinces), both citizenships (`_cboMCit/_cboFCit`), both religions (`_cboMRel/_cboFRel`), both occupations (`_cboMOcc/_cboFOcc`). Dropped the dead `Set(txtPlace,...)` (txtPlace is hidden behind the triple combos). So the Child-tab Place-of-Birth boxes + Father/Mother occupation/religion combos now auto-fill (were blank before).
- **Design answer to "textbox in Document AI, combo in Registration":** implemented as persist-into-lookup-table rather than swapping controls — the registration combos stay pick-only ComboBoxes, but any AI-extracted value is added to their source table so it's immediately selectable (and dedup'd), which is the same end result and keeps one consistent data path.
- **VERIFIED end-to-end on the REAL `Birth Certificate.jpeg`** (harness on built binaries + Tesseract): Kind=Birth 99%, OCR 72%, now **21 extracted / 3 missing** — Hospital=Cagayan Valley Medical Center, Municipality=Tuguegarao City, Province=Cagayan, Mother Occupation=Housewife, Father Occupation=Driver, Religion=Roman Catholic, plus all the previously-working fields; the 3 blanks (Registry/Time/Informant) are handwritten. `LookupStore.Ensure` harness: insert new=True, "zz test clinic zz"/"ZZ TEST CLINIC ZZ"=False (case/diacritic dedup). Test row + harness cleaned up. MSBuild clean 0/0.
- KNOWN limitation (documented): a value newly inserted into a lookup table shows in OTHER registration forms after they are next built (fresh app run / first open), because cached form combos fill in their ctor — not re-queried on navigate. The active auto-filled Birth form shows it immediately (SetLookup). Cross-form same-session live refresh not implemented (would need combo reload in RefreshData; deferred to avoid resetting in-progress selections).

### 2026-07-21 — Softcopy: save the scanned certificate with the record + reprint the original (Birth/Marriage/Death)
- User asked (via Document AI flow): also save the PICTURE (scanned certificate) so the ORIGINAL can be reprinted, and keep the softcopy in the system for all 3 certifications.
- **Schema `Database/17_scan_softcopy.sql`** (applied to `croms`): `scan_image LONGBLOB NULL` on `births`, `marriages`, `deaths`.
- **`Forms/SoftcopyViewer.cs`** (new, reusable, code-built): shows the stored scan (PictureBox Zoom) with a **Print Original** button that scales the image to fit the page (aspect preserved) via `PrintDocument`+`PrintDialog`; graceful "No softcopy saved" state; `SoftcopyViewer.Show(bytes, caption, owner)` helper. csproj: added as Compile (SubType Form).
- **Document AI handoff:** `DocumentAiForm` now keeps the ORIGINAL uploaded file bytes (`_scanBytes = File.ReadAllBytes(path)` — the real JPEG/PNG, best for reprint) and passes them to the target form on Auto-Fill: `birth/death.SetScanImage(bytes)` before priming; `MarriageEntryForm.SetScanImage(bytes)` before ShowDialog. Auto-fill message now notes the scan will be saved on Save.
- **All 3 forms wired** (same pattern): `_scanImage` field + `public SetScanImage(byte[])` + `SaveScan(id)` (`UPDATE <table> SET scan_image=@img WHERE id=@id`, `MySqlDbType.LongBlob` — same proven pattern as kiosk `id_image`) called after insert (Death/Marriage switched from `Db.Push` to `Db.Insert` to get the new id) and after update; loaded back in the row-load (`_scanImage = (byte[])r["scan_image"]`), cleared in ClearForm. **"View Softcopy" button** added to each (Birth: beside New/Update/Delete, positioned in CenterContent; Death: left of Save, Top|Right; Marriage dialog: in the action row, visible when editing) → opens `SoftcopyViewer` from the in-memory scan or, for a saved record, re-queried by id.
- So the flow: Document AI upload → Auto-Fill → the original scan rides along → on Save it's stored in the record's `scan_image` → "View Softcopy" reopens + prints the exact original anytime. Works for Birth, Marriage, Death.
- VERIFIED: compiles clean to temp OutDir (EXIT 0); `bin\Debug` copy blocked only by Visual Studio holding CROMS.pdb (VS open) — NOT a code error; user rebuilds in VS. Blob path reuses the kiosk's working LongBlob pattern; columns confirmed present on all 3 tables.

### 2026-07-23 — Fees & Payments redesign: removed Senior/PWD discount, added real payment processing
- **Domain reason:** researched how the LCRO actually handles CTC payment — collection is the Municipal **Treasury's** job (they issue the numbered Official Receipt, an accountable form audited by COA/BLGF), not the cashier workbench. Senior/PWD discounting (RA 11261) is applied at the Treasury too. So the in-app "Senior/PWD 20%" checkbox was both out of place and duplicating Treasury logic — removed it. (The OR-number field STAYS — it's the legal audit anchor CROMS reconciles against; the office debate about dropping it was resolved "keep it".)
- **Removed:** the `_chkDiscount` checkbox + all discount math (`chkDiscount_CheckedChanged`, the `disc = gross*0.20`/`discountType` logic in Recalc/RecordPayment) and the old `_lblFee`/`_lblNet` labels.
- **Added payment-processing fields** (all designer-visible in `FeesPaymentsForm.Designer.cs`): **Payment Summary** group (Document Fee value, editable Additional Fee, Total = gross+additional, Amount Tendered, auto-computed Change) + **Payment Details** group (Payment Method combo: Cash / GCash (future) / Bank Transfer (future); Reference No.; Official Receipt No.; Payment Status combo: Paid / Pending / Cancelled / Refunded) + auto Processed By (Session.User.FullName) + auto Date & Time Paid + multiline Remarks. Layout widened to a two-group balanced panel (dgv Left 540, pnlRight Fill) — no dead space.
- **Recalc:** Total = document fee + additional; Change = tendered − total (never negative shown). `ParseMoney` tolerates blank/garbage. **Validations** (only when status=Paid): OR number required; Cash → tendered ≥ total; non-Cash → reference number required. Non-Paid statuses save the row but do NOT advance the transaction to ForRelease.
- **Schema `Database/18_payment_fields.sql`** (idempotent via an information_schema-guarded throwaway procedure — MySQL has no ADD/DROP COLUMN IF [NOT] EXISTS): on `payments` ADD `payment_method`,`reference_no`,`status`,`additional_fee`,`amount_tendered`,`change_amount`,`remarks`; DROP `discount_type`,`discount_amount`. Applied to live `croms` (verified column list). `01_schema.sql` payments table updated to match for fresh installs. `net_amount` kept = total (Reports' `SUM(net_amount)` collections still valid).
- VERIFIED: MSBuild clean (`CROMS -> …CROMS.exe`, 0 err). Migration applied to `croms`; ran the form's exact 12-column INSERT inside a transaction (Cash/Paid, gross 155 + add 20 = total 175, tendered 200 → change 25) — succeeded, ROLLBACK'd. GUI not clicked (no interactive desktop). Only remaining reference to the old discount is this log + the removed migration column names.

### 2026-07-23 — Fees & Payments: removed Payment Status + added 58mm payment-slip print
- Per user: this office never processes pending/cancelled/refunded at the cashier window — every recorded payment is Paid. Removed the `_cboStatus` combo (designer + fields) and all status branching in `RecordPayment` (always inserts, always advances txn to ForRelease). `Database/19_drop_payment_status.sql` (guarded, idempotent) DROPs `payments.status`; applied to live `croms` (verified gone); `01_schema.sql` updated. Payment INSERT is now 11 columns (no status). Validations kept: OR number required; Cash → tendered ≥ total; non-Cash → reference required.
- **New Print Receipt feature:** `btnPrint` (secondary grey button beside Record Payment, disabled until a payment is recorded). On a successful record, the form snapshots the payment into a private `Receipt` object (`_last`), auto-offers the print dialog, and keeps the snapshot so the button can **reprint**. `PrintReceipt`/`DrawReceipt` draw a **58mm thermal slip** (`PaperSize 228 hundredths-in`, height estimated from line count) via `System.Drawing.Printing.PrintDocument` + `PrintDialog`, matching the kiosk's ticket pattern: centred office header → **"PAYMENT SLIP (not an Official Receipt)"** → Txn code / client / fee description → Document Fee / Additional Fee → prominent TOTAL (Consolas) → Method / Reference / O.R. No. / Tendered / Change → Processed By / Date & Time / Remarks → footer. Label deliberately "Payment Slip", NOT Official Receipt (the OR is the Treasury's accountable form — this is only CROMS's internal proof).
- VERIFIED: MSBuild clean 0 warn/0 err (after adding the missing `btnPrint = new Button()` init). Migration applied; ran the new 11-column INSERT in a transaction (Cash 155+20=175, tendered 200 → change 25) — succeeded, ROLLBACK'd. Printer/GUI not exercised (no interactive desktop) — slip layout verified by compile + the drawing code.

### 2026-07-23 — Release & Claim: Rep. ID Type is now a dropdown of accepted PH gov't IDs
- `ReleaseClaimForm.txtIdType` converted from TextBox → editable **ComboBox** (`ComboBoxStyle.DropDown` + SuggestAppend autocomplete), populated in `PopulateIdTypes()` with the IDs commonly accepted across Philippine government offices: PhilSys National ID, Passport (DFA), Driver's License (LTO), UMID, SSS, GSIS eCard, PRC, Voter's/COMELEC, Postal ID, PhilHealth, TIN, Pag-IBIG Loyalty Card Plus, Senior Citizen (OSCA), PWD, Solo Parent, Barangay ID/Cert, NBI Clearance, Police Clearance, OWWA/iDOLE, Seafarer's Book (SIRB), IBP, AFP/PVAO, Company/School ID, + "Other". Editable so an unlisted ID can still be typed.
- Code touch-ups: `.Text` still used for the insert param (unchanged); replaced `txtIdType.Clear()` (no such method on ComboBox) with `SelectedIndex=-1; Text=""`. `releases.representative_id_type` is VARCHAR(50) — longest label ~42 chars, fits. MSBuild clean 0/0.

### 2026-07-23 — Release & Claim: replaced mouse signature with a webcam claimant photo
- **Why:** the mouse-drawn signature was meaningless on a staff-operated desktop (claimant can't sign with a mouse). User picked a **webcam photo of the claimant** taken at release = real proof of who claimed. Decisions: photo **optional** (never blocks release — no camera / claimant refuses still lets you release); stored in a **new dedicated column** (not reusing signature_image).
- **Schema `Database/20_claimant_photo.sql`** (guarded/idempotent): `releases.claimant_photo LONGBLOB NULL` added; `signature_image` left in place but unused. Applied to live `croms` (verified). `01_schema.sql` updated.
- **Reused the kiosk's camera stack:** added AForge 2.2.5 references (AForge / AForge.Video / AForge.Video.DirectShow, already in solution `packages/`) to `CROMS.csproj` — main app didn't have them before; DLLs confirmed copied to `bin\Debug`.
- **Designer (`ReleaseClaimForm`):** removed the signature pad (`lblSig`/`pbSignature`/`btnClearSig`); added a camera panel — `pbCam` live-preview PictureBox (360×250, Zoom) + `btnStartCam` / `btnCapture` (disabled until camera on) / `btnRetake` (disabled until captured) + `lblCamStatus`. grpClaim grown 433→510; Recent Releases label+grid shifted down (606 / 625, grid 186 tall) so nothing overlaps. btnRelease text "Release & Sign" → "Release". The existing read-only kiosk-photo box (`picClient`, original requester's photo from `queue_tickets.id_image`) is untouched — now a second identity aid beside the live claimant capture.
- **Code-behind:** dropped all signature code (`InitSignaturePad`/`pbSignature_MouseMove`/`ClearSignature`/`SignatureBytes` + `_sig`/`_drawing`/`_hasSig`/`_last`). Added `StartCamera`/`OnFrame`/`btnCapture_Click`/`btnRetake_Click`/`StopCamera`/`ResetCamera` (mirrors the kiosk: enumerate `FilterInfoCollection`, first device, `NewFrame` clones to `_lastFrame` + preview; Capture saves JPEG to `_photoBytes` and **auto-stops the camera** to free the device; Retake restarts; camera also stopped on `Disposed`). Release INSERT now writes `claimant_photo` via a `MySqlDbType.LongBlob` param (DBNull when no photo). Reset after release calls `ResetCamera`.
- VERIFIED: MSBuild clean 0/0; AForge DLLs in `bin\Debug`; migration applied; ran the exact release INSERT (rep + Driver's License + NULL photo) in a transaction → succeeded, ROLLBACK'd. Live webcam capture not exercised (no interactive desktop/camera) — device/capture logic is the proven kiosk pattern; verified by compile + the INSERT.

### 2026-07-23 — Release & Claim: camera picker (choose among multiple webcams)
- Added a **Camera dropdown** (`cboCamera`, DropDownList) above the Start/Capture/Retake buttons in `ReleaseClaimForm`. `PopulateCameras()` (called in ctor) enumerates `FilterInfoCollection(VideoInputDevice)` into a cached `_videoDevices` field and lists each device name; selects the first, disables Start when none found. `StartCamera()` now opens `_videoDevices[cboCamera.SelectedIndex]` instead of always device[0]. `cboCamera_SelectedIndexChanged` restarts the live feed on the newly picked device if the camera is already running (otherwise the choice just applies on next Start). Status line shows the active camera name.
- Layout: buttons repositioned (Start+Capture on one row at y248, Retake y290, status y336) to make room for the picker; btnRelease TabIndex bumped to 16. MSBuild clean 0/0. Multi-camera switching not exercised live (no camera/desktop here) — standard AForge device-index pattern.

### 2026-07-27 — Six workflow improvements (queue serialization, kiosk gate, searchable record, fee ref toggle, optional camera, form reset)
- **1. Queue / single CTC form — already satisfied by architecture (no code change).** Certificate Request is a single cached embedded module in `MainForm._cache`; only one instance ever exists, so multiple CTC windows can't open. The kiosk queue serializes clients (one `queue_tickets` row each, `queue_ticket_services` per service) and staff process them one at a time via Call Next + the Now-Serving window-click flow (`WindowClicked`). Confirmed rather than rebuilt.
- **2. Kiosk office-availability gate** (`CROMS.Kiosk/KioskForm`): new full-screen `_offlineOverlay` panel + `_availability` Timer (4s). `OfficeOnline()` = `COUNT(*) FROM windows WHERE status='Active' AND current_operator IS NOT NULL AND last_heartbeat > NOW()-INTERVAL 2 MINUTE` (matches the staff heartbeat window; DB unreachable → treated as unavailable). When none online the overlay covers the wizard with "The office is currently unavailable. Please try again later."; it auto-hides the moment a window comes online (polled, no restart). Overlay anchored all-sides over the ClientRectangle so it covers header/footer too. Timer stopped in `OnFormClosing`.
- **3. Searchable Record ComboBox** (`CertificateRequestForm`): `cboRecord` changed DropDownList → DropDown with `AutoCompleteMode=SuggestAppend` + `AutoCompleteSource=ListItems` (designer). Typing part of a name filters the list (matches the `last, first` / `full_name` display strings the combo is bound to). `LoadRecords()` re-asserts the two autocomplete properties after each `DataSource` swap (a rebind can drop the ListItems source). `btnCreate` still reads `SelectedValue` — resolves when a listed name is picked; unmatched free text → record left NULL (already optional).
- **4. Fee & Payment reference toggle** (`FeesPaymentsForm`): method items relabelled `Cash` / `GCash` / `Bank Transfer` (dropped "(future)"). New `UpdateReferenceField()` (called from ctor + `method_Changed`): Cash → `_txtRef.Enabled=false` + cleared, label "Reference No. (not required for Cash)"; GCash/Bank → enabled, label "Reference No. (required)" in blue. The existing `RecordPayment` validation (non-cash requires a reference) already enforces the requirement.
- **5. Optional camera in Release & Claim** (`ReleaseClaimForm`): new `cboUseCam` "Use Camera?" (Yes/No, DropDownList) in the designer, defaulting to **No**. `ApplyCameraOption()` shows/hides the whole webcam panel (lblCam/pbCam/lblPick/cboCamera/btnStartCam/btnCapture/btnRetake/lblCamStatus); No → `ResetCamera()` (camera never initialized, no photo). Yes → panel shown, officer clicks Start Camera. Photo stays optional either way.
- **6. Release & Claim form reset after transaction** (`ReleaseClaimForm`): extracted `ClearClaimForm()` — clears selected txn, claimant name, rep checkbox, ID type/number, the kiosk `picClient` image, and resets Use Camera to No (which releases the camera). Called after a successful release; the released transaction stays saved (only the form is wiped for the next client).
- VERIFIED: full-solution MSBuild clean — CROMS.exe + CROMS.Kiosk.exe + CROMS.Display.exe all built, 0 errors. GUI not clicked (no interactive desktop); logic verified by compile + reading the exact queries.

### 2026-07-28 — NEW companion app: CROMS Mobile Scanner (Ionic Angular) + Node save-API
- Built a **phone scanner app** so staff can photograph a PSA certificate in the field, OCR it **on the device**, review the fields, and save into the same `croms` DB. Lives OUTSIDE this repo at `C:\Users\ivan palogan\ORCMobile_Application` (its own git repo, Ionic Angular **standalone** + Capacitor 8, Angular 20).
- **Architecture decision (user chose): OCR runs ON the phone**, not by sending the image to the PC. Stack = **tesseract.js** (pure JS, runs in both `ionic serve` browser AND the Android webview — ML Kit was rejected because it can't run in the browser for dev). A phone can't open a MySQL socket, so a **thin Node/Express + mysql2 save-API** (`ORCMobile_Application/server/`) runs on the office PC next to MySQL; the phone POSTs the reviewed fields over wifi.
- **Extraction parity:** ported this project's `Data/DocumentAI.cs` (classify + Birth/Marriage/Death label-anchored field extraction, name-cell split, DMY date parse, religion/occupation heuristics) to TypeScript `src/app/services/extract.service.ts` — the phone extracts the SAME fields the SAME way as the desktop Document AI. Extraction keys match desktop (ChildFirst/Sex/DateOfBirth/… etc.).
- **Save-API** (`server/index.js`): `POST /api/births|deaths|marriages` map the extraction keys → the real `croms` columns (births Form-102 set, deaths Form-103, marriages Form-97), auto-generate the registry number (year-scoped MAX+1, `YYYY-B/D/M-####`, same as desktop), set **status `Pending Approval`** (mobile is a field-capture tool; a registrar verifies/finalises on the desktop), and write an `audit_log` row. Also `/api/health` + `/api/recent`. Reads DB creds from `server/.env` (git-ignored; DB_USER=root, croms). Optional `x-api-key` gate.
- **Mobile pages:** Home → Scan (Capacitor Camera capture, on-device OCR with progress) → Review (editable field list, amber-highlights uncertain/low-confidence fields, Save). `@ionic/pwa-elements` added so the camera also works in the dev browser; `provideHttpClient` wired.
- VERIFIED end-to-end against the LIVE `croms`: `ng build` clean (no TS errors); started the Node API → `/api/health` = db connected; POSTed real Birth (→ id/registry `2026-B-0001`, Pending Approval), Death (`2026-D-0001`), Marriage (`2026-M-0001`); missing-sex birth correctly rejected; `/api/recent` listed them; then **deleted all 3 test rows + their audit rows** and confirmed gone. Test server stopped.
- Setup docs: `ORCMobile_Application/MOBILE_SETUP.md` + `server/README.md` (run steps, LAN-IP wiring, firewall port 3000, Android `npx cap` flow). KNOWN: tesseract.js fetches `eng` traineddata on first run (needs internet once, then cached) — bundling for fully-offline phone use is a later enhancement; handwritten fields (often registry no) don't OCR → operator fills them on the review screen.

### 2026-07-28 — Mobile: live phone-camera streaming to desktop (WebRTC) + direct-camera fix
- Added a **live wireless camera** feature to the mobile ecosystem: a phone streams its camera to the office PC in real time, multiple phones at once, shown in a live grid dashboard. All in `ORCMobile_Application` (+ its `server/`), NOT the CROMS desktop repo.
- **Transport = WebRTC** (peer-to-peer, low latency; media never touches the server) with a **WebSocket signaling server** added to the SAME Node server (`server/signaling.js`, path `/rtc`): device registry with unique ids + names, join/leave broadcast, SDP/ICE relay, heartbeat ping to drop dead sockets. `index.js` refactored to one `http.createServer` hosting both the REST save-API and the WS signaling, and to serve `server/public/` static.
- **Phone = broadcaster** (`src/app/services/webrtc.service.ts` + `broadcast` page): `getUserMedia` rear camera, one RTCPeerConnection per viewer (phone offers), auto-reconnect with backoff, live status + viewer count. New `/broadcast` route + Home "Start Live Camera" button. `environment.signalingUrl` added (ws://LAN-IP:3000/rtc).
- **Desktop = viewer** (`server/public/dashboard.html`, self-contained): connects `/rtc`, answers each phone's offer, renders a responsive grid tile per phone with name label + live status dot, auto add/remove on connect/disconnect, WS auto-reconnect. **Camera selection** dropdown lists local laptop/USB cameras (enumerateDevices) + every connected phone; selecting shows a big active preview; **Capture** snapshots it to PNG. Runs in Edge/Chrome on the PC today.
- **Desktop embed (optional, kept OUT of CROMS .csproj so it can't break the build):** `desktop-integration/LiveCameraDashboardForm.cs` (WebView2 host — Edge engine has full WebRTC) + `desktop-integration/README.md` (install WebView2 Runtime + NuGet, drop the form in, open from a button). Falls back to "open the URL in Edge" if WebView2 is unavailable.
- **Camera-opens-gallery fix (Scan):** confirmed `CameraSource.Camera` is correct (launches the camera directly) — but ONLY in the installed native app; a plain mobile browser has no native camera bridge so it falls back to a file/gallery picker. Documented; added `correctOrientation`/`saveToGallery:false` + an explaining comment. Live Camera uses `getUserMedia` so it works in browser too.
- VERIFIED: `ng build` clean (broadcast page compiles); server boots with signaling attached; `/dashboard.html` serves 200; **signaling relay smoke test passed** — a simulated viewer + phone registered, discovered each other (phone sees viewer, viewer sees "Counter 1" join), and an offer→answer round-trip relayed correctly; server logged join/leave. Real WebRTC media (getUserMedia/PeerConnection) NOT device-tested here (no camera/interactive desktop) — needs a real phone + PC on wifi; signaling (the hard part) is proven. Docs: `ORCMobile_Application/LIVE_CAMERA.md`.
- KNOWN/limits: public STUN only (no TURN) — fine for one office LAN; assumes phones+PC same subnet; multiple dashboards can each receive all phones.

### 2026-07-28 — Desktop auto-starts the Ionic dev server + Login shows mobile URL/QR
- CROMS.exe now **auto-launches `ionic serve --host=0.0.0.0` in the background** on startup — staff never open a Command Prompt. The Login screen shows the live mobile-connection URL + QR; the server is always killed when the app exits (no orphan node/cmd).
- **`Data/IonicServerManager.cs`** (new singleton): starts `cmd /c ionic serve …` headless (`CreateNoWindow`, redirected output → temp log `croms-ionic-serve.log`), args configurable via App.config `IonicServeArgs`. **Readiness by polling the port** (TCP connect to 127.0.0.1:port) not by fragile output-parsing — robust across ionic CLI versions; picks up a non-default port from the output if 8100 was taken. **LAN IP auto-detected** each start (`DetectLanIp`: scores Up non-loopback/non-virtual NICs, prefers Wi-Fi/Ethernet with a gateway + private range, skips APIPA 169.254) so switching wifi just works — never hardcoded. Status enum Stopped/Starting/Running/Error via a `Changed` event. **Auto-restart** on unexpected crash (max 3, 2.5s backoff). Detects "ionic not recognized" → clear Error. **Stop()** kills the whole tree via `taskkill /T /F` (no orphans).
- **`Program.cs`**: starts the manager before the login (guarded by App.config `IonicAutoStart`), and hooks `ProcessExit` + `ApplicationExit` + the non-OK-login path to `Stop()` — guaranteed shutdown.
- **`LoginForm`** widened to 800px with a right **"Mobile App Connection"** panel (designer-built): Status (colored: amber Starting / green Running / red Error / grey Stopped), the single mobile URL in a read-only copyable box, a **QR PictureBox**, a hint, and a **Retry** button (calls `IonicServerManager.Retry()`). Updates live from the `Changed` event (marshaled to the UI thread; unsubscribes on close).
- **QR** = `Data/QrHelper.cs` — generates the QR from the URL via **QRCoder loaded by reflection**, so there is NO hard dependency and the build never breaks if QRCoder is absent (then the panel just shows the URL text). To enable the QR image, install once in VS: NuGet `Install-Package QRCoder`.
- App.config: added `<appSettings>` `IonicAppPath` (default `C:\Users\ivan palogan\ORCMobile_Application`), `IonicAutoStart`, `IonicServeArgs`. csproj: added the two new `Data\*.cs`.
- VERIFIED: MSBuild (VS2022 BuildTools) `-p:GenerateResourceMSBuildArchitecture=CurrentArchitecture` build **clean, exit 0** (fixed one App.config XML-comment `--` error along the way). Runtime not exercised here (no interactive desktop) — logic verified by compile + the port/IP/kill design. On the office PC: run CROMS from VS → Login shows Starting… → Running + URL + QR once `ionic serve` is up.

### 2026-07-28 — Moved mobile connection from Login to Dashboard; QR-only (no URL)
- Per request: LoginForm is back to auth-only (reverted the mobile panel + widen + all its code — ClientSize 420 again). The mobile connection now lives on the **Dashboard**.
- **DashboardForm** — new **"Mobile App Connection"** panel (designer-built, right column): large **QR** (`picMobileQr` 220×220) generated from the detected Wi-Fi URL via `QrHelper`, a status line mapped to **Running / Connecting / Offline** (green/amber/red), and an **"Mobile App Connection Unavailable"** message that shows (QR hidden) when the server isn't running. The raw HTTP URL is kept **internal** — shown only when App.config `ShowMobileUrlDebug=true`. Updates live: subscribes to `IonicServerManager.Changed` (marshaled) + refreshes on the existing 3s `statusTimer` + `RefreshData`; QR bitmap is rebuilt only when the URL changes (so a new Wi-Fi IP auto-regenerates it, no restart). `SetMobileQr` falls back to showing the URL text if QRCoder isn't installed.
- **Layout reorg to fit the QR:** the 7-day trend was shortened (520×200, top-left) and the **Service Windows** board moved beneath it (left column, `pnlWindows` now 520 wide, Top|Bottom|Left) — freeing the whole right column for the QR panel (`pnlMobileConn`, Top|Bottom|Right). No overlap at normal content widths.
- VERIFIED: MSBuild clean, exit 0. Runtime not exercised (no interactive desktop). Ionic still auto-starts from `Program.cs` exactly as before; only the display surface moved (Login → Dashboard).

### 2026-07-28 — Installed QRCoder + fixed a QR reflection bug (QR now really renders)
- Installed **QRCoder 1.4.3** into the packages.config project (downloaded official `nuget.exe`, `nuget install` → `packages\QRCoder.1.4.3`; added `packages.config` entry + a csproj `Reference` HintPath `lib\net40\QRCoder.dll`). Build copies `QRCoder.dll` to `bin\Debug`, so `QrHelper`'s `Assembly.Load("QRCoder")` now resolves.
- **BUG found + fixed in `QrHelper`:** `CreateQrCode`'s real signature is `(string, ECCLevel, bool, bool, EciMode, int)` (optional params), so the old `GetMethod("CreateQrCode", new[]{string, ECCLevel})` returned **null** → `TryCreate` would have silently returned null and the QR never appeared even WITH QRCoder installed. Rewrote it to match the overload by its first two param types and fill the remaining params from their `DefaultValue` (bool false / EciMode 0 / int -1). 
- VERIFIED end-to-end: MSBuild clean, `QRCoder.dll` present in `bin\Debug`, and invoking the exact fixed reflection path produced a real **222×222 scannable QR PNG** for `http://192.168.1.15:8100`. So on the office PC the Dashboard QR will now render (no extra steps).

### 2026-07-28 — Mobile: desktop auto-start switched to HTTPS (ng serve --ssl); Scan page = live camera; broadcasting removed
- **HTTPS auto-start:** CROMS `IonicServerManager` now runs `npx ng serve --ssl --host 0.0.0.0 --port 4200 --disable-host-check` (was `ionic serve --host=0.0.0.0`, http:8100). Default Port 4200, Scheme **https**, both configurable in App.config (`MobileServeCommand`, `MobileScheme`). The Dashboard QR/URL is now `https://<detected-IP>:4200`. Reason: the phone camera (`getUserMedia`) needs a **secure context** — http:8100 blocked it; https:4200 works. Installed QRCoder 1.4.3 earlier so the QR renders. Build clean; verified the ssl server serves 200 on https and advertises the LAN IP.
- **Scan page rebuilt as a LIVE camera** (`src/app/scan/scan.page.*`): live `getUserMedia` preview + framing guide, a **camera picker** (enumerateDevices, switch when >1), **torch** toggle (when supported), **Capture** grabs a frame from the stream → Scan & Extract (OCR) → Review; camera released on page leave. Secure-context guard gives a clear message over http. (Replaces the old capture-a-photo-then-OCR flow.)
- **Broadcasting removed** (user doesn't need the phone-as-wireless-camera feature): deleted `broadcast` page, `webrtc.service.ts`, the Home "Live Camera" card + `/broadcast` route. The server-side signaling + `dashboard.html` remain in `server/` (unused by the phone now; harmless). Home has only **Start Scanning**.
- VERIFIED: `ng build` clean (fixed one strict-null on the video track). The running `ng serve` hot-reloads, so the phone just refreshes.

### 2026-07-28 — One-step QR pairing: auto-config from origin + device registry + smart QR
- **No-manual-IP pairing.** Because the phone is served the app FROM the laptop, the app now derives the server from its own origin — API calls are same-origin `/api/…`, proxied by the dev server to the save-API on :3000 (new `proxy.conf.json` + `angular.json` `proxyConfig`). This also fixes the **https-page → http-API mixed-content block** (same-origin, no second cert) and auto-adapts to any network/IP with zero config. Native app path reads `?host=&api=&sid=` from the QR/deep-link and persists it.
- **Mobile services:** `config.service.ts` (resolves server: same-origin in browser, QR/stored host on native; reads QR query params), `pairing.service.ts` (persistent device id + name, `register` + 8s `heartbeat`, auto-reconnect on failure, so a restart re-pairs with no re-scan). `api.service` now uses ConfigService (dropped hardcoded `environment.apiBaseUrl`). Started in `AppComponent.ngOnInit`.
- **Save-API device registry** (`server/index.js`): `POST /api/devices/register`, `POST /api/devices/heartbeat`, `GET /api/devices` (in-memory; online = heartbeat < 15s; captures client IP; prunes after 10 min offline).
- **CROMS side:** `IonicServerManager` gained `SessionToken` (per launch), `NetworkType` (Wi-Fi / Hotspot / LAN — hotspot detected via 192.168.137.x + adapter name), `ApiPort` (config), and **`QrPayload`** = the URL + `?sid=&host=&api=` (QR now encodes connection details for native auto-pair; the displayed URL text stays clean). Live **IP-change watcher** (`_ipPoll`, 10s) regenerates the URL/QR when the laptop switches wifi/hotspot — no restart. Dashboard: status shows the network type, the QR uses `QrPayload`, and a new **Connected Devices** list polls `GET http://localhost:3000/api/devices` (HttpClient + JavaScriptSerializer; added System.Net.Http + System.Web.Extensions refs) showing 🟢/⚪ name + IP.
- VERIFIED: CROMS MSBuild clean (exit 0). Save-API registry tested (register→heartbeat→list, online/offline, IP, connectedSeconds). **Proxy verified**: `POST/GET https://localhost:4200/api/devices` correctly reached the save-API on :3000 and returned the device list — so the phone loading the app over https auto-pairs same-origin with no manual IP and no mixed-content error.
- LIMITS (per the platform, as the user noted): a QR can't silently auto-join a phone to wifi/hotspot (OS security) — the user joins the network (or confirms a Wi-Fi QR), THEN the app auto-pairs. Device registry is in-memory (clears on server restart; device id persists on the phone so it re-pairs).

### 2026-07-28 — CROMS auto-starts the save-API too (single launch)
- Added `Data/ApiServerManager.cs`: auto-starts the Node **save-API** (`node index.js` in `<IonicAppPath>\server`, port 3000) headless on launch and kills its process tree on exit (no orphan). No status UI — the Dashboard already shows "Save-API offline" if it's down. Command configurable via App.config `MobileApiCommand`.
- `Program.cs`: on autostart it now starts the **save-API first** (3000, the proxy target + DB + pairing), then the mobile dev server (ng serve 4200); both stopped on ProcessExit / ApplicationExit / non-OK login. So one F5 brings up everything — DB API, phone pairing, QR, live scanning — with zero CMD windows.
- VERIFIED: MSBuild clean (exit 0). Needs `node` in PATH + `server/` npm deps + `server/.env` (all present). If node is missing the save-API just fails silently; CROMS never breaks.

### 2026-07-28 — Two-QR dashboard: ① Join Wi-Fi (auto-read hotspot) + ② Open App
- User confusion resolved: one QR can't both join Wi-Fi AND open a URL (the phone camera reads `WIFI:…` as a network-join and a URL as a link). So the Dashboard now shows **two** QRs: **① Join Wi-Fi** (a `WIFI:T:WPA;S:…;P:…;;` payload → phone prompts to join the hotspot) and **② Open App** (the https app URL). Join first, then open — after which the app auto-pairs.
- **`Data/HotspotInfo.cs`**: reads the laptop's hotspot SSID+password — App.config `HotspotSsid`/`HotspotPassword` override first, else **auto-reads the Windows Mobile Hotspot via WinRT** (`NetworkOperatorTetheringManager.CreateFromConnectionProfile(...).GetCurrentAccessPointConfiguration()` → Ssid/Passphrase). Builds the standard Wi-Fi QR text (escaped). All guarded → if unreadable, the Wi-Fi QR is just hidden and the App QR centers.
- csproj: referenced WinRT (`Windows.winmd` from Windows Kits 10.0.26100.0 + `System.Runtime.WindowsRuntime` facade). Confirmed the legacy `netsh wlan show hostednetwork` path is empty on this machine (modern hotspot needs WinRT).
- Dashboard (`DashboardForm`): `SetupMobilePanel()` lays the 271×317 panel out as two 118px QRs side by side with ①/② step labels; `ShowRunningQrs()` fills the Wi-Fi QR when creds are available (label "① Join: <ssid>") else hides it and centers the App QR; hint text guides the two steps.
- VERIFIED: MSBuild clean to a temp OutDir (exit 0 — the normal bin copy was blocked only because CROMS.exe was running). Generated a real **246×246 scannable Wi-Fi QR** for `WIFI:T:WPA;S:CROMS-Hotspot;P:…;;` via the app's QRCoder path.
- LIMIT: the WinRT auto-read can return null when the laptop has NO internet connection profile (pure-offline hotspot) — in that case set `HotspotSsid`/`HotspotPassword` in App.config once (the override path). Runtime auto-read not exercised here (no hotspot + exe locked); code compiles + is guarded.

### 2026-07-28 — Back to ONE QR + Wi-Fi-first network hint (per user)
- User wanted a single QR and Wi-Fi as primary (hotspot only as fallback). Reverted the two-QR layout: the Dashboard shows **one App QR** (170px, centered) plus the **network to join as TEXT** below it (a URL QR can't also join Wi-Fi, so the join is a one-time manual step guided by the text).
- **`HotspotInfo` reworked to Wi-Fi-first:** order is now App.config override → **current Wi-Fi** (primary, read via `netsh wlan show interfaces` for the connected SSID + `netsh wlan show profile name=… key=clear` for the password) → **Mobile Hotspot** (fallback, WinRT). `source` = wifi/hotspot/config; the panel shows "📶 Wi-Fi: <ssid> • <password>" or "📶 Hotspot: …". `DetectLan` already scores Wi-Fi (gateway+wireless) above the hotspot, so the QR/URL IP is the Wi-Fi one when on Wi-Fi.
- `DashboardForm`: `SetupMobilePanel` = single centered QR + `lblWifiInfo` text line; `ShowRunningQrs` fills the Wi-Fi/hotspot hint; removed the picWifiQr/step-label/SetWifiQr machinery.
- VERIFIED: MSBuild clean to temp OutDir (exit 0; bin copy blocked only by the running exe). **netsh read verified live**: State=connected, SSID='Meraki Cafe', password retrieved — so the Wi-Fi-primary hint works. NOTE: the network password is shown on the admin Dashboard (office's own network — acceptable; flagged for awareness).

### 2026-07-29 — CROMS.Display board now dynamic (any number of windows) — fixed the 3-window cap
- BUG (flagged since 2026-07-20): the standalone `CROMS.Display/DisplayForm.cs` "Now Serving" board was hardcoded to exactly 3 cards (`Label[3]`, a 3-column TableLayoutPanel, `for w=1..3`) reading `window_no` 1..3 directly — so adding a 4th/5th window in Settings never appeared on the public board.
- FIX: rewrote `DisplayForm` to the same DB-driven pattern the main-app `ClientDisplayForm` already uses. Queries `windows WHERE status='Active' ORDER BY display_order,id`, builds one card per active window into a responsive `TableLayoutPanel` (≤4 per row, then wraps to more rows), keyed by window id in `_codeLabels`/`_subLabels` dictionaries. Rebuilds the grid only when the active-window signature (ids+names) changes (no 2s flicker); refreshes each card's ticket_code/type_label + Online/Offline presence every tick. Code font auto-scales down as more windows share the screen (90→64→46pt). "No active windows" empty state. Online = `current_operator NOT NULL AND last_heartbeat > NOW()-INTERVAL 2 MINUTE` (StaleMinutes const = 2, matching the main app's WindowAssignmentForm).
- No schema change (reads the existing dynamic `windows` table). VERIFIED: MSBuild clean — `CROMS.Display -> bin\Debug\CROMS.Display.exe`, 0 errors. GUI not clicked (no interactive desktop). Now the display, the staff Now-Serving board, and the client kiosk all follow the `windows` table with no fixed limit.

### 2026-07-29 — Settings → Window Management: Activate All / Deactivate All (bulk admin toggle)
- Per-window Enable/Disable already existed; user wanted the admin to flip EVERY window at once. Added two toolbar buttons to `SettingsForm` Window Management tab: **Activate All** (green) sets every non-Active window to Active; **Deactivate All** (grey) sets every Active window to Inactive.
- Both go through the same admin re-verification gate (`_verified` / `RequestVerification`) as the other window CRUD, and each asks a Yes/No confirm first. Deactivate All **skips any window mid-transaction** (`IsServing`) so a client is never stranded, then reports how many were set vs skipped. Both write an `audit_log` row (record_id 0 = bulk) with the `Stamp()` verifier/role/PC suffix. No-op cases warn ("All windows are already Active/Inactive").
- Designer: `btnEnableAll`/`btnDisableAll` added to the `bar` FlowLayoutPanel between Disable and Move Up. Handlers `btnEnableAll_Click`/`btnDisableAll_Click` in code-behind. No schema change (just bulk UPDATEs on `windows.status`). VERIFIED: MSBuild clean (built to temp OutDir; exit 0). GUI not clicked (no interactive desktop). Board/kiosk/login already read `windows` live, so a bulk toggle propagates everywhere with no restart.

### 2026-07-29 — Release & Claim: "Use Camera?" is now a sliding toggle switch (was a Yes/No combo)
- User wanted the Use Camera control styled like a dark/light-mode toggle. Built a reusable **`Modules/ToggleSwitch.cs`** — a custom `Control` that draws a rounded pill track + sliding knob, animates the knob position + track colour between Off (grey) and On (green) via a 15ms timer, exposes `Checked` + `CheckedChanged`, `SetCheckedSilently()` (set state without firing the event), keyboard toggle (Space/Enter), hand cursor, double-buffered/anti-aliased. Configurable OnColor/OffColor/KnobColor. Registered in csproj.
- `ReleaseClaimForm`: replaced `cboUseCam` (ComboBox Yes/No) with `tglUseCam` (ToggleSwitch) + a bold `lblUseCamState` label that reads "On" (green) / "Off" (grey) beside it. Ctor uses `SetCheckedSilently(false)` + `ApplyCameraOption()` for the default-off state; handler renamed `cboUseCam_SelectedIndexChanged`→`tglUseCam_CheckedChanged`; `ApplyCameraOption` now reads `tglUseCam.Checked` and updates the state label; `ClearClaimForm` sets `tglUseCam.Checked = false`. Camera panel still shows/hides + releases the device exactly as before; photo stays optional.
- VERIFIED: MSBuild clean (built to temp OutDir, exit 0; temp dirs removed). GUI not clicked (no interactive desktop). The ToggleSwitch is generic — reusable for any other Yes/No option later.

### 2026-07-29 — Fix Queue Management NOW SERVING right-side gap (cards clustered left)
- Bug (screenshot): with 6 active windows the Now Serving cards clustered on the left of `grpServing`, leaving a big empty band on the right. Cause: `BuildServingCards` used a `FlowLayoutPanel` of FIXED-width cards sized from `_servingFlow.ClientSize.Width / perRow`; that width read unreliably (built before the group reached full maximized width) so cards came out narrow and didn't refill when the group stretched — the FlowLayoutPanel never re-expands fixed children.
- Fix: replaced the FlowLayoutPanel with a **TableLayoutPanel of equal-percentage columns** (up to 4 across, wraps to more rows) with `Dock=Fill` cards — the same proven approach as the client display board. Cards now always fill the whole board width to the grid's right edge, at any window count or screen size, with no dead space. `BuildWindowCard` dropped its width/height params (card is `Dock=Fill` + `MinimumSize` height 140); removed the `grpServing.SizeChanged` rebuild (percentage columns reflow automatically). `grpServing`, `dgvQueue`, `btnExport` already share the same right edge/anchor, so the group spans the same width as the live-queue grid.
- VERIFIED: MSBuild clean (temp OutDir, exit 0). GUI not clicked (no interactive desktop).

### 2026-07-29 — Settings → Window Management: per-row Active/Inactive toggle (one-click on/off per window)
- Clarified with the user: deactivating a window already correctly hides it from the Login window-assignment popup (that form queries `WHERE status='Active'`). What they wanted was a direct per-window on/off power, not select-row-then-Enable/Disable.
- Added an **"Active?" checkbox column** to the `dgvWindows` grid. Ticked = Active, unticked = Inactive. Clicking it flips that one window's status immediately via a new `dgvWindows_CellContentClick`→`ToggleActive(rowIndex)` (deferred with BeginInvoke so reloading the grid mid-click can't throw). Reuses the same admin **verification gate** (`_verified`/RequestVerification) and **serving guard** (`IsServing` blocks setting Inactive while a ticket is mid-service, then reverts the checkbox) as the Enable/Disable buttons; writes the same `audit_log` Enabled/Disabled row with `Stamp()`. `LoadWindows` now builds the bool column from `status`, makes only that column editable (rest read-only), width 70, placed right after Status.
- Inactive still propagates everywhere live (login picker, Now Serving board, dashboard status, public display all read `windows` by status). Existing Enable/Disable + Activate All/Deactivate All buttons kept. No schema change. VERIFIED: MSBuild clean (temp OutDir, exit 0). GUI not clicked (no interactive desktop).

### 2026-07-29 — Login eye button: vector eye icon (fixed broken emoji, kept the icon)
- The show/hide password button showed a broken/clipped glyph because `UiTheme` owner-draws every button via `TextRenderer` (monochrome) which can't render the color 👁 emoji; a first attempt swapping to "Show"/"Hide" text truncated to "H…" in the 40px button and lost the icon the user wanted.
- Fix: `UiTheme.Polish` now skips owner-drawing any button whose `Tag == "noskin"`. `btnEye` is tagged `noskin` (Text cleared) and LoginForm draws a crisp **vector eye** in `btnEye_Paint` (anti-aliased: two almond arcs + filled pupil, plus a diagonal slash when the password is visible = "eye off"). Click toggles `UseSystemPasswordChar` + `Invalidate()` to swap open-eye ↔ eye-with-slash. No emoji, renders sharp at any DPI.
- VERIFIED: MSBuild clean (temp OutDir, exit 0). GUI not clicked (no interactive desktop). The `noskin` tag is reusable for any future custom-drawn button.

### 2026-07-29 — Mobile Scanner: real-time AI camera assistant (green/red guidance + auto-capture + crop/enhance)
- Built the "smart document-scanning assistant" the user specced into the mobile app Scan page (`ORCMobile_Application`, separate repo). Analyses the LIVE preview every 150ms and tells the user whether the document is ready — no photo needed first.
- **`src/app/services/frame-analysis.service.ts`** (new, pure-canvas, no OpenCV, fully offline): downscales each frame to 160×213 and computes brightness, glare (blown-out %), darkness, **sharpness** (Laplacian variance = focus measure), **document box** (Otsu-thresholded bright-paper mask → robust percentile bounding box), **coverage** (too far / too close), **edgesInside** (all four corners in frame), **skew** (perspective proxy: paper width top-vs-bottom & height left-vs-right), and **motion** (frame-to-frame luma diff = shake). Returns a `green|yellow|red` verdict + the single most-important message (ordered: lighting → glare → not found → too far → corners cut → too close → out of focus → angle → steady), a normalised doc box, and the raw metrics. Thresholds are named constants for tuning.
- **`scan.page.ts`**: analysis loop while previewing; colour-coded **status pill** with the guidance message; **live document outline** drawn on an overlay `<canvas>` (object-fit:cover mapping so it lands on the paper, with scanner corner-ticks), coloured by status; **auto-capture** toggle (default on) that fires after ~1.2s of steady green; better camera constraints (ideal 1920×1080 + continuous **focusMode**, and post-start `applyConstraints` for continuous focus/exposure/white-balance where supported). Capture now **crops to the detected box** (2% margin, removes background) and applies a mild **contrast enhance** for OCR (keeps colours). HTML/SCSS: overlay canvas, pulsing status pill, coloured inset glow ring around the stage, auto-capture toggle.
- Messages match the spec ("Move closer…", "Hold the camera steady.", "Improve lighting.", "Fit all four corners…", "Perfect! Document ready to scan.", etc.). VERIFIED: `ng build` clean (fixed one `getContext` cast). Real camera behaviour not device-tested here (no phone/camera) — analysis maths + build verified; thresholds may need on-device tuning for the office's cameras/lighting.
- LIMITS (documented): axis-aligned crop, not a true 4-corner perspective warp (no OpenCV) — straightens by bounding box, good for near-flat scans; heavy skew is flagged (yellow "straighten") rather than de-warped. A full perspective transform would need OpenCV.js (~8MB wasm) — deferrable if the office wants angled-shot correction.

### 2026-07-29 — Mobile OCR: adaptive thresholding so real phone photos are readable
- User's captured PSA Form 102 (yellow tint, bright-left / dark-shadow-right, tiny dense print) failed OCR ("can't read this"). Root cause: `OcrService.preprocess` used a GLOBAL threshold (gray<145 → black) — on unevenly-lit documents that blacks out the dark side and washes out faint print; and the capture step double-processed it with a colour contrast boost.
- Fix: rewrote `preprocess` to **adaptive (local) thresholding** (Bradley/Wellner via an integral image): each pixel is compared to the mean of its own ~w/24 neighbourhood (bias t=0.15), so text stays crisp across shadows, glare and colour casts. Grayscale → integral (Uint32, exact + half the memory of Float64) → per-pixel local threshold. Raised the working resolution (upscale small captures ×1.6, cap large at 2200px) since PSA forms have tiny text. Removed the colour `enhance()` from `capture()` — the capture stays natural-colour for the review screen and OCR does its own grayscale+adaptive pass.
- VERIFIED: `ng build` clean. Not device-tested here (no phone). Should markedly improve tesseract.js classification + field extraction on real photos.
- IMPORTANT for the user: the phone is running in the **mobile browser** (192.168.x:4200) → tesseract.js, the weaker engine. The app already prefers **Google ML Kit** (far better on real photos) but only in the **installed native Android app** (`npx cap run android`). For best real-photo OCR, build/install the native app; adaptive threshold helps both paths. Handwritten fields (registry no, signatures) still won't OCR — operator fills them on the review screen.

### 2026-07-29 — Mobile Scanner: true 4-corner detection + perspective de-warp + pro enhancement
- Upgraded the mobile scanner from an axis-aligned crop to a real document-scanner pipeline (still pure JS/canvas — no OpenCV 8MB download, fully offline).
- **4-corner detection** (`frame-analysis.service.ts`): from the Otsu paper mask (restricted to the robust bbox so stray specks can't pull a corner) it finds the sheet's true corners as the extremes of x+y (tl/br) and x−y (tr/bl) — works for rotated/perspective-skewed pages. Returns a normalised `quad {tl,tr,br,bl}` in the verdict. The live overlay now draws the actual **quadrilateral** (perspective-aware polygon + corner handles) coloured by status, not a rectangle.
- **Perspective warp + enhancement** (`document-scan.service.ts`, new): on capture it computes a **homography** (8-param, Gaussian-elimination solve) mapping the output rectangle → the 4 source corners and **inverse-samples every output pixel (bilinear)** from the full-res frame → a flat, straightened, background-free page sized to the document's real edge lengths (capped 1700px). Then enhancement: **gray-world white-balance + 1–99 percentile contrast stretch + unsharp mask** (colour preserved), or an optional **B&W mode** = adaptive threshold for max OCR contrast. Result looks scanned, not photographed.
- **Scan page**: capture uses the warp when a quad is found (else falls back to the full frame); added a **"High-contrast B&W scan"** toggle beside auto-capture. OCR path (`OcrService` adaptive threshold) unchanged — now fed the clean de-warped image.
- VERIFIED: `ng build` clean. Not device-tested here (no phone/camera) — CV maths + build verified; corner-extreme detection assumes the document is the dominant bright region on a darker surface (the office's form-on-desk case) and is convex. Heavy clutter/《same-colour》backgrounds may need the OpenCV contour path later; flagged.

### 2026-07-29 — Mobile Scanner: gallery attach + max-quality enhancement (flat-field)
- **Gallery / attach a photo:** added a "Gallery" button + hidden `<input type=file accept=image/*>` on the Scan page. `onPickFile` loads the chosen photo and runs it through the SAME pipeline as a live capture — `FrameAnalysisService.analyze` (now accepts an HTMLImageElement, not just video) detects the 4 corners, then `DocumentScanService.scan` de-warps + enhances (or uses the raw photo if no document is found). So an existing photo gets the full scanner treatment.
- **Scan quality to the max:**
  - **Flat-field illumination correction** (new `flatField` in DocumentScanService, runs first in the colour path): estimates local background brightness with a large box average (integral image, ~1/6 of width) and scales each pixel so the background goes near-white — removes shadows, gradients and the yellow photo cast while keeping dark text. This is the single biggest "looks scanned, not photographed" step. Then gray-world white-balance + 1–99% contrast stretch + stronger unsharp (0.6→0.8).
  - **Higher resolution:** warp output cap 1700→2200px, and the camera is now pushed to its highest sane sensor resolution (`applyConstraints` to capabilities width/height max, capped 2560×1440) for sharper small print.
  - B&W mode (adaptive threshold) unchanged; still available via the toggle.
- VERIFIED: `ng build` clean (registered `imageOutline` icon). Not device-tested here. Flat-field + higher res add compute on capture (one-shot, fine); very large gallery images are bounded by the 2200px warp cap.

### 2026-07-29 — Mobile OCR: tuned tesseract worker (LSTM + 300 DPI + preserve spaces)
- Browser OCR still read dense PSA forms poorly. Switched `OcrService.recognizeTesseract` from the functional `recognize()` to a reused `createWorker('eng', 1 /*LSTM*/)` with `setParameters`: `tessedit_pageseg_mode=3` (auto page layout), `user_defined_dpi=300` (sharper LSTM), `preserve_interword_spaces=1` (keeps label↔value spacing the extractor anchors on). Worker cached + reused; progress via the worker logger. Meaningful accuracy bump on printed forms; combined with the flat-field/adaptive preprocess.
- REMINDER (stated to user): the phone on the browser (192.168.x:4200) uses tesseract.js, the weaker engine — even a clean scan is hard for it. The app already prefers **Google ML Kit** (far better) but only in the **installed native Android app** (`npx cap sync && npx cap run android`). For reliable reading of these forms, build the native app. VERIFIED: `ng build` clean.

### 2026-07-30 — LAN multi-PC deployment + smart-connect (in-app Server IP screen) + desktop shortcut
- **Goal:** run CROMS on 2+ office PCs sharing one `croms` database over Wi-Fi/hotspot, working online OR offline (LAN-local DB link, internet optional). Server PC hosts MySQL + `croms`; client PCs run only CROMS.exe (no MySQL install — `MySql.Data.dll` is bundled).
- **Server side set up on the dev machine (verified live):** created LAN MySQL user `croms_user` (password `Croms#2026`, granted on `croms.*` for subnets `192.168.137.%` hotspot + `192.168.1.%` Wi-Fi, `caching_sha2_password`). Confirmed firewall 3306 open (Any profile), MySQL listening `0.0.0.0:3306`, and `mysql -h 192.168.137.1 -u croms_user` logs in + reads `croms`. **TODO: change `croms_user` password before real deployment** (it's in the shipped client configs).
- **Smart-connect feature (new code) — clients never hand-edit App.config:**
  - `Data/ServerConfig.cs`: saves the DB host/port in `%APPDATA%\CROMS\server.cfg`; `EffectiveConnectionString` = the App.config "Croms" string with Server/Port overridden by the saved value (credentials stay in App.config). Falls back to App.config as-is when unconfigured (so the dev/server machine on localhost is unaffected). `TestConnection`/`BuildFor` for the setup screen.
  - `Db.cs`: `ConnectionString` changed from a readonly App.config field to `=> ServerConfig.EffectiveConnectionString` (resolves each use, so a runtime IP change applies without restart).
  - `Forms/ServerSetupForm.cs` (code-built dialog): "Connect to CROMS Server" — Server IP + Port, **Test Connection** (async, green/red status), **Save & Continue** (tests, offers save-anyway on failure). Pre-fills saved host or the App.config default.
  - `Program.cs`: `EnsureServerReachable()` runs before login — if `Db.IsConnected()` already works, proceed silently; else loop the setup dialog until a reachable address is saved or the user Exits. This covers BOTH first-run (ask for IP) and a moved/changed server IP (re-ask instead of a cryptic failure).
  - `Data/DesktopShortcut.cs`: `Ensure()` drops a "CROMS.lnk" on the desktop on first run (late-bound `WScript.Shell`, no COM reference); called at the top of `Main`.
  - csproj: added ServerConfig.cs, DesktopShortcut.cs, ServerSetupForm.cs.
- **Deliverables to the user (repo root):** `DEPLOY_WIFI.md` (step-by-step), `CROMS.client.config` (hotspot `192.168.137.1`), `CROMS.client.wifi.config` (Wi-Fi `192.168.1.100`), `CROMS.client.smart.config` + `READ_ME_FIRST.md`, and packaged zips `CROMS_Client_App.zip` (hotspot), `CROMS_Client_App_WiFi.zip` (same-Wi-Fi), and **`CROMS_Setup_SmartConnect.zip`** (the current recommended one — new exe, asks for IP first run + desktop icon; client config sets `IonicAutoStart=false`). Client zips exclude .pdb and include the x64/x86 Tesseract native folders.
- **Debugging note from the session:** a client's "cannot reach database" was NOT server-side (port test `TcpTestSucceeded=True`, `croms_user` verified) — it was the client running the wrong `CROMS.exe.config` (still `localhost`). The smart-connect screen makes that class of error self-correcting.
- VERIFIED: MSBuild Debug clean to a temp OutputPath (exit 0). Could not overwrite `bin\Debug\CROMS.exe` (locked by the user's running CROMS + VS) — the fresh exe was taken from `obj\Debug\CROMS.exe` for packaging; that lock is not a code error. GUI/runtime dialog not clicked (no interactive desktop); logic verified by compile + the live MySQL connection tests. For the SERVER/dev PC itself to get the new exe, close the running app + rebuild in VS.

### 2026-07-30 — Auto-find server (network scan) — fixes changed Wi-Fi/hotspot server IP
- Problem observed on two client laptops: the Connect-to-Server screen showed "Could not connect: Unable to connect to any of the specified MySQL hosts" because the hardcoded `192.168.1.100` no longer matched the server's current Wi-Fi IP (DHCP changes it; hotspot uses `192.168.137.1`). Clients had to know/type the right number.
- **`ServerConfig.DiscoverServerAsync(port, progress, cancel)`** (new): sweeps every /24 subnet this PC is on (from active non-loopback IPv4 NICs, APIPA 169.254 skipped, self excluded — so it covers both `192.168.1.x` Wi-Fi and `192.168.137.x` hotspot). TCP-probes each of .1–.254 on 3306 (parallel, `SemaphoreSlim(64)`, 400ms/host via `TcpClient.ConnectAsync` + `Task.Delay` race), and for each OPEN port confirms it's really CROMS with `TestConnection` (auth + DB) before accepting — a bare open MySQL elsewhere won't be mistaken for the server. Returns the first confirmed IP (via `TaskCompletionSource`, short-circuits the rest). Added `CandidateHosts()` + `PortOpenAsync()` helpers; new usings (Net/Sockets/NetworkInformation/Threading/Linq).
- **Manual button:** `ServerSetupForm` gained "🔍 Find Server Automatically" (blue, full-width) → `ScanAsync()` runs the sweep with live "Checked N/total" status, auto-fills the IP box on success, then the operator clicks Save & Continue. Form grown 300→350 tall; buttons reflowed; `Busy()` also disables the scan button.
- **Auto-scan on startup (zero clicks):** `ServerSetupForm.AutoDiscover(port)` shows a small "Looking for the CROMS server..." splash (progress bar, `Shown`→async discover→auto-close) and returns the found IP. `Program.EnsureServerReachable()` now, when a saved server is unreachable, runs `AutoDiscover` FIRST — if found it `ServerConfig.Save`s + proceeds silently; only if that fails does it show the manual Connect screen (message updated to mention the Find button). First-run (nothing saved) still goes straight to the manual screen.
- Net effect: when the server's Wi-Fi/hotspot IP changes, clients self-heal — startup finds the server automatically; if not, one "Find Server Automatically" click. No IP typing needed. REQUIREMENT unchanged: client + server must be on the SAME Wi-Fi/hotspot (same subnet) — cross-network can't be bridged in software.
- VERIFIED: MSBuild (VS2022 BuildTools) Debug clean to a temp OutputPath, exit 0. Runtime scan not exercised here (no interactive desktop / second PC) — logic verified by compile; the port-probe + credential-confirm design mirrors the working `TestConnection`. DEPLOYMENT NOTE: the shipped client zips (`CROMS_Setup_SmartConnect.zip`) still contain the OLD exe — rebuild in VS + repackage to give clients this feature.

### 2026-07-30 (eve) — Rebuilt + repackaged the auto-find client zip; broadened DB grants to all private subnets
- Live troubleshooting for a next-day presentation on a DIFFERENT Wi-Fi. Diagnosed the two real causes of the client "Unable to connect to any of the specified MySQL hosts": (a) the server's Wi-Fi IP had changed to a NEW subnet `192.168.100.111` (was `192.168.1.100`), and (b) `croms_user` grants only covered `192.168.1.%` + `192.168.137.%`, so the new subnet was refused even with the right IP.
- **DB grants broadened** on the live server: added `croms_user@'192.168.%'`, `@'10.%'`, `@'172.%'` (all `caching_sha2_password`, password `Croms#2026`, `GRANT ALL ON croms.*`). Now any private-range router (home/office/hotspot) authenticates without re-granting. Server confirmed: MySQL listening `0.0.0.0:3306`, firewall rule "Port 3306" enabled. (Security note: still LAN-only + password; acceptable for the office deployment. TODO before real deploy: rotate `Croms#2026`.)
- **Rebuilt `CROMS.exe`** with the auto-find feature (network scan + startup auto-discover from the prior entry) to `bin\Debug` (MSBuild clean, exit 0; the running app was closed first). **Repackaged `CROMS_Setup_SmartConnect.zip`** from the fresh `bin\Debug` with `CROMS.client.smart.config` swapped in as `CROMS.exe.config` (IonicAutoStart=false) and `*.pdb` removed — this zip now carries the auto-find exe. Deleted the obsolete `CROMS_Client_App.zip`, `CROMS_Client_App_WiFi.zip` (hardcoded-IP configs, superseded by auto-find) and the duplicate `new1.zip`.
- **Presentation guidance given:** prefer the SERVER PC's **Mobile Hotspot** over venue Wi-Fi — Windows hotspot always assigns the host `192.168.137.1` (stable across venues; grant already covers `192.168.137.%`), and it sidesteps venue-Wi-Fi client-isolation that can block PC-to-PC entirely. The DB link is LOCAL so it works with NO internet. Combined with the auto-find exe on the client, B connects on any network with zero IP typing.
- Deploy to client B: copy `CROMS_Setup_SmartConnect.zip` → extract on B (its `%APPDATA%\CROMS\server.cfg` is untouched, so a stale saved IP just triggers the startup auto-scan). Alternatively the in-app **⟳ Update** button once `Setup-UpdateShare.ps1` is run on the server (share `CROMSRelease` — not yet set up as of this entry).

### 2026-07-31 — Three multi-station improvements: responsive Now Serving, window-based access control, real-time sync
- **1. Responsive "Now Serving" font scaling.** The ticket-code labels were fixed-size (staff card Consolas 26F, in-app `ClientDisplayForm` 56F, standalone `CROMS.Display` board 90/64/46F by window count) — only the card resized, so a long queue code (e.g. `MARRIAGE-100`) clipped / got cut in half. Added an auto-fit helper that shrinks a single-line Label's font to fit its client area, re-fitting on `Resize` and `TextChanged` (so it adapts to any card size / screen resolution). Design rule: font never grows past the design `maxPt` (normal codes stay a consistent size) and only scales DOWN to a `minPt` when the text would overflow. Implemented as `AutoFitText` (internal static, bottom of `Forms/QueueManagementForm.cs`, used by the staff cards + `ClientDisplayForm`) and a private `AttachAutoFit`/`FitFont` copy in `CROMS.Display/DisplayForm.cs` (separate project, no shared lib). Uses `MeasureString` with `GenericTypographic`; disposes the replaced Font.
- **2. Window-based access control (staff Queue Management).** Previously every station's board showed and could control ALL windows. Now, when an operator is signed in to a window (`Session.HasWindow`), the Now Serving board shows and controls ONLY that window: `ActiveWindows()` filters `windows` to `id = Session.WindowId`; the group header reads "NOW SERVING — <name> (your window)"; `PickWindowFor` (Call Next) restricts the eligible-window query to the operator's own window; and `WindowClicked` guards `window != Session.WindowId` (defence in depth). A monitor/admin who Skipped window assignment (no `Session.WindowId`) still sees and controls every active window — unchanged. This gates all transaction types (the window is the processing gate for New Registration / CTC / Petition / etc.). The public boards (`ClientDisplayForm`, `CROMS.Display`) are deliberately NOT restricted — they show all windows for the waiting area.
- **3. Real-time synchronization + reconnect.** `QueueManagementForm` had NO auto-refresh (only refreshed on navigate), so a ticket called/completed on another PC didn't appear until manual re-navigation. Added `SetupSyncTimer()` — a 3s `Timer` calling `SyncTick()` → `RefreshAll()`, with a small "● Live" / "● Reconnecting…" status indicator (top-right). `SyncTick` is wrapped so a dropped/slow connection never crashes the timer: it retries once immediately (150ms), and on failure shows Reconnecting and retries next tick; when the DB returns, the next successful tick re-queries full current state, so updates missed while offline are picked up automatically (no manual reload). To make the timer-driven refresh flicker-free and non-disruptive: the serving cards are now rebuilt only when the shown-window set changes (signature) with live text/colour updated in place (`RefreshServing`/`RebuildServingGrid`/`CreateWindowCard`/`UpdateServingCards`, replacing the old rebuild-every-time `BuildServingCards`/`BuildWindowCard`), and `LoadQueue` only rebinds the grid when the data actually changed (content signature) so scroll/selection isn't reset every 3s. Timer stopped in `OnFormClosed`. Also wrapped `ClientDisplayForm.Reload` in try/catch (was unguarded — could crash on DB loss); `CROMS.Display` already had its "Waiting for connection…" guard.
- VERIFIED: MSBuild (VS2022 BuildTools) Debug clean to a temp OutputPath — both `CROMS.csproj` and `CROMS.Display.csproj` exit 0. No schema change. GUI/multi-PC not exercised here (no interactive desktop / second machine) — logic verified by compile + reading the exact queries. To see it on the dev/server PC, close the running app + rebuild in VS; client stations get it via a rebuilt exe (repackage `CROMS_Setup_SmartConnect.zip` or ⟳ Update).

### 2026-07-31 — ⟳ Update keeps the operator signed in across the auto-restart (skip re-login)
- Requested: after the ⟳ Update button applies a new build and auto-restarts CROMS, don't force the operator to re-enter the password for a restart they just authorized. (Confirmed intent = keep them signed in, not fixing a bypass — normal launch still requires login.)
- New `Data/SessionResume.cs`: a **one-time, short-lived resume token**. `Write()` is called in `MainForm.CheckForUpdate` right before `Application.Exit()` (Session still set) — stores userId + windowId + windowName + expiry + Windows account + machine name in `%APPDATA%\CROMS\resume.session`. `Program.Main` now calls `SessionResume.TryConsume()` before the login block: if a valid token exists it restores `Session.User` and **reclaims the same window** (re-stamps `windows.current_operator`/`operator_name`/`last_heartbeat`; `window_transactions` assignments survive a release, so only presence needs re-stamping), then skips BOTH `LoginForm` and `WindowAssignmentForm` and goes straight to `MainForm`.
- Safeguards so ONLY the update path auto-signs-in (a normal launch / logout / crash never does): the token is written nowhere but the updater path; it is **deleted on first read** (valid or not); it **expires** after 300s; it is bound to the current **Windows account + machine name**; and the user is **re-validated as active** in `users` before restore (defence against a stale/forged token naming a disabled account). Window reclaim is skipped if another operator has since taken the window. A resume writes an `audit_log` Login row ("Resumed session after in-app update").
- Files: added `Data/SessionResume.cs` (+ csproj `<Compile>`), edited `Program.cs` (wrap login/assignment in `if (!TryConsume())`) and `MainForm.cs` (`SessionResume.Write()` before the update Exit). VERIFIED: MSBuild Debug clean to temp OutputPath, exit 0. Runtime not exercised (no interactive desktop). Security note: this is a same-PC continuation aid; the token is local-file based, so a local user with filesystem access could in principle forge one — mitigated by the account/machine binding, short TTL, one-time use, and DB re-validation. Acceptable for the trusted office-desktop threat model; flagged for awareness.

### 2026-08-03 — Queue/Window/Claim workflow enhancement spec (8 desktop sections + new claimapp Ionic)
- Implemented the full 8-section enhancement spec across the desktop app, kiosk, public display, save-API, and a NEW Ionic app. All builds clean; the whole queue+claim flow was smoke-tested end-to-end against live `croms` (SQL) and the claim endpoints over real HTTP; test rows cleaned up.
- **Schema `Database/21_workflow_enhancements.sql`** (idempotent, applied to live `croms`, in csproj): `windows.is_priority`; `queue_tickets` workflow/timing cols (`accepted_at`, `accepted_window`, `called_at`, `started_at`, `completed_at`, `recall_count`, `is_priority_ticket`, `final_status`); `queue_ticket_forwards` (forward history, window→window); `claim_requests` (qr_token UNIQUE, claim_ticket_no, requester + `id_image`/`id_*name` from the mobile upload, status Pending/IDUploaded/Verified/Released, release info). Fresh installs get it because 21 runs after 01 in the migration order.
- **§1 Dedicated + Priority windows:** dedicated windows/routing already existed (window_transactions + Call-Next matching). Added **Priority Window** (`windows.is_priority`): handles ALL services (`WindowHandles` short-circuits true), keeps its ticket until completion, and Forward is blocked on it unless an `AdminVerificationForm` override passes. Admin toggle added to `SettingsForm.WindowEditDialog` (+ Type column in the grid, Add/Edit SQL). Dashboard window-status now shows Online/Offline **+ Busy/Available + ★Priority + assigned services**.
- **§6 Accept-then-call workflow:** Call Next no longer serves immediately — it sets status **`Accepted`** (window assigned, `accepted_at` stamped, OFF the public board). The records assistant locates docs, then **Call Client** (new toolbar button, or clicking the Accepted card) flips it to **`Serving`**, stamps `started_at`/`called_at`, plays a **voice callout** (System.Speech, guarded), and puts it on the public board.
- **§4 Recall + Forward** (new toolbar buttons in `QueueManagementForm`): **Recall** re-serves the same number, bumps `recall_count`, replays the callout. **Forward → Another Window** moves the ticket's first pending service to a window assigned to it (same queue number, logs `queue_ticket_forwards`, sets dest to Accepted), blocked on Priority windows without override. Shared multi-service queue (queue_ticket_services) already completes only when every service is done.
- **§5 Now Serving = queue number only:** both public boards (`ClientDisplayForm` in-app + standalone `CROMS.Display`) now show ONLY the ticket code — the `type_label`/service name was stripped for privacy. Accepted tickets stay off the public board (Serving only).
- **§8 Processing-time monitoring:** timestamps above drive a live **Proc. Time** (`Xm Ys`) + **Recalls** column in the queue grid, plus `final_status`; Accept/Call/Recall/Complete/Forward all audit-logged. Total duration computed from started→completed.
- **§7 Service availability:** the kiosk now disables/greys a service card when no ONLINE window handles it (`AvailableServiceCodes()` from window_transactions + priority/all-handlers), re-enabling automatically as windows come online (4s poll). Auto-unselects a service that becomes unavailable.
- **§3 Kiosk 2 parts:** `CROMS.Kiosk` wizard cut from 4 steps to **2** — the separate Photo + Review steps were folded into Part 2 (`BuildDetailsPhotoStep`: fill-up form left, live webcam right, Print on the last step). Removed the review-step code.
- **§2 Claim Request + QR:** new "Claim Request → QR" button in Certificate Request creates a `claim_requests` row (+ its Certification transaction) and shows/prints the QR (`ClaimQr` in `Forms/ClaimFormForm.cs`, QR via the existing `QrHelper`/QRCoder). Payload `CROMS-CLAIM:<token>`.
- **§2/§10 Claim Form (desktop module, new):** `ClaimFormForm` (key `claimform`, Client Services group, nav button added to MainForm.Designer, Registrar/Staff/Admin access, csproj). A USB QR/keyboard scanner types the token into the box (no camera lib needed) or manual entry → Load pulls the request, shows requester/ticket/details/status, the **uploaded ID image**, and the OCR'd ID name → **Verify Identity** + **Release Document** (stamps released_by/at, audit).
- **§2/§11 claimapp (NEW separate Ionic app)** at `C:\Users\ivan palogan\claimapp` (own repo; cloned from the ORCMobile stack — Ionic Angular 20 standalone + Capacitor 8). ID-upload ONLY: **Home** scans the claim QR (`jsqr` over the device camera, or manual token) → **Upload** page loads the claim, opens the camera / picks a photo, runs on-device OCR (`OcrService` — tesseract.js browser / ML Kit native) to prefill First/Middle/Last (editable), and posts the ID image + name. NO claim form, NO releasing. `ng build` clean.
- **Save-API (`ORCMobile_Application/server/index.js`):** added `GET /api/claims/:token` and `POST /api/claims/:token/id` (base64 ID → `claim_requests.id_image` + names + status IDUploaded, audit); JSON body limit 2mb→12mb. **Verified over real HTTP** against live `croms` (GET→claim, POST→IDUploaded/has_id, cleaned up).
- VERIFIED: `MSBuild` clean for CROMS + CROMS.Display + CROMS.Kiosk (0 errors, temp OutputPath); claimapp `npm install` + `ng build` clean; full queue lifecycle (Accept→Call→Recall→Forward→Complete with timestamps/recall/forward/proc-time) + claim lifecycle (create→getClaim→upload→load) exercised on live `croms` and cleaned up. GUI not clicked (no interactive desktop) — logic verified by compile + the exact queries/HTTP.
- **claimapp auto-start (same day):** `IonicServerManager` refactored from a hardcoded singleton into a reusable instance (per-instance label/appPath/serveCommand/port + per-instance log file); kept `Instance` (ORCMobile scanner, :4200) unchanged and added `ClaimApp` (claimapp, :4300, folder `C:\Users\ivan palogan\claimapp`, config keys `ClaimAppPath`/`ClaimAppServeCommand`). `Program.cs` now starts save-API(3000) + scanner(4200) + **claimapp(4300)** on launch and stops all three on exit / failed login. Dashboard shows a `claimapp ● <url>` status line (subscribes to `ClaimApp.Changed`). claimapp `proxy.conf.json` already routes `/api`→:3000, so one CROMS launch brings up claimapp end-to-end. Build clean.
### 2026-08-03 — Settings → App Updates tab: in-app Publish Release + one-click client update + live server IP
- Added an **App Updates** tab to `SettingsForm` (code-built, Admin/Registrar). Removes the need to run the `Setup-UpdateShare.ps1` script by hand.
- **Publish New Release to Clients (server):** new `Data/ReleasePublisher.cs` — behind an `AdminVerificationForm` gate, builds+runs ONE elevated batch (single UAC prompt) that creates `C:\CROMSRelease`, junctions Main/Kiosk/Display at their build outputs, and `net share CROMSRelease=… /GRANT:Everyone,READ`. Junctions mean later VS rebuilds auto-refresh the share (publish once). Resolves the three build folders from either the deployed sibling layout OR the dev/repo layout (walks up to the `.sln`, uses `<project>\bin\<config>`). Audits the publish.
- **Check for Updates & Install (client):** reuses `AppUpdater.IsUpdateAvailable`/`ApplyUpdate` + `SessionResume.Write()` (stay signed in) + `Application.Exit()` — same flow as the header ⟳ button, now also in Settings.
- **Live server IP:** a 5s timer shows `This PC: <ip> (<Wi-Fi/Hotspot/LAN>)  Update share: \\<ip>\CROMSRelease`, recomputed via `IonicServerManager.DetectLan()`, so the address follows a Wi-Fi/hotspot change with no restart. Clients still auto-find the server's new IP through the existing startup subnet scan (`ServerConfig.DiscoverServerAsync`).
- csproj: added `Data\ReleasePublisher.cs`. VERIFIED: MSBuild clean 0/0. Elevation/`net share` not exercised here (no interactive desktop) — mirrors the proven `Setup-UpdateShare.ps1` commands.

- FOLLOW-UPS (not blockers): voice callout plays on the staff PC (move to the Display PC if the waiting-area speakers are there); claimapp QR is not a URL so a phone's stock camera shows text — use the in-app scanner (or later encode a deep link); `01_schema.sql` not hand-edited (migration 21 covers fresh installs); rebuild/repackage the client zip + kiosk/display exes on the office PCs to ship these.

### 2026-08-04 — Screen-flexible scaling across all three apps (small laptop ↔ big screen)
- User: the three apps didn't look right on both small and large laptops (fixed point fonts + fixed-pixel layouts sized for one resolution). Made each app fit the actual screen.
- **CROMS.Display (public board):** every font/height is now a multiple of a `Scale` factor = `ClientSize.Height / 1080` (clamped 0.4–2.0). Header/clock fonts+heights + grid padding rescaled in `ApplyChrome`; card title/sub/margins and the count-based code size all multiplied by the factor; ticket-code auto-fit min also scaled. Rescales + re-lays the cards on `Resize` (monitor/TV change) via `_lastWins`. So the board fits a small laptop and fills a big TV instead of being sized for 1080p only.
- **CROMS.Kiosk (client wizard):** the fixed design boxes (step-1 348×200 cards, step-2 1180×640 details box) overflowed small laptops. Added one-time shrink-to-fit: on `Shown`, `FitToScreen()` computes `f = min(1, hostW/1200, hostH/680)` and `Scale`s both step panels (shrink-only; big screens keep design size centred), then re-centres. Inner containers promoted to fields (`_svcGrid`/`_svcHint`/`_detailsBox`) with named `CenterServiceStep`/`CenterDetailsStep` so the fit can re-centre after scaling.
- **CROMS main app (staff shell):** module forms are laid out on a large designer canvas and embedded Dock=Fill, so small laptops got a horizontal scrollbar. `MainForm` now shrinks each module's content once to fit the content-panel width: design size captured before docking (`_designSize`), `FitModule(form)` applies `Scale(min(1, panelW/designW))` the first time the form is shown with a real (maximized) panel size (`_fitted` guard; the initial Dashboard is fitted on `MainForm.Shown`). Shrink-only — screens ≥ design size keep the original layout.
- VERIFIED: MSBuild clean 0/0 for CROMS + CROMS.Kiosk + CROMS.Display (temp OutputPath). GUI not clicked (no interactive desktop) — scaling uses WinForms' own `Control.Scale`, shrink-only + one-time so large screens are unaffected; user should eyeball on a small laptop, esp. the main-app module forms (per-form layouts vary). Rebuild in VS on each PC to pick it up.

### 2026-08-04 — Auto-connect to the update share (no manual `net use` on clients)
- Problem: on client PCs the ⟳ Update / Settings "Check for Updates" only worked after the operator ran `net use \192.168.1.234\CROMSRelease /user:MSI\cromsshare Croms#2026 /persistent:yes` in cmd first (the share needs an authenticated SMB session). Made CROMS do it automatically.
- `AppUpdater.EnsureShareConnection(out message)`: if the share isn't already reachable, runs `net use "\<server>\CROMSRelease" /delete /y` (clears a stale/mismatched session, error-1219 guard) then `net use … /user:<domain>\cromsshare <pass> /persistent:yes` via hidden `net.exe` (RunNet helper, 15s timeout, captures stderr). Skips entirely if `Directory.Exists(share)` already works (e.g. the operator's own persistent mapping), so it never disturbs a good connection. Never throws.
- Credentials in App.config (override without recompiling): `ReleaseShareUser` (cromsshare), `ReleaseSharePassword` (Croms#2026), `ReleaseShareDomain` (blank → defaults to the **server IP** as the account domain, so the server's local account authenticates without needing its machine name; set to `MSI` etc. if a hostname is required). Called at the top of both `IsUpdateAvailable` and `ApplyUpdate` (after the server-PC check), so the header Update button and the Settings → App Updates tab both auto-connect; failure message is surfaced in the "share not found" text.
- VERIFIED: MSBuild clean 0/0. `net use` not exercised here (no second PC/interactive desktop) — mirrors the exact command the operator ran manually; password is passed as a net.exe arg (briefly visible in the process list, same exposure as the manual command — acceptable for the LAN office). SECURITY: `Croms#2026` is a default in App.config + this log; rotate before real deployment (also flagged for `croms_user`).

### 2026-08-04 — Release & Claim: "Find by no." (jump to a release by queue number)
- Per user: releasing officer wants to type just the queue number (e.g. `001`) to pull up that client's release instead of scrolling the Pending list.
- Added a `Find by no.` box (Dock=Top panel inside `grpPending`, above the Dock=Fill grid; added in code — no Designer change). Enter or the Find button runs `FindByNumber()`.
- `ResolveTxnId(q)`: (1) exact `transactions.txn_code`; else (2) queue number → its linked transaction — accepts the bare number (`001`/`1`, matched against the numeric tail of TODAY's `queue_tickets.ticket_code` where `transaction_id IS NOT NULL`) or the full code (`Q-001`). Returns the txn id + status + matched code.
- If that transaction is in the current For-Release grid → selects the row, sets `_selectedTxnId`, updates `lblSelected`, shows the kiosk photo, focuses the claimant box. If found but not ForRelease → warns with its status ("may still be at Fees / processing"). If nothing matches → "check the number".
- Note: the queue→release link relies on `queue_tickets.transaction_id` being set (done when the ticket is served into Cert Request/registration); tickets never turned into a transaction won't resolve by queue number — the txn code still works. VERIFIED: MSBuild clean 0/0. GUI not clicked (no interactive desktop).

### 2026-08-04 — Kiosk Step 2 modern redesign (two cards, step indicator, pills, camera card)
- Redesigned `CROMS.Kiosk` per the "modern government kiosk" spec. All DB logic unchanged (same Submit, `queue_tickets`/`queue_ticket_services`, priority mapping, print).
- **Header:** title bumped 24→30pt bold; the plain breadcrumb replaced by a new `StepIndicator` control — numbered circles (done ✓ green / current blue / upcoming grey) with labels "Select Services" → "Personal Info & Photo"; "Step X of 2" stays top-right. Updated per step in `ShowStep`.
- **Step 2 two-card layout** (`BuildDetailsPhotoStep` rebuilt): left **Personal Information** card + right **Your Photo** card, both `RoundPanel` (rounded 14px + soft shadow, white fill).
  - Fields via `MakeField` = borderless TextBox inside a rounded bordered host that turns **blue while focused** (1.5px grey → 2px accent). Optional fields labelled in lighter grey. Claim ticket = short label + one-line helper, in `_claimPanel` toggled for Claim/Pickup.
  - **Priority lane** = new `PillToggle` control (icon + label, rounded pill, hover + selected blue state + ✓), replacing the 3 checkboxes. `PriorityValue`/`ResetForm` now read `.Checked`/`SetChecked`.
  - **Camera card:** rounded dark preview frame, "● LIVE" badge, 🟢 Camera Ready / 🔴 Camera Not Detected indicator (`SetCamState`), full-width dynamic capture button (📷 Capture Photo → 🔄 Retake Photo, ✅ Photo Captured), and a dashed **face-guide oval** drawn over the preview (`DrawCameraGuide`).
- **Footer:** Back / Print buttons enlarged with hand cursor + hover tint (green hover on the Print step).
- New controls appended in-file (no csproj change): `RoundPanel`, `PillToggle`, `StepIndicator`. Fit-to-screen scaling still applies (box now 1200×742; DesignH 820).
- **LIMITATION (told user):** the face guide is a STATIC overlay, not real face detection — live "Face Detected / Move Closer / Improve Lighting" feedback needs a vision library (AForge HaarObjectDetector or similar), not added. Everything else from the spec is in.
- VERIFIED: MSBuild clean 0/0 (temp OutputPath). GUI not clicked (no interactive desktop) — user should rebuild `CROMS.Kiosk` in VS and eyeball the two cards / pills / focus borders / camera states.

### 2026-08-04 — Reverted main-app FitModule scaling (caused overlap on smaller screens) + Release&Claim Requested-date column
- **Bug:** Release & Claim (and other modules) overlapped on computer B but were fine on computer A. Cause: the 2026-08-04 `MainForm.FitModule` shrink-to-fit `Scale()` — on a design-size screen (A) factor=1 so no scaling (fine), on a smaller screen (B) factor<1 scaled the form, distorting hand-placed/anchored controls (Client Photo box, Claim-by-QR button, Rep ID field) into an overlap.
- **Fix:** removed `FitModule` + `_designSize`/`_fitted` + the `Shown` fit hook + the design-size capture + the `FitModule(form)` call in `ShowModule`. Module forms now render at their design layout on every machine; small screens use the existing `form.AutoScroll = true` (set in ShowModule) to scroll instead of distort — consistent A and B. Kiosk + Display keep their own scaling (separate projects, verified, no anchored-overlap issue).
- **Release & Claim pending list:** added a `Requested` date column (`DATE_FORMAT(created_at,'%b %d, %Y')`), ordered newest-first, so staff can see a request is from yesterday/last week. NOTE on lookup: the queue number (Q-001) resets daily so it can't identify an old request — the durable identifier is the `txn_code` (TXN-2026-000xxx, shown in the list + accepted by the Find box) or, for claim-by-QR, the CLM ticket number.
- VERIFIED: MSBuild clean 0/0 (temp OutputPath). GUI not clicked (no interactive desktop). Rebuild on each PC (esp. B) to pick up the revert.

### 2026-08-04 — Release & Claim: full responsive card rebuild (no more overlap at any resolution/DPI)
- Rebuilt the module layout from absolute pixel coordinates to **docked TableLayoutPanels + rounded cards** — the only WinForms-correct way to guarantee no overlap across 1366×768 → 4K and 100–175% DPI. All Designer controls reused (event wiring intact); the absolute GroupBoxes are discarded in `BuildResponsiveLayout` (Controls.Clear + re-parent).
- **3-column responsive root** (30% / 42% / 28%), title row spanning top:
  - **Left card "Pending Releases":** summary tiles (PENDING / RELEASED TODAY / THIS WEEK via `UpdateSummary`), a cleaner search bar (`txtSearch` with cue text + Search + Clear buttons; `DoSearch` filters `LoadPending(filter)` by txn_code/client_name and jump-selects a number/code via `ResolveTxnId`), and the pending grid Dock=Fill.
  - **Center column:** "Claim Details" card (fields in a 2-col TableLayoutPanel — Selected line, Claimant, Rep checkbox, Rep ID Type/Number, a **validation label** under the fields instead of overlapping popups) + full-width prominent green **Release** button (icon, hover) with a **confirmation dialog**; below it a "Recent Releases" card (dgvReleased Dock=Fill, double-click → `ShowReleaseDetails`).
  - **Right card "Claim & Verification":** Claim-by-QR button, claim status line, kiosk photo, and the optional camera block — the "Use Camera?" toggle stays visible while the preview collapses to zero height (one `_camPanel` in an AutoSize row) so there's no leftover gap.
- New `Card` control (rounded 12px + soft shadow + white + title) and helpers (`StyleGrid` = fill columns + alternating rows + soft selection + styled headers; `Tile`; `MakeMiniButton`; `SetCue` via EM_SETCUEBANNER; `AddFieldRow`/`AddRowSpan`/`AddStack`). Grids: AutoSizeColumnsMode=Fill (no horizontal scroll), header-click sorting works (bound DataTable).
- **Out of scope (told user):** the top header (title/user/Update/Logout) and the sidebar are the **shell (MainForm)**, not this module — not touched (changing them affects every screen). Export-to-Excel/PDF left as optional.
- VERIFIED: MSBuild clean 0/0 (temp OutputPath). GUI not clicked (no interactive desktop) — the layout is pure docking/TableLayoutPanel so it can't overlap; user should rebuild in VS and eyeball on B + at different DPI.

### 2026-08-04 — Certificate Request new flow: Create → Find/Print → Pay → Release; park to Waiting-to-Release; continuous queue numbers; kiosk reclaim-by-ticket
- Reworked the CTC lifecycle per user spec. Old flow: Create Request → straight to Fees (ForPayment) → Release. New flow: **Create Request → Find/Print the certificate (real printer) → Fees & Payment → Release**. No fee is assessed until the certificate is actually produced.
- **Schema `Database/22_certificate_workflow.sql`** (applied to live `croms`, in csproj): `transactions.status` ENUM gains **`ForPrint`** (created, staff must locate + print the CTC) and **`WaitingToRelease`** (parked before payment). Added `parked_at` / `parked_reason` / `parked_ticket` columns (guarded by an information_schema procedure — MySQL has no ADD COLUMN IF NOT EXISTS). Idempotent.
- **Certificate Request (`CertificateRequestForm`):** `btnCreate` now opens the txn at **ForPrint** (cert request 'Processing'), then shows a new in-file modal **`CertificatePrintForm`** (Find/Print step): a **🖨 Find & Print Certificate** button that pulls the linked births/deaths/marriages record and prints a **Certified True Copy / Certification** on a real printer (`PrintDocument`+`PrintDialog`, LGU header + record fields + certification statement + registrar signature line), then two choices — **✔ Proceed to Payment** (→ status ForPayment, jumps to Fees with the txn preselected) or **⏸ Put to Waiting-to-Release** (→ status WaitingToRelease, stamps parked_at/reason/queue-ticket, no payment). Closing the dialog leaves it ForPrint (still findable in the Waiting list). Client name is captured before ClearForm so the dialog has it.
- **Release & Claim (`ReleaseClaimForm`):** left card now has **two tabs — "For Release" and "Waiting to Release"** (`_listMode`). Waiting tab lists `WaitingToRelease` + `ForPrint` txns (with a Queue Ticket column) and shows a **➡ Resume — Send to Payment** button that sets the txn to ForPayment and jumps to Fees. Third summary tile changed to **WAITING** count. The center Release button is now guarded to only hand over a paid **ForRelease** txn (a parked/awaiting-print one must be Resumed first). Search also matches `parked_ticket`.
- **Continuous queue numbers (kiosk `NextNum`):** dropped the `AND DATE(created_at)=CURDATE()` filter — the number is now **global MAX+1 and never resets** (today ends Q-005, tomorrow starts Q-006), so it is unique forever and doubles as the reclaim key. Per-day client counts still come from counting tickets by date. (Reverses the long-standing "queue number resets daily so can't identify old request" seam — that was the whole point.)
- **Kiosk reclaim-by-ticket:** the **CLAIM** service (relabelled **"Release & Claim (Pick-up)"**) field is now **"Queue Ticket Number (from your last visit)"**. On submit, `ResolveParkedByQueue(entered)` maps the typed Q-number (Q-006 / 006 / 6) to the id of its PARKED transaction (WaitingToRelease/ForPrint). If matched → **reclaim**: the new ticket is forced to **Priority** (go first) and linked (`queue_tickets.transaction_id`) to that same parked transaction, so staff resolve it from the queue number and finish it (Resume → Pay → Release); no new claim QR is created. Falls back to the existing claimapp CLM lookup, then to a brand-new claim request (blank). Returning client flow is now: come back → kiosk → Release & Claim → type old Q-number → priority queue → window finishes it.
- Same-day / never-left clients are unchanged: found → pay → release right at the window (webcam claimant photo, no QR, no second kiosk trip). The QR/claimapp ID-upload path remains for the come-back-later CLM claims.
- VERIFIED: MSBuild clean (both `CROMS` and `CROMS.Kiosk`, temp OutputPath, 0 errors). Live-`croms` smoke test: parked txn + linked queue ticket → Waiting list shows it, `ResolveParkedByQueue(90007)` returns the txn, continuous NextNum = MAX+1, For-Release list correctly excludes the parked one; test rows cleaned up. GUI/printer not exercised (no interactive desktop). Rebuild both in VS on each PC to pick it up.
- FOLLOW-UP same day: added a **📢 Call / Recall Client** button to the Find/Print dialog (`CertificatePrintForm`). Voice-calls the client's queue number to the window via the same `System.Speech` engine the queue board uses (`SpeechSynthesizer.SpeakAsync`, guarded — no audio never crashes; `Spell()` reads "Q-006" as "Q 0 0 6"). Each press increments a recall counter shown on the button ("Recall Client (Q-006) · called 2×"); after the 2nd call the status line prompts the no-show path. Park button relabelled "No-show — Put to Waiting-to-Release". Dialog height 420→476 to fit the new button; constructor now takes the queueCode. `_voice` disposed on FormClosed. Build clean.
- FOLLOW-UP same day: **claimapp QR now shows ON the kiosk Step-2 screen** (was previously only on the post-print confirmation, so users didn't see it). When the client picks **Release & Claim (Pick-up)**, the right card swaps the webcam for a **claimapp QR** (`ApplyClaimMode`): title "Upload Your ID", a 300px QR (`QrHelper.TryCreate("CROMS-CLAIM:"+token)`), and the claim ticket number. The claim_requests row + token are created lazily when the QR is shown (`EnsureClaimQr`, guarded so it's made once) with blank names OK; at submit `FinalizeClaimRow` stamps the final name and links `transaction_id` (to the parked txn for a reclaim). `CreateClaimRequest` now tolerates blank names and labels the request "Release & Claim (Pick-up)". `ResetForm` clears the QR/token for the next client. Removed the obsolete CLM-reprint path from the ticket field — the field is now ONLY the Q-number reclaim key (blank = new claim). QRCoder 1.4.3 is referenced by the kiosk csproj + present in its bin, so the QR renders. VERIFIED: kiosk clean-compiles (temp OutDir; the bin\Debug copy was blocked only because the kiosk exe was running); claim_requests insert+finalize(link) smoke-tested on live croms + cleaned up. User must close the running kiosk + rebuild to see it.
- FOLLOW-UP (same day): kept the **webcam photo AND the QR together** for Release & Claim (user wanted the picture too). `ApplyClaimMode` now shrinks the camera frame to the left half (250px) and shows the QR panel in the right half (258px) instead of swapping the camera out; `_picCam` anchored all-sides so it resizes with the frame; camera runs in both modes; the Capture Photo button + Camera Ready indicator stay below. Right-card title becomes "Your Photo + Upload ID". Kiosk clean-compiles (temp OutDir; local kiosk exe running blocks the bin copy).

### 2026-08-06 — Claim QR is now a deep-link URL (stock phone camera opens claimapp directly)
- Problem: the claim QR encoded plain text `CROMS-CLAIM:<token>`, so scanning it with a phone's STOCK camera just displayed the text — nothing opened. It only worked via the claimapp's in-app jsQR scanner (which the client first had to reach by typing the https URL). Fixed so the ordinary phone camera opens the claimapp straight onto the ID-upload page.
- **QR payload changed to a URL** `https://<server-ip>:4300/?claim=<token>`. claimapp's Home constructor ALREADY reads `?claim=`/`?token=` from `location.search` and jumps to `/upload`, so a stock-camera open now deep-links with no in-app scan.
- **New `ClaimLink.Build(token)` helpers (one per app):**
  - Main app `Data/ClaimLink.cs` — host/port/scheme from the auto-started `IonicServerManager.ClaimApp` (detected LAN IPv4, 4300, https via `ng serve --ssl`); falls back to `IonicServerManager.DetectLanIp()` if the server hasn't reported its IP yet.
  - Kiosk `ClaimLink.cs` — host = the saved DB server (`ServerConfig.Host`, exposed new; claimapp runs on that same PC); if empty/loopback (kiosk ON the server) uses the kiosk's own LAN IPv4. Scheme/port App.config-overridable (`ClaimAppScheme`/`ClaimAppPort`, default https/4300).
- **Swapped every QR generator** from `"CROMS-CLAIM:"+token` to `ClaimLink.Build(token)`: kiosk 3 sites (step-2 on-screen QR `EnsureClaimQr`, printed ticket, ShowTicket confirmation dialog) + main app `ClaimQr.Show` (on-screen + `PrintSlip`).
- **Backward-compatible scanners:** both `extractToken` (claimapp home.page.ts, in-app jsQR) and `ClaimFormForm.ExtractToken` (desktop USB scanner) now parse a URL first (`[?&](claim|token)=…`), then the legacy `CROMS-CLAIM:` prefix, then a raw token — so old printed QRs and USB scanners still resolve.
- Updated all client-facing hints from "Scan in claimapp…" to "Scan with your phone camera to upload your ID."
- VERIFIED: MSBuild clean 0/0 for CROMS + CROMS.Kiosk (temp OutputPath); claimapp `ng build` EXIT 0 (only pre-existing jsqr/tesseract ESM warnings). Not exercised on a real phone here (no device) — the deep-link path is the claimapp's existing, working `?claim=` handler; logic verified by compile + build.
- LIMITATIONS (told user): claimapp is served over self-signed HTTPS, so the phone shows a one-time certificate warning to accept before the page loads; and the phone must already be on the same Wi-Fi/hotspot as the server (a QR can't join a network). Rebuild CROMS + CROMS.Kiosk in VS on each PC to ship; the running `ng serve` hot-reloads the claimapp extractToken change.
- FOLLOW-UP same day (claimapp purpose clarified): the claimapp's only job is to upload the claimant's valid-ID photo so the releasing officer can compare it to the kiosk face photo. Previously the uploaded ID showed only inside the **Claim Form** modal (opened from Release & Claim via the Claim-by-QR button). Now the uploaded ID also shows **directly on the main Release & Claim screen**, beside the existing kiosk photo, so the officer sees both faces side by side without opening a dialog. Added `picUploadedId` + `lblUploadedIdCap` to `ReleaseClaimForm` right card (below "Client Photo (from kiosk)"); new `ShowUploadedIdFor(txnId)` loads `claim_requests.id_image` (+ OCR'd `id_*_name` shown in the caption "name on ID: …") for the selected transaction, called from `ShowPhotoFor`. Caption states "none (no claimapp upload)" / "not yet uploaded" when absent. Both images disposed+cleared on row-change and in `ClearClaimForm`. Added a local `JoinName` helper. VERIFIED: MSBuild clean 0/0 (temp OutputPath); claim_requests.id_image/id_first_name/transaction_id confirmed in schema 21. Rebuild CROMS in VS to see it.
- FOLLOW-UP same day (camera-free fallback + IP-picker fix): show the app URL below the claim QR so a client whose camera fails can type it, AND fixed a wrong-IP bug likely behind "claimapp won't load on the phone".
  - `ClaimLink.BaseUrl()` (both apps) returns the short base URL `https://<ip>:4300` (no token) — typeable by hand; `Build()` now = BaseUrl()+"/?claim=<token>".
  - **Kiosk step-2 QR panel** now shows under the QR: "No camera? On your phone open: https://<ip>:4300 then enter ticket: CLM-YYYY-####". Same fallback added to the main-app `ClaimQr.Show` dialog + printed 58mm slip.
  - **Manual entry now works by the printed ticket number.** Save-API `GET /api/claims/:token` and `POST /:token/id` changed to match `qr_token = ? OR claim_ticket_no = ?`, so a client typing the base URL → claimapp Home → entering "CLM-2026-0006" (the untypeable 32-char GUID token is no longer required) resolves the claim. claimapp Home manual-box hint/placeholder updated to the CLM ticket format.
  - **Wrong-IP fix (kiosk `ClaimLink.LanIp`):** replaced "first private IPv4" with a scored picker (gateway +8, hotspot +7, wireless +4, ethernet +3, virtual −6) mirroring the main app's DetectLan. On this dev PC (3 IPs: idle hotspot 192.168.137.1, Ethernet 192.168.10.50, Wi-Fi 192.168.100.111) the old code could return the idle hotspot IP the phone can't reach; the scored picker returns the Wi-Fi 192.168.100.111 (gateway+wireless). Likely the real cause of the phone not loading when the kiosk built the QR/URL.
  - VERIFIED: MSBuild clean 0/0 for CROMS + CROMS.Kiosk; claimapp `ng build` EXIT 0; server side confirmed live (port 4300 listening 0.0.0.0, node inbound firewall Allow, HTTPS serves the app). **Needs restart to take effect:** the save-API (node index.js) and CROMS (to rebuild kiosk + reload IP logic) — the running `ng serve` hot-reloads the claimapp changes. NOTE: self-signed HTTPS still shows a one-time phone cert warning (accept it); phone must be on the same Wi-Fi/hotspot.
